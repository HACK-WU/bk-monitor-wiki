{
  "Host场景视图生成与拆分链路": "### Host 场景视图生成与拆分链路\n- **入口**: `GetSceneViewResource.perform_request(params)`\n- **路径**: `bkmonitor/packages/monitor_web/scene_view/resources/view.py`\n- **核心链路**:\n  1. `perform_request` → 查询 `SceneViewModel` 视图配置\n  2. `get_view_config(view, params)` → 分发到 `HostBuiltinProcessor`\n  3. `HostBuiltinProcessor.get_view_config` → 读取 `host.json`/`process.json`，调用 `get_auto_view_panels(view)`\n  4. `get_auto_view_panels` → `get_panels(view)` + `get_order_config(view)` + `sort_panels(...)`\n- **关键发现**: `get_auto_view_panels` 已天然按 `view.id`（`\"host\"`/`\"process\"`）区分，返回 `(panels, order)` 元组。拆分 4 个新接口时可直接复用，分别取 `panels` 或 `order`。\n- **文件**: `builtin/host.py`（`HostBuiltinProcessor`、`get_auto_view_panels`）, `builtin/utils.py`（`sort_panels`）",
  "version": 1,
  "updatedAt": "2026-07-07T07:55:21.018Z",
  "进程列表字段补全可行性": "### GetHostProcessListResource 字段补全调研结论\n- **现状**: 当前仅返回 `{\"status\", \"name\", \"id\"}`，前端期望 `ProcessItem` 共 14 个字段。\n- **P0（直接补全）**: `name`→`display_name`，`bind_ip`，`port`，`protocol` — CMDB 已有，改映射即可。\n- **P1（实时查 system.proc）**: `pid`、`cpuUsage`→`cpu_usage_pct`、`memUsage`→`mem_usage_pct`、`uptime`、运行状态 — 需通过时序查询接口按 `bk_host_id`+`display_name` 取最新数据。\n- **P2（需改 Model）**: `startCommand`→`start_cmd`、`created_at`→`create_time` — 受限于 `Process` 类无 `_extra_attr` 兜底。\n- **路径**: `scene_view/resources/host.py`（`GetHostProcessListResource`）, `cc/resources/cmdb.py`（`get_process_info`）, `api/cmdb/define.py`（`Process` 类）"
}