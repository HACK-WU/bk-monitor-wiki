# 关键决策：Issue 专家

> 契约快照补建于 2026-08-31（本次仅补建 C5；证据挖掘基于 HEAD `4827f0d7`，专家资产最后更新于 2026-08-03）。
> 优先记录**有证据支撑**的决策；无直接证据但代码结构强烈暗示的已前置 `[推测]`；**纯猜测不写入**。
> 证据来源限三类：**设计文档/Wiki 明写**、commit message/PR 描述明说、代码注释/docstring 明写。
> 本模块是少数**有配套设计文档**的模块（Wiki：`bk-monitor-wiki/wiki/告警系统设计/Issue功能/`），其中「关键设计决策」表是本 C5 的主要证据来源之一。
> 路径约定：`issue/` 相对 `bkmonitor/packages/fta_web/issue/`；`issue_processor.py` / `tasks/issue_tasks.py` 相对 `bkmonitor/alarm_backends/service/fta_action/`；`documents/issue.py` / `issue_merge/` 相对 `bkmonitor/bkmonitor/`；`constants/issue.py` 相对 `bkmonitor/`。
> 本文件是**父专家级**决策（跨域/模块级）；子专家域内的细节决策见各子专家目录。

## 决策清单

### 决策 1：接口契约-指纹每个元素带 prefix（`strategy:` / `key=value`），防维度错位
- **分类**：接口契约
- **动机**：避坑（不同维度组合算出同一指纹 → 不相关告警被错误聚合到同一 Issue）
- **决策**：`gen_issue_fingerprint` 构造 payload 时，每个元素都带语义前缀——策略段为 `strategy:{strategy_id}`，维度段为 `{key}={value}`；维度按 key 排序后参与计算（顺序无关）。
- **背景约束**：底层 `count_md5` 内部使用 `list_sort=True`，若只传裸值，`{a:X, b:Y}` 与 `{a:Y, b:X}` 排序后结果相同，会产生维度错位的误聚合。
- **被否决方案**：直接把维度值按序拼接后 md5：否决理由 —— 见上述背景，裸值在 `list_sort=True` 下会丢失 key 与 value 的绑定关系（Wiki 明确列出该反例）
- **已知代价**：指纹串比裸值拼接更长；prefix 格式一旦确定即成为事实契约，改动会导致存量 Issue 无法按新指纹命中（需配套迁移）。
- **重新评估触发条件**：聚合维度模型变更（需同时评估存量 fingerprint 的迁移方案）
- **关联代码**：`gen_issue_fingerprint` @ issue_processor.py
- **证据来源**：Wiki《Issue 聚合引擎》「指纹计算 → 关键设计决策」表（「每个元素带 prefix，防止 count_md5 内部 list_sort=True 导致维度错位（如 {a:X,b:Y} 与 {a:Y,b:X} 排序后相同）」）

### 决策 2：接口契约-指纹取值源必须是 `origin_alarm.data.dimensions`，不能用 `alert.dimensions` 或 event 顶层
- **分类**：接口契约
- **动机**：避坑（维度取值源选错 → 聚合维度取不到值 → 告警不进任何 Issue）
- **决策**：指纹计算与 `conditions` 匹配统一从 `origin_alarm.data.dimensions` 取值（adapter 收编前的原始 record）。
- **背景约束**：`issue_config.aggregate_dimensions` 的命名层级与原始告警维度严格一致。
- **被否决方案**（Wiki 明确列出两条）：
  - 用 `alert.dimensions`：否决理由 —— trigger 阶段会将主机维度收编为 `target_type` / `target`，命名层级错位
  - 用 event 顶层字段：否决理由 —— adapter 已把 `bk_host_id` / `bk_target_ip` 等关键字段 pop 走
- **已知代价**：取值路径依赖告警文档的内部结构（`origin_alarm.data.dimensions`），上游 adapter 若调整结构会直接影响聚合。
- **重新评估触发条件**：告警文档结构调整（需同步评估所有指纹与条件匹配取值点）
- **关联代码**：`gen_issue_fingerprint`、conditions 匹配分支 @ issue_processor.py
- **证据来源**：Wiki《Issue 聚合引擎》「取值源选型说明」三条（含两条否决理由）

### 决策 3：接口契约-聚合维度缺失时指纹返回 `None`，跳过该告警而非降级聚合
- **分类**：接口契约
- **动机**：避坑（维度凑不齐的告警若降级聚合，会混入不相关的 Issue 池，污染聚合语义）
- **决策**：`gen_issue_fingerprint` 遍历 `sorted(aggregate_dimensions)` 时，任一维度的取值为 `None` 或空串 → **立即返回 `None`**；调用方据此跳过该告警，不创建也不关联任何 Issue。空 `aggregate_dimensions` 是唯一例外，退化为 `count_md5(["strategy:{id}"])`（一策略一 Issue 的 catch-all 路径）。
- **背景约束**：「同一具体问题」的判定依赖完整维度组合，缺一维就无法确认是同一个问题。
- **被否决方案**：维度缺失时用空值占位继续计算：否决理由 —— 会让不同维度完整度的告警算出同一指纹（Wiki：「避免『维度凑不齐』的告警进入任何 Issue 池」）
- **已知代价**：**告警静默不聚合**——策略配置了告警中不存在的聚合维度时，该策略下所有告警都不会产生 Issue，且不会报错（C0 已知问题 1）。排查成本高。
- **重新评估触发条件**：需要「维度缺失」的可观测手段（当前只在指纹层静默跳过）
- **关联代码**：`gen_issue_fingerprint` @ issue_processor.py
- **证据来源**：Wiki《Issue 聚合引擎》「关键设计决策」表（「维度缺失时返回 None → 调用方据此跳过该告警」「空 aggregate_dimensions 退化为 catch-all」）；C0 已知问题 1

### 决策 4：接口契约-高基数防护改为 warn-only，超阈值不阻塞新建
- **分类**：接口契约
- **动机**：避坑（原实现触达阈值即阻塞新建，导致超阈值后该策略的告警**永久失联**）
- **决策**：单策略活跃 Issue 数超过 `ISSUE_MAX_ACTIVE_PER_STRATEGY` 时**仅上报 metric + warning**，不阻塞新建；通过 `bkmonitor_issue_fingerprint_blocked{reason=high_cardinality}` 速率告警让运维发现。ES count 结果缓存到 Redis（5 分钟 TTL ± 20% jitter 打散 thundering herd），缓存 miss 时用 `SET NX EX 10s` 短锁让一个 worker 探 ES，其他 worker 跳过。
- **背景约束**：高基数（维度组合爆炸）会拖垮 ES 与聚合链路，但阻塞新建的代价比高计数更严重。
- **被否决方案**：**历史实现**：触达阈值时 `return False` 阻塞新建 —— 否决理由（Wiki 明写）：「这会导致超阈值后该策略所有告警永久失联」
- **已知代价**：阈值失效后高基数风险靠监控发现，而非系统自保护；ES 仍需承受高基数的查询压力。
- **重新评估触发条件**：出现高基数把 ES 打挂的生产事件（届时需引入真正的限流/降级，而非简单恢复阻塞）
- **关联代码**：`_check_active_issue_count` @ issue_processor.py
- **证据来源**：Wiki《Issue 聚合引擎》「高基数防护 → 设计决策」段（含历史实现与其否决理由、缓存与短锁参数）

### 决策 5：架构设计-活跃 Issue 三级查找 + Legacy 迁移哨兵，保证模型切换窗口期可用
- **分类**：架构设计
- **动机**：性能取舍（Redis 缓存最快路径）+ 兼容处理（部署窗口期旧模型 Issue 仍需可命中）
- **决策**：`_find_active_issue` 三级查找——Step 0 Redis 缓存（`ISSUE_ACTIVE_CONTENT_KEY`）→ Step 1 ES 标准查找（`fingerprint + strategy_id + ACTIVE_STATUSES`，命中回填缓存）→ Step 2 Legacy 兜底（**仅当** legacy 迁移哨兵未完成时，查 `strategy_id + fingerprint=null + ACTIVE_STATUSES`，read-only，不写缓存不抢锁）。迁移由 `post_migrate` hook 触发 `migrate_legacy_active_issues`，全局哨兵 `ISSUE_LEGACY_MIGRATION_DONE_KEY` 标记完成，周期任务 `_renew_legacy_migration_done_sentinel_if_needed` 负责续命（防 30 天 TTL 失效后 processor 退化到 fallback 全索引查询）。
- **背景约束**：fingerprint 改造把模型从「1:1（strategy ↔ 活跃 Issue）」升级为「按维度组合切分」；**存量 `fingerprint=null` 的活跃 Issue 在新模型下无法可靠归属，必须在新代码生效前强制关闭**（`migrate_legacy_active_issues` docstring 原文）。
- **被否决方案**：不做迁移、让 legacy Issue 自然过期：否决理由 —— 旧模型活跃 Issue 在新模型下无法归属，会与新 Issue 并存造成同策略双活跃
- **已知代价**：迁移哨兵是有状态的 Redis key，续命失败会导致 processor 退化到 fallback ES 查询；Legacy 兜底路径只应在短期窗口触达，长期残留说明迁移未完成。
- **重新评估触发条件**：全量环境迁移完成后移除 Step 2 兜底与哨兵续命任务
- **关联代码**：`_find_active_issue` / `_legacy_migration_done` @ issue_processor.py；`migrate_legacy_active_issues` @ documents/issue.py；`_renew_legacy_migration_done_sentinel_if_needed` @ tasks/issue_tasks.py
- **证据来源**：Wiki《Issue 聚合引擎》「活跃 Issue 查找 → 三级策略说明」「部署窗口期兼容 → Legacy 迁移机制」；`migrate_legacy_active_issues` 函数 docstring

### 决策 6：架构设计-并发控制用 fingerprint 粒度 Redis 锁 + Token 锁（Lua）防误删
- **分类**：架构设计
- **动机**：避坑（并发创建同一 fingerprint 的 Issue → 同策略同维度出现多个活跃 Issue）
- **决策**：`_acquire_lock` 按 fingerprint 加锁（不同 fingerprint 互不阻塞），一次性尝试 `SET NX EX`（失败即返回 `None`，不等待不重试）；获锁后**二次确认**无活跃 Issue 才创建；释放走 `_TokenLock` 的 Lua 脚本，仅当 value 与自身 token 相等才 `del`。
- **背景约束**：告警处理是多 worker 并发，同一 fingerprint 的告警可能同时到达；锁 TTL 过期后若直接 `del`，会误删其他 worker 持有的锁。
- **被否决方案**：
  - 全局锁 / 按策略加锁：否决理由 —— 不同 fingerprint 互相阻塞，吞吐下降（Wiki 明确「锁粒度：按 fingerprint，不同 fingerprint 互不阻塞」）
  - 普通 `del` 释放锁：否决理由 —— TTL 过期后可能误删其他 worker 持有的锁
- **已知代价**：抢锁失败即跳过本次聚合（该告警本次不进 Issue），依赖周期任务 `backfill_unlinked_alerts` 补偿；锁 TTL 内 worker 崩溃需等 TTL 自然过期。
- **重新评估触发条件**：出现「抢锁失败导致告警长期未关联」的反馈（需评估补偿任务的覆盖度与时效）
- **关联代码**：`_acquire_lock`、`_TokenLock` @ issue_processor.py
- **证据来源**：Wiki《Issue 聚合引擎》「并发控制」节（锁粒度/获取方式/释放安全/TTL 表 + Lua 脚本 + 「避免 TTL 过期后误删其他 worker 持有的锁」）

### 决策 7：接口契约-LLM 标题派发：两级闸门 + 仅新建触发 + 延迟导入 + 异常静默
- **分类**：接口契约
- **动机**：可维护性（避免启动期依赖未就绪模块）；可观测性（失败不影响主链路）
- **决策**：`_maybe_dispatch_llm_title` 四项约定：
  - **两级闸门**：部署级 env `ENABLE_ISSUE_LLM_TITLE`（由 helm chart 按 llmWorker 有效容量派生注入，env 不存在即不派发）+ 运行时业务白名单 `is_llm_title_enabled_for_biz`
  - **仅新建时派发**（`created=True`），查找已有 Issue 不触发
  - **导入延迟化**（`from ... import` 写在函数内），避免启动时依赖未就绪的 LLM 模块
  - **异常全捕获**，`logger.warning` 记录，不影响主链路（标题是体验增强非关键数据）
- **背景约束**：LLM 依赖是可选的外部能力，且其模块启动顺序晚于聚合链路。
- **被否决方案**：模块顶层 import LLM 依赖：否决理由 —— 启动时依赖未就绪的 LLM 模块（Wiki 明写）
- **已知代价**：标题生成失败对用户不可见（只有日志）；两级闸门意味着默认关闭，需部署与业务白名单同时放行。
- **重新评估触发条件**：LLM 能力成为默认依赖（可移除 env 闸门）；或需要向用户暴露标题生成失败状态
- **关联代码**：`_maybe_dispatch_llm_title` @ issue_processor.py；`generate_issue_llm_title` / `refresh_issue_llm_title_examples` @ tasks/issue_tasks.py
- **证据来源**：Wiki《Issue 聚合引擎》「LLM 标题派发 → 两级闸门表 + 设计决策三条」

### 决策 8：接口契约-Issue 名称唯一性分场景：用户改名强制唯一，系统自动标题允许重名
- **分类**：接口契约
- **动机**：避坑（把给用户的约束套到系统自动标题上，会让自动标题被卡回默认名）
- **决策**：`IssueDocument.rename(new_name, operator, enforce_unique=True)` 用 `enforce_unique` 区分两种调用方：
  - **用户改名（默认 `True`）**：人手填重名是误操作，撞名抛 `IssueNameDuplicatedError` 拦下
  - **系统自动标题（`False`）**：同类错误天然生成相同标题（标题描述错误、不含实例维度），实例区分交由 Issue 的结构化维度承担，**允许重名**
- **背景约束**：自动标题只描述「是什么错」，不携带实例维度，因此同策略同类错误的多个 Issue 标题必然相同。
- **被否决方案**：对系统自动标题也强制唯一：否决理由（docstring 原文）——「不应被给用户用的唯一性约束卡回默认名」
- **已知代价**：同业务下可能存在多个同名 Issue（系统生成的），依赖结构化维度区分；按名称检索时需预期重名结果。
- **重新评估触发条件**：自动标题开始携带实例维度（可恢复强制唯一）
- **关联代码**：`IssueDocument.rename` @ documents/issue.py；`build_issue_default_name` @ issue_processor.py
- **证据来源**：`rename` 方法 docstring（「用户改名（默认 True）：人手填重名是误操作，撞名抛 IssueNameDuplicatedError 拦下。系统自动标题（False）：同类错误天然生成相同标题…允许重名；不应被给用户用的唯一性约束卡回默认名」）

### 决策 9：接口契约-系统自动改名的 `operator` 固定为 `system`，审计人另记 `content`
- **分类**：接口契约
- **动机**：避坑（把审计人写进 `operator` 会污染下游「是否为系统改名」的判据）
- **决策**：`rename` 增加可选 `content` 参数用于写入 `NAME_CHANGE` 活动日志。LLM 标题生成路径下 `operator` **固定为 `system`**（保证「是否系统改名」的判据稳定），非 system 的真实发起人标识另记于 `content`，而不是写进 `operator`。用户手工改名不传 `content`。
- **背景约束**：下游功能（如「是否系统改名」判断、标题来源判别）依赖 `operator` 取值稳定；而审计需要知道真实发起人。
- **被否决方案**：把真实发起人写进 `operator`：否决理由（docstring 原文）——「避免把审计人写进 operator 污染下游标题来源判别」
- **已知代价**：审计信息分散在两个字段（`operator` / `content`），查询活动日志时需按场景解析。
- **重新评估触发条件**：活动日志模型增加独立的 `source` / `initiator` 字段（可移除 `content` 承载审计的约定）
- **关联代码**：`IssueDocument.rename` @ documents/issue.py；`IssueActivityType.NAME_CHANGE` @ constants/issue.py
- **证据来源**：`rename` 方法 docstring（`content` 参数说明段）

### 决策 10：安全合规-业务可见性过滤改 fail-close，「全部业务」查询在全量授权时可省略业务 terms
- **分类**：安全合规
- **动机**：避坑（过滤子句为空时业务过滤整体消失 → 越权可见）；优化（十万级 terms 撑爆查询 DSL）
- **决策**：`add_biz_condition` 显式收口四种情形——已构造业务子句取并集；**指定了业务范围但无任何业务子句 → 追加 `match_none` 失败关闭**；未指定业务范围保持各 Handler 既有语义；单租户 + 明确选择全部业务 + IAM 授予全量权限时可省略无区分度的业务 terms。省略需**同时满足**五条件：单租户模式、请求明确包含 `-1`、IAM 返回 `OP.ANY` 或启用 `skip_check`、授权业务非空、不存在真实未授权业务。同时把 `-1` 从 `unauthorized_bizs` 归一化移除（它是「全部业务」标记而非真实业务 ID）。
- **背景约束**：① 生产取证中授权业务列表可达 **11.5 万个**，展开后单次查询 DSL 约 **1.15 MB**，是 TopN 接口超时的放大因素之一；② 告警检索链路不能统一依赖 `bk_tenant_id`（历史索引存在完全没有该字段的数据）。
- **被否决方案**：
  - 维持「构造不出子句就原样返回」（fail-open）：否决理由 —— 分片资源透传 `authorized_bizs` / `unauthorized_bizs` 时 kwargs 通道允许出现 `([], [])`，业务过滤会整体消失
  - **通过「缓存中的当前空间集合恰好被授权集合覆盖」推断全量权限**：否决理由（commit 原文）——「避免缓存时序、新增业务或遗留索引文档使查询范围被错误放宽」
- **已知代价**：多租户模式、显式业务 ID 列表、无请求上下文、IAM 非全量策略四种情况仍保留业务 terms（不做省略）；fail-close 分支会收紧原本依赖空子句放行的调用方。
- **重新评估触发条件**：历史索引补齐 `bk_tenant_id` 后可评估多租户下的省略优化
- **关联代码**：`add_biz_condition` @ fta_web/alert/handlers/action.py（告警/故障/处理记录/Issue 共用的业务可见性入口）；`get_biz_filter_ids`；`Permission.filter_space_list_by_action_with_scope`
- **证据来源**：commit bdc35a27d2（body 的「问题/改动/租户隔离边界/验证/影响范围」五段，含上述两条否决理由与 11.5 万 / 1.15 MB 实测数据）；commit 71b100a2dd（`get_biz_filter_ids` 收口 `-1` 哨兵）；commit 96c8639cb0、f199ce93f3（ES terms 65536 限制导致权限过滤失效的前置修复）

### 决策 11：接口契约-查询条件无法构造时显式 `match_none`，不静默丢弃条件
- **分类**：接口契约
- **动机**：避坑（条件被静默丢弃 → 返回业务范围内全量数据，用户以为过滤生效）
- **决策**：全文检索场景下，词过短/过长且非枚举显示名时，显式返回 `Q("match_none")` 表示无命中。
- **背景约束**：ES 查询串在极端词长下无法构造有效 DSL；若返回「无过滤」则等价于全量。
- **被否决方案**：条件构造失败时忽略该条件继续查询：否决理由 —— 代码注释「避免静默丢掉条件后返回业务范围内全量数据」
- **已知代价**：用户遇到「查什么都为空」时会困惑（实际是条件被判定为无效），需前端配合提示。
- **重新评估触发条件**：调整最小/最大词长阈值；或改为向用户返回明确的「条件无效」错误
- **关联代码**：全文检索构造分支 @ fta_web/alert/handlers/base.py
- **证据来源**：代码注释（base.py：「词过短/过长且非枚举显示名：显式无命中，避免静默丢掉条件后返回业务范围内全量数据」）

### 决策 12：接口契约-以 `resolved_time` 为可靠解决时间，并按状态分流时间过滤
- **分类**：接口契约
- **动机**：避坑（`resolved_time` 残留会导致 `is_resolved` 误判；统一用 `create_time` 过滤会导致已解决 Issue 归属错分片）
- **决策**：四项约定——reopen / restore / 合并成员回到活跃时用 `skip_empty=False` **显式清空** `resolved_time`（避免残留值导致 `is_resolved` 误判）；合并成员转到 RESOLVED 时同步写入 `resolved_time`；查询时间过滤**按状态分流**（活跃不受时间约束、RESOLVED 按 `resolved_time`、ARCHIVED 按 `update_time`，非分片分支保留 `create_time <= end_time` 上界）；`is_resolved` 改为基于 `status == RESOLVED`（**排除 ARCHIVED**）。
- **背景约束**：Issue 的「解决时间」与「归档时间」是两个不同语义，统一按一个字段过滤会让已解决 Issue 在错误的时间分片中失踪。
- **被否决方案**：统一按 `create_time` 或 `update_time` 过滤：否决理由 —— 已解决 Issue 的归属与 `resolved_time` 不一致，会漏数据（C0 已知问题 5）
- **已知代价**：时间过滤规则分三支，新增状态或改状态语义时需同步调整分流逻辑。
- **重新评估触发条件**：状态机新增终态；或时间分片策略调整
- **关联代码**：状态流转方法（`resolve` / `reopen` / `restore`）@ documents/issue.py；时间过滤分流 @ fta_web/issue/handlers/issue.py
- **证据来源**：commit ee169861d1（body 四条改动，含「显式清空 resolved_time 避免 is_resolved 误判」「按状态分流」「is_resolved 排除 ARCHIVED」）；C0 已知问题 5

### 决策 13：架构设计-合并语义：member 状态门槛放开、main 仍须活跃、合并前终态不保真
- **分类**：架构设计
- **动机**：可维护性（member 并入即被冻结，自身状态不再权威，再要求它活跃是无意义的约束）
- **决策**：合并校验**仅保留「main 必须活跃」**，移除 member 状态白名单（已解决/已归档 Issue 可并入活跃主）；同时补 `ListIssueHistory` 的 active 冻结 member 排除（此前是唯一未做排除的列表路径）。
- **背景约束**：列表隐藏、防链式校验、聚合、级联**全部按「关系状态」而非 member 自身 ES status 判断**——这是放开门槛不留悬空假设的前提（commit「关键设计判断」原文）。main 是存活、对外聚合、吸告警的主体，故保留其活跃约束。
- **被否决方案**：维持「main 与所有 member 均须活跃」：否决理由 —— member 一旦并入即冻结，自身状态不再权威，后续由主状态级联与拆分重置接管
- **已知代价/边界（commit 明确标注为「已确认可接受」）**：
  - **member 合并前的终态不保留**：合并前已 RESOLVED/ARCHIVED 的 member，主 reopen/restore 级联会把它复活成 UNRESOLVED、拆出时重置为待审核，**不回到合并前的终态**。这与「合并=同根因、拆分=重新独立评审」语义一致，属有意为之；如需保真需在关系表落 pre-merge 状态快照（本 PR 不做）
  - **「同 fingerprint 双活跃」**（resolve → 同指纹再生 → reopen）是系统既有、设计容忍的良性状态：路由层 `_find_active_issue` 按 `-create_time` 取最新活跃，确定收敛、不冲突
- **重新评估触发条件**：需要保真 member 合并前终态（需引入 pre-merge 状态快照）
- **关联代码**：Issue 合并视图 @ kernel_api/views/v4/issue.py；`ListIssueHistory` @ fta_web/issue/resources.py；`IssueMergeResolver` @ issue_merge/
- **证据来源**：commit 4024a7b0b5（body 含「背景/改动/关键设计判断/边界说明（行为变化，已确认可接受）/测试」五段）；相关 commit f851490e11（合并/拆分功能）、b26382f4d4 与 ca6011523f（合并/拆分依据 reasons 改为非必填）、bd7c598464（合并后主 Issue 聚合被并入告警）

### 决策 14：安全合规-TAPD OAuth 回调：区分用户主动取消与系统错误，并防御 CRLF 日志注入
- **分类**：安全合规
- **动机**：避坑（用户取消授权被误判为系统错误 → 用户看到错误页且无法区分原因）；安全（外部回调参数直接进日志）
- **决策**：TAPD OAuth 回调三条约定——**`signed_state` 验签前置**（确保 `error_url` 来自受信任签名）；检测到 `error` 参数时识别为「用户主动取消」，友好重定向到 `error_url` 并记 **info 日志**（不当作系统错误）；记录 `error_description` 前**剔除 CRLF**，防御日志注入。缺少 `code` 的非取消场景同样重定向到 `error_url`。
- **背景约束**：用户取消授权时 TAPD 重定向回 `redirect_uri` 并携带 `error=access_denied`（无 `code`），原逻辑把「无 code」统一重定向到根路径，误判为参数缺失。
- **被否决方案**：维持「无 code 即重定向根路径」：否决理由 —— 无法区分用户主动取消与真实系统错误，排障与体验都受损
- **已知代价**：`error_url` 依赖签名校验，签名配置错误会导致取消流程不可用；`signed_state` 的签名截断为 hex 16 字符（兼顾安全性与 URL 长度），是安全强度与可用性的折中。
- **重新评估触发条件**：TAPD 回调参数协议变化；或需要把「用户取消」计入运营指标
- **关联代码**：`tapd_user_oauth_callback`（用户态 OAuth 回调）@ fta_web/issue/resources.py；`verify_signed_state` / `generate_signed_state` @ fta_web/issue/utils/tapd.py；`tapd_app_install_callback`（应用态授权回调，同为验签前置）@ fta_web/issue/resources.py
- **证据来源**：commit 9bd1cf3213（body 四条改动，含「将 signed_state 验签前置，确保 error_url 来自受信任签名」「记录 error_description 前剔除 CRLF，防御日志注入」）；代码注释（resources.py：「用户主动取消授权：TAPD 重定向回 redirect_uri 并携带 error=access_denied / 属正常业务分支，重定向到 error_url 展示友好提示，不当作系统错误」）；`generate_signed_state` docstring（「签名截断至 hex 16 字符，兼顾安全性与 URL 长度；payload 必须包含 exp（建议 TTL = 15min）」）；相关 commit 30df862d4d（TAPD 未授权响应状态码改为 200 并同步前端请求方法）
