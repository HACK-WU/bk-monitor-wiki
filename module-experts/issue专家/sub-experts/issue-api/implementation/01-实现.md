# Issue API 实现文档

> 子专家：[agent.md](agent.md)
> 最后更新：2026-07-31

## 文件结构

```
fta_web/issue/
├── __init__.py
├── API_DOC.md
├── resources.py      # 142KB - 所有 Resource 类
├── views.py          # 13KB - IssueViewSet + 权限
├── serializers.py    # 1.6KB - 序列化器
├── urls.py           # 1.2KB - 路由
├── handlers/
│   ├── __init__.py
│   ├── issue.py      # 56KB - IssueQueryHandler + IssueQueryTransformer
│   └── translator.py
├── utils/
│   ├── __init__.py
│   └── tapd.py       # 12KB - TAPD 工具函数
└── docs/
```

## IssueViewSet 注册

```python
# views.py
class IssueViewSet(ResourceViewSet):
    READ_ONLY_ENDPOINTS = {
        "issue/search", "issue/detail", "issue/activities",
        "issue/history", "issue/top_n", "issue/export",
        "issue/recent_assignees", "issue/trend",
        "issue/get_tapd_fields", "issue/search_tapd_items",
        "issue/tapd_relations", "tapd/workspace", "tapd/user_workspace",
    }
    NO_BIZ_REQUIRED_ENDPOINTS = {
        "issue/search", "issue/top_n", "issue/recent_assignees",
        "tapd/workspace", "tapd/user_workspace",
    }
    TAPD_ENDPOINTS = {
        "issue/get_tapd_fields", "issue/search_tapd_items",
        "issue/create_tapd", "issue/link_tapd",
        "issue/tapd_relations", "tapd/user_workspace",
        "tapd/unbind_workspace", "tapd/rebind_workspace",
        "tapd/revoke_auth",
    }
```

## 批量操作框架

```python
# resources.py
def _run_batch(issues: list[dict], action_fn: Callable, max_workers=10) -> dict:
    """
    并发执行批量操作，单条失败不影响其他条目。
    返回: {"succeeded": [...], "failed": [{"bk_biz_id", "issue_id", "message"}]}
    """
```

## 新增端点模板

```python
# 1. resources.py - 创建 Resource 类
class IssueNewFeatureResource(Resource):
    def perform_request(self, validated_request_data):
        # 业务逻辑
        return result

# 2. views.py - 注册端点
class IssueViewSet(ResourceViewSet):
    READ_ONLY_ENDPOINTS = {...}  # 或 MANAGE 写操作

# 3. urls.py - 添加路由
router.register(r"new_feature", IssueNewFeatureResource, basename="issue_new_feature")
```

## API Gateway 接口模板

```python
# api/issue/default.py
class NewFeatureResource(KernelAPIResource):
    action = "/app/issue/new_feature/"
    method = "POST"
    backend = IssueNewFeatureResource
```
