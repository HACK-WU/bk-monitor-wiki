# 05-接口 — Resource 与 Service 接口说明

> **子专家**: API资源子专家
> **创建时间**: 2026-07-28
> **类型**: 实现文档

---

## 一、Resource RESTful 端点

### 1.1 数据源相关

| Resource 类 | 功能 | 关键参数 |
|------------|------|---------|
| `CreateDataIDResource` | 创建数据源 ID | data_name, etl_config, operator, source_label, type_label, space_uid |
| `GetOrCreateAgentEventDataIdResource` | 获取/创建 Agent 事件 DataID | bk_biz_id |
| `ModifyDatasourceResultTable` | 切换结果表与数据源关联 | table_id, bk_data_id |

### 1.2 结果表相关

| Resource 类 | 功能 | 关键参数 |
|------------|------|---------|
| `CreateResultTableResource` | 创建结果表 | bk_data_id, table_id, table_name_zh, is_custom_table, schema_type, default_storage, field_list |
| `ListResultTableResource` | 查询结果表列表 | datasource_type, bk_biz_id, with_option, page, page_size |

### 1.3 集群相关

| Resource 类 | 文件 | 功能 | 关键参数 |
|------------|------|------|---------|
| `RegisterCluster` | cluster.py | 注册集群 | cluster_name, cluster_type, domain, port, username, password |
| `ModifyCluster` | cluster.py | 修改集群 | cluster_id + 可修改字段 |
| `DeleteCluster` | cluster.py | 删除集群 | cluster_id |
| `ListClusters` | cluster.py | 查询集群列表 | cluster_type, page, page_size |
| `GetStorageClusterDetail` | cluster.py | 获取集群详情 | cluster_id |

### 1.4 实体关系

| Resource 类 | 文件 | 功能 | 关键参数 |
|------------|------|------|---------|
| EntityHandler.apply | entity_relation.py | 创建/更新实体 | metadata: {namespace, name, labels}, spec |
| EntityHandler.get | entity_relation.py | 获取实体 | namespace, name |
| EntityHandler.list | entity_relation.py | 列出实体 | namespace |
| EntityHandler.delete | entity_relation.py | 删除实体 | namespace, name |

### 1.5 数据链路

| Resource 类 | 文件 | 功能 |
|------------|------|------|
| 数据链路 CRUD | `bkdata_link.py` (50.9 KB) | 计算平台数据链路管理 |
| 链路操作 | `datalink_operation.py` (18.1 KB) | 链路启停/迁移 |
| 日志链路 | `log_datalink.py` (39.1 KB) | 日志数据链路管理 |

### 1.6 空间与 VM

| Resource 类 | 文件 | 功能 |
|------------|------|------|
| 空间管理 | `space.py` (18.2 KB) | 空间 CRUD |
| VM 存储 | `vm.py` (13.1 KB) | VM 接入管理 |

---

## 二、Service 方法完整签名

### 2.1 数据源服务 (`data_source.py`)

```python
def modify_kafka_cluster_id(
    bk_data_id: int,
    topic: str | None = None,
    partition: int | None = None,
) -> None

def get_transfer_cluster() -> list[str]

def filter_data_id_and_transfer() -> dict[str, list[int]]

def stop_or_enable_datasource(
    bk_tenant_id: str,
    data_id_list: list[int],
    is_enabled: bool,
) -> bool

def query_biz_plugin_data_id_list(
    biz_id_list: list[int],
) -> dict[int, list[int]]

def modify_data_id_source(
    bk_tenant_id: str,
    data_id_list: list[int],
    source_type: str,  # "bkdata" | "bkgse"
) -> bool
```

### 2.2 存储详情服务 (`storage_details.py`)

```python
class ResultTableAndDataSource:
    def __init__(
        self,
        bk_tenant_id: str,
        table_id: str | None = None,
        bk_data_id: int | None = None,
        bcs_cluster_id: str | None = None,
        vm_table_id: str | None = None,
        metric_name: str | None = None,
        data_label: str | None = None,
        with_gse_router: bool | None = False,
    ): ...

    def get_detail(self) -> list[dict[str, Any]]: ...
    def get_basic_detail(self, data_id: int | None) -> dict[str, Any]: ...
    def get_data_source(self, bk_data_id: int) -> dict[str, Any]: ...
    def get_table_id(self, table_id: str) -> dict[str, Any]: ...
    def get_table_id_data_id(self) -> dict[str, Any]: ...
    def get_biz_info(self, table_id: str, data_source: dict) -> dict[str, Any]: ...
    def get_clusters(self, bk_data_id: int) -> dict[str, Any]: ...
    def get_storage_cluster(self, table_id: str) -> dict[str, Any]: ...
    def get_influxdb_instance_cluster(self, table_id: str) -> dict[str, Any]: ...
    def query_gse_router(self, bk_data_id: int) -> dict[Any, list[Any]]: ...

class StorageClusterDetail:
    @classmethod
    def get_detail(cls, bk_tenant_id: str, cluster_id: str | int) -> list[dict[str, Any]]: ...

    @classmethod
    def get_cluster(cls, bk_tenant_id: str, cluster_id: int) -> ClusterInfo: ...

    @classmethod
    def get_kafka_detail(cls, cluster_obj: ClusterInfo) -> list[dict[str, Any]]: ...

    @classmethod
    def get_influxdb_proxy_detail(cls, cluster_obj: ClusterInfo) -> list[dict[str, Any]]: ...

    @classmethod
    def get_es_detail(cls, cluster_obj: ClusterInfo) -> list[dict[str, Any]]: ...

    @classmethod
    def get_vm_details(cls, cluster_obj: ClusterInfo) -> list[dict[str, Any]]: ...
```

### 2.3 元数据同步服务 (`sync_metadata.py`)

```python
def parse_time(time_str: str) -> datetime | None: ...

def sync_kafka_metadata(
    bk_tenant_id: str,
    kafka_info: dict[str, Any],
    ds: DataSource,
    bk_data_id: int,
) -> None: ...

def sync_es_metadata(
    bk_tenant_id: str,
    es_info: list[dict[str, Any]],
    table_id: str,
) -> None: ...

def sync_vm_metadata(
    bk_tenant_id: str,
    vm_info: dict[str, dict[str, Any]],
) -> None: ...
```

### 2.4 空间 Redis 服务 (`space_redis.py`)

```python
def get_space_config_from_redis(space_uid: str, table_id: str) -> dict[str, Any]: ...

def get_kihan_prom_field_list(domain: str) -> list[str]: ...

def push_and_publish_es_aliases(bk_tenant_id: str, data_label: str) -> None: ...
```

### 2.5 ES/VM 存储服务

```python
# es_storage.py
class ESIndex:
    def query_es_index(self, table_id_list: list[str]) -> dict[str, Any]: ...

# vm_storage.py
def disable_influxdb_router_for_vm_table(
    table_ids: list[str],
    switched_storage_id: int | None = 0,
    can_deleted: bool | None = False,
) -> None: ...

def query_vm_datalink_all(bk_data_id: int) -> dict[str, Any]: ...

def query_bcs_cluster_vm_rts(bcs_cluster_id: str) -> dict[str, Any]: ...

def get_table_id_from_vm(
    bk_base_data_id: int | None = None,
    vm_table_id: str | None = None,
) -> str: ...
```

---

## 三、Resource 基类 API

### Resource 抽象基类

**文件**: [`resources/base.py`](file://bk-monitor/bk-monitor-base/src/bk_monitor_base/metadata/resources/base.py)

```python
class Resource(ABC):
    RequestSerializer: type[Serializer] | None = None
    ResponseSerializer: type[Serializer] | None = None
    many_request_data: bool = False
    many_response_data: bool = False

    def __init__(self, context: Any = None): ...
    def __call__(self, *args, **kwargs) -> Any: ...  # 线程安全入口
    def request(self, request_data=None, **kwargs) -> Any: ...  # 主流程
    def validate_request_data(self, request_data) -> Any: ...
    def validate_response_data(self, response_data) -> Any: ...
    @abstractmethod
    def perform_request(self, validated_request_data) -> Any: ...
```

**契约**:
- 子类必须实现 `perform_request()`
- 子类必须定义 `RequestSerializer`（可选但推荐）
- `__call__` 保证线程安全（每次创建新实例）
- 所有异常统一转换为 `BkApiError`

---

## 四、请求处理契约

### 4.1 请求生命周期

```
1. Resource()(request_data)  ← 线程安全入口
2. validate_request_data()   ← 参数校验（RequestSerializer）
3. perform_request()         ← 业务逻辑（子类实现）
4. validate_response_data()  ← 响应校验（ResponseSerializer）
5. 异常 → BkApiError         ← 统一异常转换
```

### 4.2 参数校验契约

- `TenantIdField` 自动从 request 注入 `bk_tenant_id`
- 校验失败抛出 `Exception`，由 `request()` 统一转换为 `BkApiError`
- `format_serializer_errors()` 将 DRF errors 转为人类可读格式

### 4.3 错误处理契约

```python
# 所有异常统一转换为:
BkApiError(
    module="metadata_resource",
    action=resource_name,
    method="",
    url=resource_name,
    message=str(e),
    status_code=HTTPStatus.BAD_REQUEST,
)
```
