# 01-架构

> **专家**: 任务调度与运维专家
> **文档类型**: 实现层 — 架构设计
> **创建时间**: 2026-07-28

---

## 一、任务调度架构总览

### 1.1 分层架构

```mermaid
graph TB
    subgraph "触发层"
        SCHEDULER["Celery Beat<br/>定时调度器"]
        CLI["Django manage.py<br/>命令行"]
        SIGNAL["Django Signal<br/>事件触发"]
    end

    subgraph "入口层"
        INIT["task/__init__.py<br/>任务导出"]
        CMD_ENTRY["management/commands/<br/>命令入口"]
    end

    subgraph "编排层"
        CR["task/custom_report.py<br/>自定义上报编排"]
        CFG["task/config_refresh.py<br/>配置刷新编排"]
        SPACE["task/sync_space.py<br/>空间同步编排"]
        BCS["task/bcs.py<br/>BCS编排"]
        BKBASE["task/bkbase.py<br/>计算平台编排"]
    end

    subgraph "执行层"
        TASKS["task/tasks.py<br/>Celery @app.task<br/>异步任务执行"]
    end

    subgraph "基础设施"
        CELERY["Celery Worker<br/>celery_metadata_task_worker<br/>celery_long_task_cron"]
        REDIS["Redis<br/>分布式锁/缓存"]
        CONSUL["Consul<br/>配置中心"]
        PROMETHEUS["Prometheus<br/>指标上报"]
    end

    SCHEDULER --> INIT
    CLI --> CMD_ENTRY
    SIGNAL --> TASKS
    INIT --> CR
    INIT --> CFG
    CMD_ENTRY --> SPACE
    CMD_ENTRY --> TASKS
    CR --> TASKS
    CFG --> TASKS
    SPACE --> TASKS
    BCS --> TASKS
    BKBASE --> TASKS
    TASKS --> CELERY
    TASKS --> REDIS
    TASKS --> CONSUL
    TASKS --> PROMETHEUS
```

### 1.2 核心设计原则

| 原则 | 实现方式 | 示例 |
|------|---------|------|
| **幂等性** | `@share_lock` 装饰器 + 唯一 identify | `@share_lock(identify="metadata_refreshInfluxdbRoute")` |
| **队列隔离** | 长耗时任务使用独立队列 | `queue="celery_long_task_cron"` |
| **容错性** | try/except 包裹 + 日志记录 | 单任务异常不影响其他任务 |
| **可观测性** | Prometheus 指标上报 | `METADATA_CRON_TASK_STATUS_TOTAL`、`METADATA_CRON_TASK_COST_SECONDS` |
| **重试机制** | `tenacity.retry` 指数退避 | `create_es_storage_index` 最多重试 4 次 |

---

## 二、Celery 任务注册与调度

### 2.1 任务注册

所有 Celery 任务通过 `@app.task` 装饰器注册：

```python
# task/tasks.py:55
from celery import current_app as app

@app.task(ignore_result=True, queue=settings.metadata.celery_queue or "celery_metadata_task_worker")
def refresh_custom_report_config(bk_biz_id: int | None = None):
    ...
```

- `ignore_result=True` — 不存储任务结果，减少 Redis 内存占用
- `queue` — 指定目标队列，通过 `settings.metadata.celery_queue` 可配置

### 2.2 队列设计

```mermaid
graph LR
    BEAT["Celery Beat"] -->|"定时触发"| Q1["celery_metadata_task_worker<br/>通用任务队列"]
    BEAT -->|"定时触发"| Q2["celery_long_task_cron<br/>长耗时任务队列"]
    Q1 --> W1["Worker 1"]
    Q1 --> W2["Worker 2"]
    Q2 --> W3["Long Worker 1"]
```

| 队列 | 任务类型 | 典型任务 |
|------|---------|---------|
| `celery_metadata_task_worker` | 通用元数据任务 | `refresh_custom_report_config`、`push_and_publish_space_router`、`access_to_bk_data_task` |
| `celery_long_task_cron` | 长耗时任务 | `manage_es_storage`、`clean_disable_es_storage` |

### 2.3 调度策略

**定时任务**通过 Celery Beat 配置周期触发：

- `check_event_update` — 每 3 分钟从 ES 同步事件维度
- `refresh_influxdb_route` — 定期刷新 InfluxDB 路由至 Consul
- `manage_es_storage` — 定期执行 ES 索引轮转
- `sync_bkcc_space` — 定期同步 CMDB 业务空间

**事件驱动任务**通过 Django Signal 或 API 调用触发：

- `create_es_storage_index` — 创建 ES 存储时异步触发
- `access_to_bk_data_task` — 结果表接入计算平台时触发
- `apply_log_datalink` — 日志配置变更时触发

---

## 三、管理命令分层

### 3.1 命令架构

```mermaid
graph TB
    subgraph "Django 管理框架"
        MANAGE["manage.py"]
        BASE["BaseCommand<br/>Django 内置基类"]
    end

    subgraph "命令实现层"
        SPACE_CMD["空间管理命令<br/>sync_cmdb_space/sync_bcs_space/..."]
        DS_CMD["数据源命令<br/>switch_kafka_for_data_id/..."]
        CLUSTER_CMD["集群命令<br/>sync_cluster_config/check_bcs_cluster_status/..."]
        DL_CMD["数据链路命令<br/>check_datalink_health/..."]
        OPS_CMD["运维命令<br/>clean_old_consul_config/..."]
        INIT_CMD["初始化命令<br/>init_space_data/init_redis_data/..."]
    end

    subgraph "业务层"
        MODELS["models/<br/>数据模型"]
        TASK["task/<br/>Celery 任务"]
        SERVICE["service/<br/>服务层"]
    end

    MANAGE --> BASE
    BASE --> SPACE_CMD
    BASE --> DS_CMD
    BASE --> CLUSTER_CMD
    BASE --> DL_CMD
    BASE --> OPS_CMD
    BASE --> INIT_CMD
    SPACE_CMD --> MODELS
    SPACE_CMD --> TASK
    DS_CMD --> MODELS
    CLUSTER_CMD --> MODELS
    CLUSTER_CMD --> SERVICE
    DL_CMD --> SERVICE
```

### 3.2 命令实现模式

所有命令继承 Django `BaseCommand`，遵循标准模式：

```python
from django.core.management.base import BaseCommand

class Command(BaseCommand):
    help = "命令描述"

    def add_arguments(self, parser):
        parser.add_argument("--arg_name", type=str, help="参数说明")

    def handle(self, *args, **options):
        # 1. 参数校验
        # 2. 执行业务逻辑
        # 3. 输出结果
        self.stdout.write("result message")
```

---

## 四、运维工具链

### 4.1 工具链全景

```mermaid
graph LR
    subgraph "锁机制"
        SHARE_LOCK["share_lock 装饰器<br/>utils/lock.py"]
        DIST_LOCK["DistributedLock<br/>tools/redis_lock.py"]
    end

    subgraph "批量处理"
        BULK["bulk_handle<br/>task/utils.py"]
        CHUNK["chunk_list<br/>task/utils.py"]
    end

    subgraph "迁移工具"
        MIG_UTIL["migration_util.py<br/>数据迁移辅助"]
    end

    subgraph "健康检查"
        HC["health_check.py<br/>数据链路健康检查"]
    end

    TASK_LAYER["Celery 任务层"] --> SHARE_LOCK
    TASK_LAYER --> BULK
    TASK_LAYER --> CHUNK
    BKBASE_TASK["bkbase 监听任务"] --> DIST_LOCK
    MIGRATION["migrations/"] --> MIG_UTIL
    CLI_CMD["管理命令"] --> HC
```

### 4.2 share_lock 装饰器

`share_lock` 是 metadata 模块最核心的并发控制机制：

- 基于 Redis 实现分布式锁
- 通过 `identify` 参数作为锁的唯一标识
- 通过 `ttl` 参数设置锁过期时间，防止死锁
- 获取锁失败时任务静默跳过（不报错）

### 4.3 DistributedLock 类

`tools/redis_lock.py` 提供了更灵活的锁控制：

- `acquire()` — 非阻塞获取
- `release()` — 安全释放
- `renew()` — 手动续约（用于长时间运行的任务）

主要用于 `watch_bkbase_meta_redis_task` 这种常驻进程场景。

---

## 五、种子数据架构

### 5.1 数据加载流程

```mermaid
sequenceDiagram
    participant Django as "Django Migration"
    participant M0002 as "0002_initial_data.py"
    participant MUtil as "migration_util.py"
    participant FS as "data/ 文件系统"
    participant DB as "MySQL"

    Django->>M0002: 执行 migration
    M0002->>FS: 读取 init_label.json
    M0002->>MUtil: add_datasource()
    M0002->>FS: 读取 init_datasource.json
    M0002->>MUtil: add_resulttable()
    M0002->>FS: 读取 init_resulttable.json
    M0002->>MUtil: add_esstorage()
    M0002->>FS: 读取 init_data.json
    MUtil->>DB: bulk_create(batch_size=500)
    DB-->>MUtil: 写入成功
```

### 5.2 数据文件分类

| 类别 | 文件 | 加载方式 |
|------|------|---------|
| **Migration 自动加载** | `init_label.json`, `init_cluster_info.json`, `init_datasource.json`, `init_resulttable.json`, `init_data.json`, `init_storage.json`, `init_ts_or_event_group.json` | `migrations/0002_initial_data.py` |
| **运行时加载** | `k8s_metrics/*.yaml` | 管理命令或 API 调用时动态加载 |
| **参考数据** | `description_unit.json`, `bkci_data.json`, `metadata_resulttablefield.txt` | 按需读取 |

---

## 六、数据库迁移架构

### 6.1 迁移依赖图

```mermaid
graph TB
    M1["0001_initial.py<br/>(115 KB)<br/>初始模型创建"] --> M2["0002_initial_data.py<br/>(7 KB)<br/>种子数据加载"]
    M2 --> M3["0003_init_storage_backend_info.py<br/>(3 KB)<br/>存储后端初始化"]
    M3 --> M4["0004_*.py<br/>(4 KB)<br/>DataBusConfig 变更"]
    M4 --> M5["0005_*.py<br/>(16 KB)<br/>RelationDefinition 新增"]
```

### 6.2 迁移策略

- **前向迁移**: 标准 Django `migrate` 命令，按编号顺序执行
- **回滚**: 支持 Django `migrate <app> <NNNN>` 回滚到指定版本
- **初始数据**: 通过 migration 自动加载，与模型创建绑定在同一迁移链中
- **幂等性**: 迁移文件中的 `init_*` 函数通过检查数据是否存在来保证幂等
