# 05-接口

> **专家**: 任务调度与运维专家
> **文档类型**: 实现层 — 接口说明
> **创建时间**: 2026-07-28

---

## 一、Celery 任务签名汇总

### 1.1 自定义上报

```python
# task/tasks.py:55
@app.task(ignore_result=True, queue=settings.metadata.celery_queue or "celery_metadata_task_worker")
def refresh_custom_report_config(bk_biz_id: int | None = None) -> None

# task/tasks.py:61
@app.task(ignore_result=True, queue=settings.metadata.celery_queue or "celery_metadata_task_worker")
def refresh_custom_log_report_config(log_group_id: int | None = None) -> None

# task/custom_report.py:84
@share_lock(identify="metadata_refreshEventGroup")
def check_event_update() -> None
```

### 1.2 配置刷新

```python
# task/config_refresh.py:42
@share_lock(ttl=PERIODIC_TASK_DEFAULT_TTL, identify="metadata_refreshConsulInfluxdbTableInfo")
def refresh_consul_influxdb_tableinfo() -> None

# task/config_refresh.py:52
@share_lock(ttl=PERIODIC_TASK_DEFAULT_TTL, identify="metadata_refreshConsulStorage")
def refresh_consul_storage() -> None

# task/config_refresh.py:67
@share_lock(identify="metadata_refreshConsulESInfo")
def refresh_consul_es_info() -> None

# task/config_refresh.py:80
@share_lock(ttl=1800, identify="metadata_refreshInfluxdbRoute")
def refresh_influxdb_route() -> None
```

### 1.3 ES 索引管理

```python
# task/tasks.py:114
@app.task(ignore_result=True, queue=settings.metadata.celery_queue or "celery_metadata_task_worker")
@retry(stop=stop_after_attempt(4), wait=wait_exponential(multiplier=1, min=1, max=10),
       retry=retry_if_exception_type(models.ESStorage.DoesNotExist), reraise=True)
def create_es_storage_index(table_id: str) -> None

# task/tasks.py:215
@app.task(ignore_result=True, queue=settings.metadata.celery_queue or "celery_long_task_cron")
def manage_es_storage(storage_record_ids: list[int], cluster_id: int = None) -> None

# task/tasks.py:262
@app.task(ignore_result=True, queue=settings.metadata.celery_queue or "celery_long_task_cron")
def clean_disable_es_storage(es_storages: list[ESStorage], cluster_id: int | None = None) -> None

# task/tasks.py:140
@app.task(ignore_result=True, queue=settings.metadata.celery_queue or "celery_metadata_task_worker")
def delete_es_result_table_snapshot(
    table_id: str, target_snapshot_repository_name: str, bk_tenant_id: str = DEFAULT_TENANT_ID
) -> None

# task/tasks.py:148
@app.task(ignore_result=True, queue=settings.metadata.celery_queue or "celery_metadata_task_worker")
def retry_es_result_table_snapshot(
    table_id: str, target_snapshot_repository_name: str, bk_tenant_id: str = DEFAULT_TENANT_ID
) -> None

# task/tasks.py:155
@app.task(ignore_result=True, queue=settings.metadata.celery_queue or "celery_metadata_task_worker")
def restore_result_table_snapshot(indices_group_by_snapshot: dict, restore_id: str) -> None

# task/tasks.py:164
@app.task(ignore_result=True, queue=settings.metadata.celery_queue or "celery_metadata_task_worker")
def delete_restore_indices(restore_id: str) -> None
```

### 1.4 空间与路由

```python
# task/tasks.py:400
@app.task(ignore_result=True, queue=settings.metadata.celery_queue or "celery_metadata_task_worker")
def push_and_publish_space_router(
    space_type: str | None = None,
    space_id: str | None = None,
    table_id_list: list | None = None,
) -> None

# task/sync_space.py:101
@share_lock(identify="metadata__sync_bkcc_space")
def sync_bkcc_space(
    bk_tenant_id: str | None = None,
    allow_deleted: bool = False,
    create_builtin_data_link_delay: bool = True,
) -> None
```

### 1.5 计算平台

```python
# task/tasks.py:73
@app.task(ignore_result=True, queue=settings.metadata.celery_queue or "celery_metadata_task_worker")
def access_to_bk_data_task(bk_tenant_id: str, table_id: str) -> None

# task/tasks.py:83
@app.task(ignore_result=True, queue=settings.metadata.celery_queue or "celery_metadata_task_worker")
def create_statistics_data_flow(table_id: str, agg_interval: int) -> None

# task/tasks.py:91
@app.task(ignore_result=True, queue=settings.metadata.celery_queue or "celery_metadata_task_worker")
def create_full_cmdb_level_data_flow(table_id: str, bk_tenant_id: str = DEFAULT_TENANT_ID) -> None

# task/bkbase.py:42
def watch_bkbase_meta_redis_task() -> None
```

### 1.6 BCS

```python
# task/bcs.py:32
@share_lock(ttl=PERIODIC_TASK_DEFAULT_TTL, identify="metadata_refreshBCSMonitorInfo")
def refresh_bcs_monitor_info() -> None
```

### 1.7 数据链路

```python
# task/datalink.py:28
@app.task(ignore_result=True, queue=settings.metadata.celery_queue or "celery_metadata_task_worker")
def apply_log_datalink(bk_tenant_id: str, table_id: str) -> None

# task/refresh_data_link.py:14
@share_lock(identify="metadata_refreshDataLink", ttl=1800)
def refresh_data_link_status() -> None

# task/tasks.py:852
@app.task(ignore_result=True, queue=settings.metadata.celery_queue or "celery_metadata_task_worker")
def bulk_refresh_data_link_status(bkbase_rt_records: list) -> None
```

### 1.8 运维

```python
# task/entity_relation.py:14
@share_lock(ttl=3600, identify="metadata_refresh_entity_definition_to_redis")
def refresh_entity_definition_to_redis() -> None

# task/sync_cmdb_relation.py:24
@share_lock(ttl=3600, identify="metadata_sync_relation_redis_data")
def sync_relation_redis_data() -> None

# task/vm.py:16
@share_lock(ttl=3600, identify="metadata_check_access_vm_task")
def check_access_vm_task(only_v4: bool = False) -> None

# task/ping_server.py:20
def refresh_ping_server_2_node_man() -> None

# task/refresh_default_rp.py:10
def main() -> None
```

---

## 二、管理命令参数汇总

### 2.1 空间管理

```bash
python manage.py sync_cmdb_space
    --bk_tenant_id=system         # 租户ID（默认 system）

python manage.py init_space_data
    --init_type=True              # 初始化空间类型（默认 True）
    --sync_bkcc=True              # 同步 BKCC 业务（默认 True）
    --sync_bcs=True               # 同步 BCS 项目（默认 True）
    --init_redis=True             # 推送空间至 Redis（默认 True）

python manage.py init_redis_data
    --force                       # 强制推送（即使 Redis 已有数据）
```

### 2.2 数据源/结果表

```bash
python manage.py access_bkdata_vm
    --space_type=bkcc             # 空间类型（必填）
    --space_id=2                  # 空间ID（必填）
    --table_ids="t1;t2"           # 结果表ID列表，分号分隔
    --partition=1                 # partition 数量（默认 1）
    --vm_cluster_name=""          # VM 集群名称
    --vm_retention_time=90d       # 数据保留时间（默认 90d）

python manage.py switch_kafka_for_data_id
    --data_ids=1001 1002          # data_id 列表
    --bk_data_ids="1001,1002"     # 逗号分隔的 data_id
    --kind=frontend               # frontend 或 backend（必填）
    --kafka_cluster_id=1          # Kafka 集群ID（必填）

python manage.py switch_transfer_for_dataid
    --data_ids=1001               # data_id 列表

python manage.py query_data_id_by_mq
    --mq_cluster_id=1             # MQ 集群ID

python manage.py query_data_id_by_transfer
    --transfer_cluster_id=1       # Transfer 集群ID

python manage.py query_disabled_data_id
    # 无参数

python manage.py query_no_data_data_id
    # 无参数

python manage.py modify_data_source_space_type
    --data_ids=1001               # data_id 列表
    --space_type_id=bkcc          # 目标空间类型

python manage.py modify_data_id_belong_space
    --data_id=1001                # data_id
    --space_id=2                  # 目标空间ID

python manage.py redirect_datasource
    --src_data_id=1001            # 源 data_id
    --dst_data_id=1002            # 目标 data_id
```

### 2.3 集群/存储

```bash
python manage.py sync_cluster_config
    # 无参数（从环境变量读取配置）

python manage.py check_bcs_cluster_status
    # 无参数（遍历所有 BCS 集群）

python manage.py init_influxdb_proxy_storage
    # 无参数

python manage.py switch_vm_cluster
    --table_ids="t1;t2"           # 结果表ID列表
    --vm_cluster_id=1             # 目标 VM 集群ID

python manage.py query_storage
    --table_id=system.cpu_summary # 结果表ID

python manage.py query_storage_cluster
    --cluster_id=1                # 集群ID

python manage.py query_es_index
    --table_id=system.cpu_summary # 结果表ID
```

### 2.4 数据链路

```bash
python manage.py check_datalink_health
    --bk_tenant_id=system         # 租户ID（必填）
    --scene=host                  # 场景（必填）：uptimecheck/custom_metric/host/k8s/apm/custom_event/log
    --bk_biz_id=2                 # 业务ID（必填）
    --bk_data_id=1001             # 数据ID（custom_metric 场景需要）
    --bcs_cluster_id=BCS-K8S-xxx  # BCS集群ID（k8s 场景需要）
    --app_name=myapp              # 应用名称（apm 场景需要）

python manage.py create_shortcut_data_link
    --bk_tenant_id=system         # 租户ID
    --table_id=xxx                # 结果表ID

python manage.py sync_bklog_es_router
    --force                       # 强制刷新
```

### 2.5 指标/维度

```bash
python manage.py add_bkci_metrics_and_dimensions
    # 无参数

python manage.py add_extend_dimensions
    # 无参数

python manage.py check_ts_metrics
    # 无参数

python manage.py refresh_ts_metric
    --table_id=xxx                # 结果表ID

python manage.py refresh_custom_report_metric_path
    # 无参数
```

### 2.6 运维/清理

```bash
python manage.py clean_old_consul_config
    # 无参数

python manage.py delete_data_source_consul_config
    --data_id=1001                # data_id

python manage.py delete_gse_router
    --data_id=1001                # data_id

python manage.py remove_repeated_target
    # 无参数
```

### 2.7 初始化

```bash
python manage.py init_redis_data
    --force                       # 强制推送

python manage.py init_tenant
    # 无参数

python manage.py enable_global_biz
    # 无参数
```

### 2.8 迁移

```bash
python manage.py migrate_nano_log
    --table_ids="t1;t2"           # 结果表ID列表

python manage.py switch_data_id_from_influxdb_to_bkbase_v4
    --data_ids=1001 1002          # data_id 列表
```

### 2.9 其他

```bash
python manage.py deploy_official_plugin
    --plugin_name=bk-collector    # 插件名称

python manage.py modify_kafka_cluster
    --cluster_id=1                # 集群ID
    --domain_name=xxx             # 域名
    --port=9092                   # 端口

python manage.py query_es_index
    --table_id=xxx                # 结果表ID

python manage.py refresh_influxdb_router
    # 无参数

python manage.py refresh_influxdb_proxy_config
    # 无参数

python manage.py update_influxdb_proxy_config
    --table_id=xxx                # 结果表ID

python manage.py disable_influxdb_router_for_vm
    --table_id=xxx                # 结果表ID

python manage.py sync_zk_config
    # 无参数

python manage.py update_bcs_cluster_cloud_id_config
    # 无参数

python manage.py update_bcs_replace_config
    # 无参数

python manage.py deploy_proxy_and_collector_plugin
    # 无参数
```

---

## 三、工具函数签名

### 3.1 DistributedLock

文件：`tools/redis_lock.py:10-66`

```python
class DistributedLock:
    def __init__(
        self,
        redis_client,                                              # Redis 客户端实例
        lock_name: str,                                            # 锁名称
        timeout: int = settings.metadata.bkbase_redis_watch_lock_expire_seconds,  # 过期时间（秒）
    ) -> None

    def acquire(self) -> bool:                                     # 非阻塞获取锁
    def release(self) -> None:                                     # 释放锁
    def renew(self) -> None:                                       # 手动续约锁
```

### 3.2 批量操作工具

文件：`task/utils.py:10-38`

```python
def bulk_handle(
    handler: Callable,                                             # 处理函数
    data: list[Any],                                               # 数据列表
    bulk_size: int = DEFAULT_BULK_SIZE,                            # 批量大小（默认 50）
    is_wait_finish: bool | None = True,                            # 是否等待所有线程完成
) -> None

def chunk_list(
    data: list,                                                    # 原始列表
    size: int,                                                     # 每块大小
) -> list[list]:                                                   # 切分后的列表
```

### 3.3 share_lock 装饰器

文件：`utils/lock.py`（由 API与工具库专家覆盖）

```python
@share_lock(
    ttl: int = PERIODIC_TASK_DEFAULT_TTL,                          # 锁过期时间（秒）
    identify: str,                                                 # 锁唯一标识
)
def decorated_function(*args, **kwargs) -> Any
```

### 3.4 自定义上报内部函数

文件：`task/custom_report.py`

```python
def update_event_by_cluster(
    cluster_id_with_table_ids: dict[tuple[str, int], list[str]]    # ES集群ID与table_id映射
) -> None

def refresh_custom_report_2_node_man(
    bk_biz_id: int | None = None                                   # 业务ID，None 表示全局
) -> None

def refresh_custom_log_config(
    log_group_id: int | None = None                                # 日志组ID
) -> None
```

### 3.5 配置刷新内部函数

文件：`task/config_refresh.py`

```python
def refresh_consul_influxdb_tableinfo() -> None                    # 刷新 InfluxDB 表信息
def refresh_consul_storage() -> None                               # 刷新 ES 存储配置
def refresh_consul_es_info() -> None                               # 刷新 ES 表配置
def refresh_influxdb_route() -> None                               # 刷新 InfluxDB 路由
```

### 3.6 空间同步内部函数

文件：`task/sync_space.py`

```python
def sync_bkcc_space(
    bk_tenant_id: str | None = None,
    allow_deleted: bool = False,
    create_builtin_data_link_delay: bool = True,
) -> None

def push_and_publish_space_router(
    space_type: str | None = None,
    space_id: str | None = None,
    table_id_list: list | None = None,
) -> None
```

### 3.7 迁移工具函数

文件：`migration_util.py`

```python
def add_datasource(models, data_id, data_name, ...) -> None       # 创建数据源
def add_datasource_option(models, ...) -> None                     # 创建数据源选项
def add_resulttable(models, table_id, ...) -> None                 # 创建结果表
def add_resulttablefield(models, ...) -> None                      # 创建结果表字段
def add_resulttable_option(models, ...) -> None                    # 创建结果表选项
def add_datasourceresulttable(models, ...) -> None                 # 创建数据源-结果表关联
def add_esstorage(models, ...) -> None                             # 创建 ES 存储
def add_influxdbstorage(models, ...) -> None                       # 创建 InfluxDB 存储
def add_kafkastorage(models, ...) -> None                          # 创建 Kafka 存储
```
