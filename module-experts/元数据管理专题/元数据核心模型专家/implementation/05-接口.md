# 05-接口

> **专家**: 元数据核心模型专家
> **类型**: 实现层文档
> **创建时间**: 2026-07-28

---

## 一、DataSource 接口

### 1.1 类方法

| 方法 | 签名 | 说明 |
|------|------|------|
| `create_data_source` | `(data_name, etl_config, operator, source_label, type_label, **kwargs) -> DataSource` | 创建数据源 |
| `apply_for_data_id_from_bkdata` | `(bk_tenant_id, data_name, bk_biz_id, **kwargs) -> int` | 从计算平台申请 DataID |
| `apply_for_data_id_from_gse` | `(bk_tenant_id, operator) -> int` | 从 GSE 申请 DataID |
| `refresh_consul_data_list` | `() -> None` | 刷新 Consul data_list（已废弃） |
| `_add_time_unit_options` | `(operator, bk_data_id, etl_config) -> None` | 添加时间相关 option |

### 1.2 实例方法

| 方法 | 签名 | 说明 |
|------|------|------|
| `make_token` | `() -> str` | 生成随机 token |
| `get_transfer_storage_conf` | `(table_id) -> list` | 获取 Transfer 写入存储配置 |
| `get_spaces_by_data_id` | `(bk_tenant_id, bk_data_id) -> dict` | 通过数据源ID查询空间 |
| `clean_cache` | `() -> None` | 强制从 DB 刷新缓存 |
| `to_json` | `(is_consul_config=False, with_rt_info=True) -> dict` | 序列化为 JSON |
| `register_to_bkbase` | `(bk_biz_id, namespace, bkbase_data_name) -> None` | 注册到计算平台 |
| `update_config` | `(operator, **kwargs) -> bool` | 更新数据源配置 |
| `can_refresh_consul_and_gse` | `() -> bool` | 判断是否可刷新 Consul/GSE |
| `refresh_gse_config` | `() -> None` | 刷新 GSE 配置 |
| `refresh_gse_config_to_gse` | `() -> None` | 同步路由到 GSE |
| `register_to_gse` | `() -> bool` | 注册数据源到 GSE |
| `add_built_in_channel_id_to_gse` | `() -> None` | 注册内置 channel_id |
| `delete_consul_config` | `(consul_config_path="") -> None` | 删除 Consul 配置 |
| `redirect_consul_config` | `(new_transfer_cluster_id) -> None` | 重定向 Consul 配置 |
| `refresh_consul_config` | `() -> None` | 刷新 Consul 配置 |
| `create_mq` | `() -> None` | 创建消息队列 Topic |
| `get_consul_fields` | `() -> list` | 获取 Consul 字段配置 |
| `update_field_config` | `() -> bool` | 更新字段配置 |
| `refresh_outer_config` | `() -> bool` | 刷新所有外部配置 |
| `_save_space_datasource` | `(creator, space_type_id, space_id, ...) -> None` | 保存空间数据源关系 |

### 1.3 属性

| 属性 | 返回类型 | 说明 |
|------|---------|------|
| `mq_cluster` | ClusterInfo | 消息队列集群（懒加载） |
| `datalink_version` | str | 数据链路版本 |
| `consul_config_path` | str | Consul 配置路径 |
| `consul_fields_path` | str | Consul 字段路径 |
| `mq_config` | KafkaTopicInfo | 消息队列配置（懒加载） |
| `is_custom_timeseries_report` | bool | 是否自定义时序上报 |
| `gse_route_config` | dict | GSE 路由配置 |
| `is_field_discoverable` | bool | 是否支持字段自发现 |

## 二、ResultTable 接口

### 2.1 类方法

| 方法 | 签名 | 说明 |
|------|------|------|
| `create_result_table` | `(bk_data_id, table_id, table_name_zh, ...) -> ResultTable` | 创建结果表 |
| `refresh_consul_influxdb_tableinfo` | `() -> None` | 刷新 InfluxDB Consul 配置 |
| `is_disable_metric_cutter` | `(table_id, bk_tenant_id) -> bool` | 是否禁用指标切分 |
| `get_table_id_cutter` | `(table_ids, bk_tenant_id) -> dict` | 批量获取切分配置 |
| `get_result_table_storage_info` | `(table_id, storage_type, bk_tenant_id) -> dict` | 获取存储配置 |
| `get_result_table_storage` | `(table_id, storage_type, bk_tenant_id) -> Storage` | 获取存储对象 |
| `get_real_storage_list` | `() -> list` | 获取支持的存储列表 |
| `get_result_table` | `(table_id, bk_tenant_id) -> ResultTable` | 查询结果表（兼容多种命名） |
| `batch_to_json` | `(bk_tenant_id, result_table_id_list, with_option) -> list` | 批量序列化 |

### 2.2 实例方法

| 方法 | 签名 | 说明 |
|------|------|------|
| `get_related_datasource` | `(refresh=False) -> DataSource` | 获取关联数据源（带缓存） |
| `get_target_bk_biz_id` | `() -> int` | 获取目标业务ID |
| `delete_datalink` | `() -> None` | 删除数据链路 |
| `apply_datalink` | `(force_update=False) -> None` | 应用数据链路 |
| `check_and_create_storage` | `(bk_tenant_id, is_sync_db, ...) -> None` | 检测并创建存储 |
| `create_storage` | `(storage, is_sync_db, ...) -> bool` | 创建存储 |
| `bulk_create_fields` | `(field_data, is_etl_refresh, is_force_add, bk_tenant_id) -> bool` | 批量创建字段 |
| `create_field` | `(field_name, field_type, operator, ...) -> bool` | 创建单个字段 |
| `refresh_etl_config` | `() -> bool` | 刷新 ETL 配置 |
| `get_storage` | `(storage_type) -> Storage` | 获取指定存储对象 |
| `get_storage_info` | `(storage_type) -> dict` | 获取指定存储配置 |
| `raw_delete` | `(qs, using) -> None` | 原始删除（绕过 Django） |
| `modify` | `(operator, **kwargs) -> None` | 修改结果表 |
| `notify_bkdata_log_data_id_changed` | `(data_id) -> bool` | 通知计算平台 ES 变更 |
| `upgrade_result_table` | `(operator) -> bool` | 升级为全业务结果表 |
| `set_metric_split` | `(cmdb_level, operator) -> CMDBLevelRecord` | 设置 CMDB 层级拆分 |
| `clean_metric_split` | `(cmdb_level, operator) -> bool` | 清理 CMDB 层级拆分 |
| `to_json` | `() -> dict` | 完整序列化 |
| `to_json_self_only` | `() -> dict` | 仅自身字段序列化 |
| `get_tag_values` | `(tag_name) -> list` | 获取标签值 |

### 2.3 属性

| 属性 | 返回类型 | 说明 |
|------|---------|------|
| `real_storage_list` | list[Storage] | 所有实际存储对象 |
| `storage_list` | list[str] | 存储方案字符串列表 |
| `data_source` | DataSource | 关联的数据源（懒加载） |

## 三、ResultTableField 接口

### 3.1 类方法

| 方法 | 签名 | 说明 |
|------|------|------|
| `create_field` | `(table_id, field_name, field_type, ...) -> ResultTableField` | 创建字段 |
| `bulk_create_fields` | `(table_id, field_data, bk_tenant_id) -> list` | 批量创建字段 |
| `bulk_create_default_fields` | `(table_id, include_cmdb_level, ...) -> None` | 创建默认字段 |
| `batch_get_fields` | `(table_id_list, is_consul_config, bk_tenant_id) -> dict` | 批量获取字段 |

### 3.2 实例方法

| 方法 | 签名 | 说明 |
|------|------|------|
| `to_json` | `() -> dict` | 序列化为 JSON |
| `to_json_self_only` | `() -> dict` | 仅自身字段序列化 |

### 3.3 属性

| 属性 | 返回类型 | 说明 |
|------|---------|------|
| `is_dimension` | bool | 是否维度字段 |
| `is_metric` | bool | 是否指标字段 |

## 四、DataSourceOption 接口

| 方法 | 签名 | 说明 |
|------|------|------|
| `create_option` | `(bk_data_id, name, value, creator, bk_tenant_id) -> DataSourceOption` | 创建选项 |
| `create_or_update` | `(bk_data_id, name, value, creator, bk_tenant_id) -> DataSourceOption` | 创建或更新选项 |

## 五、ResultTableOption 接口

| 方法 | 签名 | 说明 |
|------|------|------|
| `create_option` | `(table_id, name, value, creator, bk_tenant_id) -> ResultTableOption` | 创建选项 |
| `bulk_create_options` | `(table_id, option_data, creator, bk_tenant_id) -> None` | 批量创建选项 |
| `batch_result_table_option` | `(table_id_list, bk_tenant_id) -> dict` | 批量获取选项 |
| `sync_cmdb_level_option` | `(table_id, operator, bk_tenant_id) -> bool` | 同步 CMDB 层级选项 |

## 六、ResultTableFieldOption 接口

| 方法 | 签名 | 说明 |
|------|------|------|
| `create_option` | `(table_id, field_name, name, value, creator, bk_tenant_id) -> ResultTableFieldOption` | 创建字段选项 |
| `bulk_create_options` | `(table_id, option_data, bk_tenant_id) -> None` | 批量创建字段选项 |
| `batch_field_option` | `(table_id_list, bk_tenant_id) -> dict` | 批量获取字段选项 |
| `get_field_option` | `(table_id, field_name, bk_tenant_id) -> dict` | 获取字段选项 |
| `get_field_option_es_format` | `(table_id, field_name, bk_tenant_id) -> dict` | 获取 ES 格式选项 |

## 七、DataSourceResultTable 接口

| 方法 | 签名 | 说明 |
|------|------|------|
| `modify_table_id_datasource` | `(bk_tenant_id, table_id, bk_data_id) -> None` | 修改结果表关联的数据源 |
| `refresh_consul_config_by_table_id` | `(table_id, bk_tenant_id) -> None` | 通过 table_id 刷新 Consul |

## 八、Label 接口

| 方法 | 签名 | 说明 |
|------|------|------|
| `exists_label` | `(label_id, label_type=None) -> bool` | 判断标签是否存在 |
| `get_label_info` | `(include_admin_only=False, label_type=None, level=None) -> dict` | 获取标签信息（按类型分组） |
| `to_json` | `() -> dict` | 序列化为 JSON |

## 九、EntityRelation 接口

### 9.1 EntityMeta（抽象基类）

| 方法 | 签名 | 说明 |
|------|------|------|
| `get_kind` | `() -> str` | 返回实体类型（默认类名） |
| `get_spec_fields` | `() -> list[str]` | 获取规格字段列表 |
| `get_spec` | `() -> dict` | 获取规格字典 |
| `to_json` | `() -> dict` | 生成 K8s 风格 JSON |
| `get_serializer_class` | `() -> type[ModelSerializer]` | 获取 DRF 序列化器类（带缓存） |
| `save` | `(*args, **kwargs) -> None` | 空 namespace 归一化为 `__all__` |

### 9.2 ResourceDefinition

| 方法 | 签名 | 说明 |
|------|------|------|
| `to_redis_json` | `() -> dict` | 生成 Redis 存储格式 JSON |

### 9.3 RelationDefinition

| 方法 | 签名 | 说明 |
|------|------|------|
| `to_redis_json` | `() -> dict` | 生成 Redis 存储格式 JSON |

## 十、OptionBase（抽象基类）

| 方法 | 签名 | 说明 |
|------|------|------|
| `get_option` | `(query_id) -> dict` | 获取选项配置 |
| `_parse_value` | `(value) -> tuple[str, str]` | 解析值类型 |
| `_create_option` | `(value, creator) -> OptionBase` | 创建选项实例 |
| `get_value` | `() -> Any` | 获取反序列化值 |
| `to_json` | `() -> dict` | 返回 `{name: value}` |

---

> **下一步**: 查阅子专家文档了解 DataSource 和 ResultTable 的深入源码细节。
