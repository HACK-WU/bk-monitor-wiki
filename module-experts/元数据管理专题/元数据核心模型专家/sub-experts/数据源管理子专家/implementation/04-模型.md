# 04-模型

> **子专家**: 数据源管理子专家
> **类型**: 实现层文档
> **创建时间**: 2026-07-28

---

## 一、DataSource 数据模型

**文件**: `bk-monitor-base/src/bk_monitor_base/metadata/models/data_source.py` 行 60-130

**数据库表**: `metadata_datasource`

### 1.1 完整字段定义

| 字段 | Django 类型 | 约束 | 默认值 | 说明 |
|------|-----------|------|--------|------|
| `bk_data_id` | AutoField | PK | — | 数据源ID（自增主键） |
| `token` | CharField(256) | — | `""` | 上报校验 token（随机生成） |
| `bk_tenant_id` | CharField(256) | db_index | `"system"` | 租户ID |
| `data_name` | CharField(128) | db_index | — | 数据源名称 |
| `data_description` | TextField | — | — | 数据源描述 |
| `mq_cluster_id` | IntegerField | — | — | 消息队列集群ID（FK→ClusterInfo） |
| `mq_config_id` | IntegerField | — | — | 消息队列配置ID（FK→KafkaTopicInfo） |
| `etl_config` | TextField | — | — | ETL 清洗配置（函数名或 JSON） |
| `is_custom_source` | BooleanField | — | — | 是否自定义数据源 |
| `creator` | CharField(32) | — | — | 创建者 |
| `create_time` | DateTimeField | auto_now_add | — | 创建时间 |
| `last_modify_user` | CharField(32) | — | — | 最后更新者 |
| `last_modify_time` | DateTimeField | auto_now | — | 最后更新时间 |
| `type_label` | CharField(128) | — | — | 数据类型标签（如 `time_series`） |
| `source_label` | CharField(128) | — | — | 数据源标签（如 `custom`） |
| `custom_label` | CharField(256) | null=True | — | 自定义标签 |
| `source_system` | CharField(256) | — | — | 请求来源系统 |
| `is_enable` | BooleanField | — | True | 是否启用 |
| `transfer_cluster_id` | CharField(50) | — | — | Transfer 集群ID |
| `is_tenant_specific_global` | BooleanField | — | False | （已废弃）租户全局标记 |
| `is_platform_data_id` | BooleanField | — | False | 是否平台级数据源 |
| `space_type_id` | CharField(64) | db_index | — | 空间类型（如 `bkcc`） |
| `space_uid` | CharField(256) | — | `""` | 空间 UID（格式：`{type}__{id}`） |
| `created_from` | CharField(16) | db_index | `"bkgse"` | 数据源来源（`bkgse`/`bkdata`） |

### 1.2 索引与约束

| 约束类型 | 字段 |
|---------|------|
| **主键** | `bk_data_id` |
| **唯一索引** | `(data_name, bk_tenant_id)` |
| **普通索引** | `bk_tenant_id`, `data_name`, `space_type_id`, `created_from` |

### 1.3 Meta 配置

```python
# data_source.py 行 125-130
class Meta:
    db_table = "metadata_datasource"
    verbose_name = "数据源管理"
    verbose_name_plural = "数据源管理表"
    unique_together = ("data_name", "bk_tenant_id")
```

### 1.4 类常量

| 常量 | 值 | 说明 |
|------|-----|------|
| `DEFAULT_MQ_TYPE` | `"kafka"` | 默认消息队列类型 |
| `MQ_CONFIG_DICT` | `{cluster_type: KafkaTopicInfo}` | MQ 配置类映射 |

## 二、DataSourceOption 数据模型

**文件**: `data_source.py` 行 1275-1360

**数据库表**: `metadata_datasourceoption`

### 2.1 完整字段定义

| 字段 | Django 类型 | 约束 | 说明 |
|------|-----------|------|------|
| `bk_data_id` | IntegerField | db_index | 数据源ID |
| `bk_tenant_id` | CharField(256) | — | 租户ID |
| `name` | CharField(128) | — | 选项名称 |
| `value_type` | CharField(64) | — | bool/string/list/int/dict |
| `value` | TextField | — | JSON 序列化的值 |
| `creator` | CharField(32) | — | 创建者 |
| `create_time` | DateTimeField | auto_now_add | 创建时间 |

### 2.2 选项常量定义

```python
# data_source.py 行 1280-1295
OPTION_USE_SOURCE_TIME = "use_source_time"
OPTION_DISABLE_METRIC_CUTTER = "disable_metric_cutter"
OPTION_ALLOW_METRICS_MISSING = "allow_metrics_missing"
OPTION_ALLOW_DIMENSIONS_MISSING = "allow_dimensions_missing"
OPTION_TIME_PRECISION = "time_precision"
OPTION_INJECT_LOCAL_TIME = "inject_local_time"
OPTION_TIMESTAMP_UNIT = "timestamp_precision"
OPTION_IS_SPLIT_MEASUREMENT = "is_split_measurement"
OPTION_ALIGN_TIME_UNIT = "align_time_unit"
OPTION_DROP_METRICS_ETL_CONFIGS = "drop_metrics_etl_configs"
```

### 2.3 继承关系

DataSourceOption 继承自 `OptionBase`（`common.py` 行 120-228），获得：
- `_parse_value(value)`: 自动识别值类型
- `get_value()`: 自动反序列化
- `to_json()`: 返回 `{name: value}`

## 三、DataSourceResultTable 数据模型

**文件**: `data_source.py` 行 1365-1430

**数据库表**: `metadata_datasourceresulttable`

### 3.1 完整字段定义

| 字段 | Django 类型 | 约束 | 说明 |
|------|-----------|------|------|
| `bk_data_id` | IntegerField | — | 数据源ID |
| `table_id` | CharField(128) | — | 结果表ID |
| `bk_tenant_id` | CharField(256) | — | 租户ID |
| `creator` | CharField(32) | — | 创建者 |
| `create_time` | DateTimeField | auto_now_add | 创建时间 |

### 3.2 约束

```python
# data_source.py 行 1425-1430
class Meta:
    db_table = "metadata_datasourceresulttable"
    verbose_name = "数据源结果表"
    verbose_name_plural = "数据源结果表"
    unique_together = ("bk_data_id", "table_id", "bk_tenant_id")
```

## 四、Label 数据模型

**文件**: `common.py` 行 10-115

**数据库表**: `metadata_label`

### 4.1 完整字段定义

| 字段 | Django 类型 | 约束 | 说明 |
|------|-----------|------|------|
| `label_id` | CharField(128) | PK | 标签ID（主键） |
| `label_name` | CharField(128) | — | 标签名 |
| `label_type` | CharField(64) | choices | source_label/result_table_label/type_label |
| `is_admin_only` | BooleanField | — | 是否仅管理员可见 |
| `parent_label` | CharField(128) | null=True | 父级标签ID（自引用） |
| `level` | IntegerField | null=True | 标签层级 |
| `index` | IntegerField | null=True | 标签排序 |

### 4.2 类常量

```python
# common.py 行 15-20
LABEL_TYPE_SOURCE = "source_label"
LABEL_TYPE_RESULT_TABLE = "result_table_label"
LABEL_TYPE_TYPE = "type_label"
```

### 4.3 Meta 配置

```python
# common.py 行 110-115
class Meta:
    db_table = "metadata_label"
    verbose_name = "元数据标签"
    verbose_name_plural = "元数据标签"
```

## 五、OptionBase 抽象基类

**文件**: `common.py` 行 120-228

**数据库表**: 不创建表（`abstract = True`）

### 5.1 字段定义

| 字段 | Django 类型 | 说明 |
|------|-----------|------|
| `value_type` | CharField(64) | 值类型（bool/string/list/int/dict） |
| `value` | TextField | JSON 序列化的值 |
| `creator` | CharField(32) | 创建者 |
| `create_time` | DateTimeField(auto_now_add) | 创建时间 |
| `bk_tenant_id` | CharField(256) | 租户ID |

### 5.2 Meta 配置

```python
# common.py 行 225-228
class Meta:
    abstract = True
```

### 5.3 值类型常量

```python
# common.py 行 130-140
TYPE_OPTION_DICT = {
    "bool": bool,
    "list": list,
    "dict": dict,
    "int": int,
}
```

## 六、BaseModel 抽象基类

**文件**: `common.py` 行 230-240

| 字段 | Django 类型 | 说明 |
|------|-----------|------|
| `creator` | CharField(64) | 创建者 |
| `create_time` | DateTimeField(auto_now_add) | 创建时间 |
| `updater` | CharField(64) | 更新者 |
| `update_time` | DateTimeField(auto_now) | 更新时间 |

## 七、BaseModelWithTime 抽象基类

**文件**: `common.py` 行 243-260

| 字段 | Django 类型 | 说明 |
|------|-----------|------|
| `creator` | CharField(64) | 创建者 |
| `created_at` | DateTimeField(auto_now_add) | 创建时间 |
| `updater` | CharField(64) | 更新者 |
| `updated_at` | DateTimeField(auto_now) | 更新时间 |

> **与 BaseModel 的区别**: 时间字段命名为 `created_at`/`updated_at` 而非 `create_time`/`update_time`。

---

> **下一步**: 查阅 [05-接口.md](05-接口.md) 了解 DataSource 对外暴露的完整接口。
