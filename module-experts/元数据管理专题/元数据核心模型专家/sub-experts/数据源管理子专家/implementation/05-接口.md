# 05-接口

> **子专家**: 数据源管理子专家
> **类型**: 实现层文档
> **创建时间**: 2026-07-28

---

## 一、DataSource 接口

**文件**: `bk-monitor-base/src/bk_monitor_base/metadata/models/data_source.py`

### 1.1 类方法

| 方法 | 签名 | 源码行 | 说明 |
|------|------|--------|------|
| `create_data_source` | `(data_name, etl_config, operator, source_label, type_label, **kwargs) -> DataSource` | 行 499-700 | 创建数据源（7步流程） |
| `apply_for_data_id_from_bkdata` | `(bk_tenant_id, data_name, bk_biz_id, **kwargs) -> int` | 行 470-495 | 从计算平台申请 DataID |
| `apply_for_data_id_from_gse` | `(bk_tenant_id, operator) -> int` | 行 450-465 | 从 GSE 申请 DataID |
| `make_token` | `() -> str` | 行 145-150 | 生成随机 token（uuid4） |
| `refresh_consul_data_list` | `() -> None` | 行 1280-1290 | 刷新 Consul data_list（已废弃） |
| `_add_time_unit_options` | `(operator, bk_data_id, etl_config) -> None` | 行 720-750 | 添加时间相关 option |

### 1.2 实例方法

| 方法 | 签名 | 源码行 | 说明 |
|------|------|--------|------|
| `get_transfer_storage_conf` | `(table_id) -> list` | 行 330-350 | 获取 Transfer 写入存储配置 |
| `get_spaces_by_data_id` | `(bk_tenant_id, bk_data_id) -> dict` | 行 380-400 | 通过数据源ID查询空间信息 |
| `clean_cache` | `() -> None` | 行 200-210 | 强制从 DB 刷新缓存（重置懒加载） |
| `to_json` | `(is_consul_config=False, with_rt_info=True) -> dict` | 行 220-300 | 序列化为 JSON |
| `register_to_bkbase` | `(bk_biz_id, namespace, bkbase_data_name) -> None` | 行 350-380 | 注册到计算平台 |
| `update_config` | `(operator, **kwargs) -> bool` | 行 800-920 | 更新数据源配置 |
| `can_refresh_consul_and_gse` | `() -> bool` | 行 1230-1245 | 判断是否可刷新 Consul/GSE |
| `refresh_gse_config` | `() -> None` | 行 1150-1170 | 刷新 GSE 配置 |
| `refresh_gse_config_to_gse` | `() -> None` | 行 1175-1200 | 同步路由到 GSE ZK |
| `register_to_gse` | `() -> bool` | 行 1100-1120 | 注册数据源到 GSE |
| `add_built_in_channel_id_to_gse` | `() -> None` | 行 1125-1145 | 注册内置 channel_id |
| `delete_consul_config` | `(consul_config_path="") -> None` | 行 1050-1065 | 删除 Consul 配置 |
| `redirect_consul_config` | `(new_transfer_cluster_id) -> None` | 行 1070-1085 | 重定向 Consul 配置 |
| `refresh_consul_config` | `() -> None` | 行 1200-1250 | 刷新 Consul 配置 |
| `create_mq` | `() -> None` | 行 400-420 | 创建消息队列 Topic |
| `get_consul_fields` | `() -> list` | 行 420-440 | 获取 Consul 字段配置 |
| `update_field_config` | `() -> bool` | 行 440-450 | 更新字段配置 |
| `refresh_outer_config` | `() -> bool` | 行 1250-1270 | 刷新所有外部配置（统一入口） |
| `_save_space_datasource` | `(creator, space_type_id, space_id, ...) -> None` | 行 1435-1480 | 保存空间数据源关系 |

### 1.3 属性

| 属性 | 返回类型 | 源码行 | 说明 |
|------|---------|--------|------|
| `mq_cluster` | ClusterInfo | 行 160-175 | 消息队列集群（懒加载，首次访问查DB） |
| `datalink_version` | str | 行 130-145 | 数据链路版本（`v3`/`v4`） |
| `consul_config_path` | str | 行 200-210 | Consul 配置路径 |
| `consul_fields_path` | str | 行 215-220 | Consul 字段路径 |
| `mq_config` | KafkaTopicInfo | 行 180-195 | 消息队列配置（懒加载） |
| `is_custom_timeseries_report` | bool | 行 148-155 | 是否自定义时序上报 |
| `gse_route_config` | dict | 行 1090-1120 | GSE 路由配置 |
| `is_field_discoverable` | bool | 行 310-320 | 是否支持字段自发现 |

## 二、DataSourceOption 接口

**文件**: `data_source.py` 行 1275-1360

| 方法 | 签名 | 说明 |
|------|------|------|
| `create_option` | `(bk_data_id, name, value, creator, bk_tenant_id) -> DataSourceOption` | 创建选项（调用 `_parse_value` 自动识别类型） |
| `create_or_update` | `(bk_data_id, name, value, creator, bk_tenant_id) -> DataSourceOption` | 创建或更新选项（`update_or_create`） |
| `get_option` | `(bk_data_id) -> dict` | 获取数据源所有选项（返回 `{name: value}` 字典） |

## 三、DataSourceResultTable 接口

**文件**: `data_source.py` 行 1365-1430

| 方法 | 签名 | 说明 |
|------|------|------|
| `modify_table_id_datasource` | `(bk_tenant_id, table_id, bk_data_id) -> None` | 修改结果表关联的数据源（批量 update） |
| `refresh_consul_config_by_table_id` | `(table_id, bk_tenant_id) -> None` | 通过 table_id 刷新 Consul 配置 |

## 四、Label 接口

**文件**: `common.py` 行 10-115

| 方法 | 签名 | 说明 |
|------|------|------|
| `exists_label` | `(label_id, label_type=None) -> bool` | 判断标签是否存在 |
| `get_label_info` | `(include_admin_only=False, label_type=None, level=None) -> dict` | 获取标签信息（按 `label_type` 分组） |
| `to_json` | `() -> dict` | 序列化为 JSON |

### 4.1 get_label_info 返回值结构

```python
{
    "source_label": [
        {"label_id": "bkmonitor", "label_name": "监控采集", ...},
        {"label_id": "custom", "label_name": "自定义", ...},
    ],
    "result_table_label": [...],
    "type_label": [...],
}
```

## 五、OptionBase 接口

**文件**: `common.py` 行 120-228

| 方法 | 签名 | 说明 |
|------|------|------|
| `get_option` | `(query_id) -> dict` | 获取选项配置（子类实现查询逻辑） |
| `_parse_value` | `(value) -> tuple[str, str]` | 解析值类型，返回 `(序列化值, 类型)` |
| `_create_option` | `(value, creator) -> OptionBase` | 创建选项实例（子类实现） |
| `get_value` | `() -> Any` | 获取反序列化值（根据 `value_type` 自动转换） |
| `to_json` | `() -> dict` | 返回 `{name: value}` 格式 |

### 5.1 _parse_value 实现

```python
# common.py 行 150-170
@classmethod
def _parse_value(cls, value):
    if type(value) in (bool, list, dict):
        val = json.dumps(value)
        val_type = "bool" if isinstance(value, bool) else (
            "list" if isinstance(value, list) else "dict"
        )
    elif type(value) in (int,):
        val = json.dumps(value)
        val_type = "int"
    else:
        val, val_type = value, "string"
    return val, val_type
```

### 5.2 get_value 实现

```python
# common.py 行 200-220
def get_value(self):
    try:
        option_value = self.TYPE_OPTION_DICT[self.value_type]
        real_value = option_value(self.value) if self.value_type != "string" else str(self.value)
    except KeyError:
        trans_method = getattr(self, f"_trans_{self.value_type}")
        real_value = trans_method()
    return real_value
```

## 六、BaseModel / BaseModelWithTime 接口

**文件**: `common.py` 行 230-260

两个抽象基类仅提供字段定义，无额外方法。用于被 EntityMeta 等模型继承。

| 基类 | 时间字段命名 | 典型使用者 |
|------|-----------|-----------|
| BaseModel | `create_time` / `update_time` | EntityMeta |
| BaseModelWithTime | `created_at` / `updated_at` | 其他需要不同命名的模型 |

---

> **下一步**: 查阅父专家 [结果表管理子专家](../结果表管理子专家/agent.md) 了解 ResultTable 的深入源码细节。
