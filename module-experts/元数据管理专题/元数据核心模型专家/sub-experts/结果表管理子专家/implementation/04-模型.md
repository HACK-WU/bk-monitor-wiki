# 04-模型

> **子专家**: 结果表管理子专家
> **类型**: 实现层文档
> **创建时间**: 2026-07-28

---

## 一、ResultTable 数据模型

**文件**: `result_table.py` 行 58-117

**数据库表**: `metadata_resulttable`

### 1.1 完整字段定义

| 字段 | Django 类型 | 约束 | 默认值 | 说明 |
|------|-----------|------|--------|------|
| `id` | BigAutoField | PK | — | 自增主键 |
| `table_id` | CharField(128) | db_index | — | 结果表ID |
| `bk_tenant_id` | CharField(256) | — | `"system"` | 租户ID |
| `table_name_zh` | CharField(128) | — | — | 结果表中文名 |
| `is_custom_table` | BooleanField | — | — | 是否自定义结果表 |
| `schema_type` | CharField(64) | choices | — | `free`/`dynamic`/`fixed` |
| `default_storage` | CharField(32) | choices | — | 默认存储方案 |
| `creator` | CharField(32) | — | — | 创建者 |
| `create_time` | DateTimeField | auto_now_add | — | 创建时间 |
| `last_modify_user` | CharField(32) | — | — | 最后更新者 |
| `last_modify_time` | DateTimeField | auto_now | — | 最后更新时间 |
| `bk_biz_id` | IntegerField | — | 0 | 业务ID（0=全业务） |
| `is_deleted` | BooleanField | — | False | 是否已删除 |
| `is_enable` | BooleanField | — | True | 是否启用 |
| `label` | CharField(128) | — | — | 结果表标签 |
| `data_label` | CharField(128) | null=True | — | 数据标签 |
| `labels` | JSONField | — | dict | 扩展标签 |
| `is_builtin` | BooleanField | — | False | 是否内置 |
| `bk_biz_id_alias` | CharField(128) | null=True | — | 业务ID别名 |

### 1.2 类常量

| 常量 | 值 | 说明 |
|------|-----|------|
| `SCHEMA_TYPE_FREE` | `"free"` | 无固定字段 |
| `SCHEMA_TYPE_DYNAMIC` | `"dynamic"` | 动态字段 |
| `SCHEMA_TYPE_FIXED` | `"fixed"` | 固定字段 |
| `REAL_STORAGE_DICT` | `{...}` | 存储类型到存储类的映射 |

### 1.3 索引与约束

| 约束类型 | 字段 |
|---------|------|
| **主键** | `id` |
| **唯一索引** | `(table_id, bk_tenant_id)` |
| **普通索引** | `table_id` |

### 1.4 Meta 配置

```python
# result_table.py 行 110-117
class Meta:
    db_table = "metadata_resulttable"
    verbose_name = "结果表"
    verbose_name_plural = "结果表"
    unique_together = ("table_id", "bk_tenant_id")
```

## 二、ResultTableField 数据模型

**文件**: `result_table.py` 行 2500-2580

**数据库表**: `metadata_resulttablefield`

### 2.1 完整字段定义

| 字段 | Django 类型 | 约束 | 说明 |
|------|-----------|------|------|
| `table_id` | CharField(128) | — | 结果表ID |
| `bk_tenant_id` | CharField(256) | — | 租户ID |
| `field_name` | CharField(255) | db_collation | 字段名 |
| `field_type` | CharField(32) | choices | int/float/string/boolean/timestamp/object/nested/long |
| `description` | TextField | — | 字段描述 |
| `unit` | CharField(32) | — | 字段单位 |
| `tag` | CharField(16) | choices | metric/dimension/timestamp/group |
| `is_config_by_user` | BooleanField | — | 是否用户确认字段 |
| `default_value` | CharField(128) | null=True | 字段默认值 |
| `creator` | CharField(32) | — | 创建者 |
| `create_time` | DateTimeField | auto_now_add | 创建时间 |
| `last_modify_user` | CharField(32) | — | 最后更新者 |
| `last_modify_time` | DateTimeField | auto_now | 最后更新时间 |
| `alias_name` | CharField(64) | — | 字段别名（映射前名称） |
| `is_disabled` | BooleanField | — | 是否禁用 |

### 2.2 类常量

| 常量 | 值 | 说明 |
|------|-----|------|
| `FIELD_TAG_METRIC` | `"metric"` | 指标字段 |
| `FIELD_TAG_DIMENSION` | `"dimension"` | 维度字段 |
| `FIELD_TAG_TIMESTAMP` | `"timestamp"` | 时间戳字段 |
| `FIELD_TAG_GROUP` | `"group"` | 标签字段 |

### 2.3 约束

```python
unique_together = ("table_id", "field_name", "bk_tenant_id")
```

### 2.4 属性

| 属性 | 返回类型 | 说明 |
|------|---------|------|
| `is_dimension` | bool | `tag == "dimension"` |
| `is_metric` | bool | `tag == "metric"` |

## 三、ResultTableOption 数据模型

**文件**: `result_table.py` 行 2860-2960

**数据库表**: `metadata_resulttableoption`

### 3.1 完整字段定义

| 字段 | Django 类型 | 约束 | 说明 |
|------|-----------|------|------|
| `table_id` | CharField(128) | db_index | 结果表ID |
| `name` | CharField(128) | choices | 选项名称 |
| `value_type` | CharField(64) | — | bool/string/list |
| `value` | TextField | — | JSON 序列化的值 |
| `creator` | CharField(32) | — | 创建者 |
| `create_time` | DateTimeField | auto_now_add | 创建时间 |
| `bk_tenant_id` | CharField(256) | — | 租户ID |

### 3.2 选项常量

```python
OPTION_CMDB_LEVEL_CONFIG = "cmdb_level_config"
OPTION_ES_DOCUMENT_ID = "es_unique_field_list"
OPTION_SEGMENTED_QUERY_ENABLE = "segmented_query_enable"
OPTION_IS_SPLIT_MEASUREMENT = "is_split_measurement"
OPTION_ENABLE_FIELD_BLACK_LIST = "enable_field_black_list"
OPTION_ENABLE_V4_LOG_DATA_LINK = "enable_log_v4_data_link"
OPTION_ENABLE_PLUGIN_V4_DATA_LINK = "enable_plugin_v4_data_link"
OPTION_BINDING_BCS_CLUSTER_ID = "binding_bcs_cluster_id"
```

## 四、ResultTableFieldOption 数据模型

**文件**: `result_table.py` 行 3000-3150

**数据库表**: `metadata_resulttablefieldoption`

### 4.1 完整字段定义

| 字段 | Django 类型 | 约束 | 说明 |
|------|-----------|------|------|
| `table_id` | CharField(128) | db_index | 结果表ID |
| `field_name` | CharField(255) | db_collation | 字段名 |
| `name` | CharField(128) | choices | 选项名称 |
| `value_type` | CharField(64) | — | 值类型 |
| `value` | TextField | — | JSON 序列化的值 |
| `creator` | CharField(32) | — | 创建者 |
| `create_time` | DateTimeField | auto_now_add | 创建时间 |
| `bk_tenant_id` | CharField(256) | — | 租户ID |

### 4.2 选项常量

```python
OPTION_ES_TYPE = "es_type"
OPTION_ES_INCLUDE_IN_ALL = "es_include_in_all"
OPTION_ES_FORMAT = "es_format"
OPTION_ES_DOC_VALUES = "es_doc_values"
OPTION_ES_INDEX = "es_index"
```

## 五、CMDBLevelRecord 数据模型

**文件**: `result_table.py` 行 3200-3300

**数据库表**: `metadata_cmdblevelrecord`

### 5.1 完整字段定义

| 字段 | Django 类型 | 说明 |
|------|-----------|------|
| `source_table_id` | CharField(128) | 源结果表ID |
| `target_table_id` | CharField(128) | 目标结果表ID |
| `cmdb_level` | CharField(64) | CMDB 层级（如 `set`、`module`） |
| `bk_tenant_id` | CharField(256) | 租户ID |
| `is_enable` | BooleanField | 是否启用 |
| `creator` | CharField(32) | 创建者 |
| `create_time` | DateTimeField(auto_now_add) | 创建时间 |

### 5.2 自定义 Manager

```python
# 仅查询启用的记录
enable_object = CMDBLevelRecordManager()
```

---

> **下一步**: 查阅 [05-接口.md](05-接口.md) 了解 ResultTable 对外暴露的完整接口。
