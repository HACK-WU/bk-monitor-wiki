# 05-接口

> **子专家**: 结果表管理子专家
> **类型**: 实现层文档
> **创建时间**: 2026-07-28

---

## 一、ResultTable 接口

**文件**: `bk-monitor-base/src/bk_monitor_base/metadata/models/result_table.py`

### 1.1 类方法

| 方法 | 签名 | 源码行 | 说明 |
|------|------|--------|------|
| `create_result_table` | `(bk_data_id, table_id, table_name_zh, is_custom_table, schema_type, operator, default_storage, **kwargs) -> ResultTable` | 行 323-570 | 创建结果表（13步流程） |
| `refresh_consul_influxdb_tableinfo` | `() -> None` | 行 580-600 | 刷新 InfluxDB Consul 配置 |
| `is_disable_metric_cutter` | `(table_id, bk_tenant_id) -> bool` | 行 700-710 | 是否禁用指标切分 |
| `get_table_id_cutter` | `(table_ids, bk_tenant_id) -> dict` | 行 715-730 | 批量获取切分配置 |
| `get_result_table_storage_info` | `(table_id, storage_type, bk_tenant_id) -> dict` | 行 735-750 | 获取存储配置 |
| `get_result_table_storage` | `(table_id, storage_type, bk_tenant_id) -> Storage` | 行 755-770 | 获取存储对象 |
| `get_real_storage_list` | `() -> list` | 行 775-785 | 获取支持的存储列表 |
| `get_result_table` | `(table_id, bk_tenant_id) -> ResultTable` | 行 790-810 | 查询结果表（兼容多种命名） |
| `batch_to_json` | `(bk_tenant_id, result_table_id_list, with_option) -> list` | 行 815-850 | 批量序列化 |

### 1.2 实例方法

| 方法 | 签名 | 源码行 | 说明 |
|------|------|--------|------|
| `get_related_datasource` | `(refresh=False) -> DataSource` | 行 228-235 | 获取关联数据源（带缓存） |
| `get_target_bk_biz_id` | `() -> int` | 行 240-250 | 获取目标业务ID |
| `delete_datalink` | `() -> None` | 行 255-270 | 删除数据链路 |
| `apply_datalink` | `(force_update=False) -> None` | 行 600-680 | 应用数据链路（决策树） |
| `check_and_create_storage` | `(bk_tenant_id, is_sync_db, ...) -> None` | 行 800-880 | 检测并创建存储 |
| `create_storage` | `(storage, is_sync_db, ...) -> bool` | 行 890-950 | 创建存储 |
| `bulk_create_fields` | `(field_data, is_etl_refresh, is_force_add, bk_tenant_id) -> bool` | 行 960-1020 | 批量创建字段 |
| `create_field` | `(field_name, field_type, operator, ...) -> bool` | 行 1000-1100 | 创建单个字段 |
| `refresh_etl_config` | `() -> bool` | 行 1100-1145 | 刷新 ETL 配置 |
| `get_storage` | `(storage_type) -> Storage` | 行 1145-1155 | 获取指定存储对象 |
| `get_storage_info` | `(storage_type) -> dict` | 行 1155-1165 | 获取指定存储配置 |
| `raw_delete` | `(qs, using) -> None` | 行 200-210 | 原始删除（绕过 Django） |
| `modify` | `(operator, **kwargs) -> None` | 行 1149-1500 | 修改结果表（逐项更新） |
| `notify_bkdata_log_data_id_changed` | `(data_id) -> bool` | 行 1500-1505 | 通知计算平台 ES 变更 |
| `upgrade_result_table` | `(operator) -> bool` | 行 1505-1570 | 升级为全业务结果表 |
| `set_metric_split` | `(cmdb_level, operator) -> CMDBLevelRecord` | 行 1575-1700 | 设置 CMDB 层级拆分 |
| `clean_metric_split` | `(cmdb_level, operator) -> bool` | 行 1705-1750 | 清理 CMDB 层级拆分 |
| `to_json` | `() -> dict` | 行 1800-1900 | 完整序列化（含字段、存储、选项） |
| `to_json_self_only` | `() -> dict` | 行 1905-1920 | 仅自身字段序列化 |
| `get_tag_values` | `(tag_name) -> list` | 行 1925-1940 | 获取标签值 |

### 1.3 属性

| 属性 | 返回类型 | 源码行 | 说明 |
|------|---------|--------|------|
| `real_storage_list` | list[Storage] | 行 240-260 | 所有实际存储对象 |
| `storage_list` | list[str] | 行 265-275 | 存储方案字符串列表 |
| `data_source` | DataSource | 行 228-235 | 关联的数据源（懒加载） |

## 二、ResultTableField 接口

**文件**: `result_table.py` 行 2500-2800

### 2.1 类方法

| 方法 | 签名 | 说明 |
|------|------|------|
| `create_field` | `(table_id, field_name, field_type, operator, tag, description, unit, alias_name, is_config_by_user, bk_tenant_id, **kwargs) -> ResultTableField` | 创建单个字段 |
| `bulk_create_fields` | `(table_id, field_data, bk_tenant_id) -> list` | 批量创建字段 |
| `bulk_create_default_fields` | `(table_id, include_cmdb_level, is_time_field_only, time_alias_name, time_option, bk_tenant_id) -> None` | 创建默认字段 |
| `batch_get_fields` | `(table_id_list, is_consul_config, bk_tenant_id) -> dict` | 批量获取字段 |

### 2.2 实例方法

| 方法 | 签名 | 说明 |
|------|------|------|
| `to_json` | `() -> dict` | 序列化为 JSON（含 option） |
| `to_json_self_only` | `() -> dict` | 仅自身字段序列化 |

### 2.3 属性

| 属性 | 返回类型 | 说明 |
|------|---------|------|
| `is_dimension` | bool | 是否维度字段 |
| `is_metric` | bool | 是否指标字段 |

## 三、ResultTableOption 接口

**文件**: `result_table.py` 行 2860-2960

| 方法 | 签名 | 说明 |
|------|------|------|
| `create_option` | `(table_id, name, value, creator, bk_tenant_id) -> ResultTableOption` | 创建选项 |
| `bulk_create_options` | `(table_id, option_data, creator, bk_tenant_id) -> None` | 批量创建选项 |
| `batch_result_table_option` | `(table_id_list, bk_tenant_id) -> dict` | 批量获取选项（返回 `{table_id: {name: value}}`） |
| `sync_cmdb_level_option` | `(table_id, operator, bk_tenant_id) -> bool` | 同步 CMDB 层级选项 |

## 四、ResultTableFieldOption 接口

**文件**: `result_table.py` 行 3000-3150

| 方法 | 签名 | 说明 |
|------|------|------|
| `create_option` | `(table_id, field_name, name, value, creator, bk_tenant_id) -> ResultTableFieldOption` | 创建字段选项 |
| `bulk_create_options` | `(table_id, option_data, bk_tenant_id) -> None` | 批量创建字段选项 |
| `batch_field_option` | `(table_id_list, bk_tenant_id) -> dict` | 批量获取字段选项 |
| `get_field_option` | `(table_id, field_name, bk_tenant_id) -> dict` | 获取字段选项 |
| `get_field_option_es_format` | `(table_id, field_name, bk_tenant_id) -> dict` | 获取 ES 格式选项 |

## 五、CMDBLevelRecord 接口

**文件**: `result_table.py` 行 3200-3300

| 属性/方法 | 说明 |
|----------|------|
| `enable_object` | 自定义 Manager，仅查询 `is_enable=True` 的记录 |

---

> **下一步**: 返回父专家 [agent.md](../../agent.md) 查看完整文档导航。
