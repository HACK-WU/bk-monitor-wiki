# 04-模型

> **专家**: 存储与数据链路专家
> **层级**: 实现层
> **最后更新**: 2026-07-28

---

## 一、ClusterInfo — 集群信息配置

位置：[storage.py](/root/bk-monitor/bk-monitor-base/src/bk_monitor_base/metadata/models/storage.py#L82)

| 字段 | 类型 | 说明 |
|------|------|------|
| `bk_tenant_id` | CharField(64) | 租户 ID |
| `cluster_id` | AutoField (PK) | 集群 ID |
| `cluster_name` | CharField(128) | 集群英文名（正则：`[_A-Za-z0-9][_A-Za-z0-9-]{0,49}`） |
| `display_name` | CharField(128) | 集群显示名称 |
| `cluster_type` | CharField(32, db_index) | 集群类型（influxdb/kafka/redis/elasticsearch/argus/victoria_metrics/doris/bkdata） |
| `domain_name` | CharField(128) | 集群域名 |
| `port` | IntegerField | 端口 |
| `extranet_domain_name` | CharField(128) | 外网域名 |
| `extranet_port` | IntegerField | 外网端口 |
| `description` | CharField(256) | 备注说明 |
| `is_default_cluster` | BooleanField | 是否默认集群 |
| `username` | CharField(64) | 用户名 |
| `password` | SymmetricTextField(128) | 密码（加密存储） |
| `version` | CharField(64) | 存储集群版本 |
| `custom_option` | TextField | 自定义标签（JSON） |
| `schema` | CharField(32) | 访问协议（http/https） |
| `is_ssl_verify` | BooleanField | SSL 强验证 |
| `ssl_verification_mode` | CharField(16) | CA 校验模式 |
| `ssl_certificate_authorities` | TextField | CA 内容 |
| `ssl_certificate` | TextField | SSL/TLS 证书 |
| `ssl_certificate_key` | TextField | SSL/TLS 私钥 |
| `ssl_insecure_skip_verify` | BooleanField | 跳过服务器校验 |
| `is_auth` | BooleanField | 是否开启鉴权 |
| `sasl_mechanisms` | CharField(64) | SASL 认证机制 |
| `security_protocol` | CharField(64) | 安全协议 |
| `registered_system` | CharField(128) | 注册来源系统 |
| `registered_to_bkbase` | BooleanField | 是否已注册到 BKBase |
| `is_register_to_gse` | BooleanField | 是否往 GSE 注册 |
| `gse_stream_to_id` | IntegerField | GSE 接收端配置 ID |
| `label` | CharField(32) | 用途标签 |
| `default_settings` | JSONField | 集群默认配置 |
| `creator` | CharField(255) | 创建者 |
| `create_time` | DateTimeField | 创建时间 |
| `last_modify_user` | CharField(32) | 最后更新者 |
| `last_modify_time` | DateTimeField | 最后更新时间 |

**唯一约束**: `(bk_tenant_id, cluster_type, cluster_name)`
**DB 表**: `metadata_clusterinfo`

---

## 二、InfluxDBStorage — 时序存储

位置：[storage.py](/root/bk-monitor/bk-monitor-base/src/bk_monitor_base/metadata/models/storage.py#L827)

| 字段 | 类型 | 说明 |
|------|------|------|
| `table_id` | CharField(128, PK) | 结果表名 |
| `storage_cluster_id` | IntegerField | 存储集群 ID（外键 → ClusterInfo） |
| `real_table_name` | CharField(128) | 实际存储表名 |
| `database` | CharField(128) | 数据库名 |
| `source_duration_time` | CharField(32) | 原始数据保留时间 |
| `down_sample_table` | CharField(128) | 降样结果表名 |
| `down_sample_gap` | CharField(32) | 降样聚合区间 |
| `down_sample_duration_time` | CharField(32) | 降样数据保存时间 |
| `proxy_cluster_name` | CharField(128) | 实际存储集群名（InfluxDB Proxy 路由） |
| `use_default_rp` | BooleanField | 是否使用默认 RP 配置 |
| `enable_refresh_rp` | BooleanField | 是否周期刷新 RP |
| `partition_tag` | CharField(128) | Tag 分组列表 |
| `vm_table_id` | CharField(128) | VM 结果表 ID（已废弃） |
| `influxdb_proxy_storage_id` | IntegerField | Proxy 与存储关联关系 ID |
| `bk_tenant_id` | CharField(256) | 租户 ID |

**唯一约束**: `(real_table_name, database)`
**DB 表**: `metadata_influxdbstorage`

---

## 三、ESStorage — ES 存储

位置：[storage.py](/root/bk-monitor/bk-monitor-base/src/bk_monitor_base/metadata/models/storage.py#L1897)

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BigAutoField (PK) | 主键 |
| `table_id` | CharField(128) | 结果表名 |
| `origin_table_id` | CharField(128) | 原始结果表名（虚拟 RT 场景） |
| `date_format` | CharField(64) | 日期格式化配置（默认 `%Y%m%d%H`） |
| `slice_size` | IntegerField | Index 大小切分阈值（GB，默认 500） |
| `slice_gap` | IntegerField | Index 分片时间间隔（分钟，默认 120） |
| `retention` | IntegerField | Index 保存时间（天，默认 30） |
| `warm_phase_days` | IntegerField | 暖数据等待天数（默认 0） |
| `warm_phase_settings` | JSONField | 暖数据配置（allocation_attr_name/value/type） |
| `time_zone` | IntegerField | 时区设置（[-12, 12]，默认 0） |
| `index_settings` | TextField | 索引配置信息（JSON） |
| `mapping_settings` | TextField | Mapping 配置信息（JSON） |
| `storage_cluster_id` | IntegerField | 存储集群 ID |
| `source_type` | CharField(16) | 数据源类型（默认 "log"） |
| `index_set` | TextField | 索引集 |
| `need_create_index` | BooleanField | 是否需要创建索引 |
| `archive_index_days` | IntegerField | 索引归档天数 |
| `bk_tenant_id` | CharField(256) | 租户 ID |
| `long_term_storage_settings` | TextField | 长期存储配置 |

**唯一约束**: `(table_id, bk_tenant_id)`
**DB 表**: `metadata_esstorage`

---

## 四、KafkaStorage — Kafka 存储

位置：[storage.py](/root/bk-monitor/bk-monitor-base/src/bk_monitor_base/metadata/models/storage.py#L1760)

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | BigAutoField (PK) | 主键 |
| `bk_tenant_id` | CharField(256) | 租户 ID |
| `table_id` | CharField(128) | 结果表名 |
| `topic` | CharField(256) | Topic 名 |
| `partition` | IntegerField | 分区数量（默认 1） |
| `storage_cluster_id` | IntegerField | 存储集群 ID |
| `retention` | IntegerField | 数据过期时间（ms，默认 1800000 = 30 分钟） |

**唯一约束**: `(table_id, bk_tenant_id)`
**DB 表**: `metadata_kafkastorage`

---

## 五、DorisStorage — Doris 存储

位置：[storage.py](/root/bk-monitor/bk-monitor-base/src/bk_monitor_base/metadata/models/storage.py#L5446)

| 字段 | 类型 | 说明 |
|------|------|------|
| `table_id` | CharField(128) | 结果表 ID |
| `bk_tenant_id` | CharField(256) | 租户 ID |
| `bkbase_table_id` | CharField(128) | BKBase 表名 |
| `source_type` | CharField(32) | 数据源类型（默认 "log"） |
| `index_set` | TextField | 索引集 |
| `table_type` | CharField(128) | 物理表类型（primary_table/duplicate_table） |
| `field_config_mapping` | TextField | 字段/分词配置（JSON） |
| `expire_days` | IntegerField | 过期天数（默认 30） |
| `storage_cluster_id` | IntegerField | 存储集群 ID |

**DB 表**: `metadata_dorisstorage`

---

## 六、DataLink — 数据链路

位置：[data_link.py](/root/bk-monitor/bk-monitor-base/src/bk_monitor_base/metadata/models/data_link/data_link.py#L106)

| 字段 | 类型 | 说明 |
|------|------|------|
| `data_link_name` | CharField(255, PK) | 链路名称 |
| `bk_tenant_id` | CharField(256) | 租户 ID |
| `bk_data_id` | IntegerField | 关联数据源 ID |
| `table_ids` | JSONField | 关联结果表 ID 列表 |
| `namespace` | CharField(255) | 命名空间 |
| `data_link_strategy` | CharField(255) | 链路策略（11 种策略） |
| `create_time` | DateTimeField | 创建时间 |
| `last_modify_time` | DateTimeField | 最后更新时间 |

**DB 表**: `metadata_datalink`

---

## 七、DataLinkResourceConfigBase — 链路配置基类

位置：[data_link_configs.py](/root/bk-monitor/bk-monitor-base/src/bk_monitor_base/metadata/models/data_link/data_link_configs.py#L26)

| 字段 | 类型 | 说明 |
|------|------|------|
| `kind` | CharField(64) | 配置类型（DataId/ResultTable/VmStorageBinding/...） |
| `name` | CharField(64) | 实例名称 |
| `namespace` | CharField(64) | 命名空间 |
| `create_time` | DateTimeField | 创建时间 |
| `last_modify_time` | DateTimeField | 最后更新时间 |
| `status` | CharField(64) | 状态 |
| `data_link_name` | CharField(64) | 数据链路名称 |
| `bk_biz_id` | BigIntegerField | 业务 ID |
| `bk_tenant_id` | CharField(256) | 租户 ID |

**抽象基类**（`abstract = True`）

### 7.1 子类速查

| 子类 | kind 值 | 额外字段 | DB 表 |
|------|---------|---------|-------|
| `DataIdConfig` | `DataId` | `bk_data_id` | `metadata_dataidconfig` |
| `ResultTableConfig` | `ResultTable` | `bkbase_result_table_name`, `bkbase_result_table_config` | `metadata_resulttableconfig` |
| `VMStorageBindingConfig` | `VmStorageBinding` | `vm_cluster_name`, `vm_result_table_name` | `metadata_vmstoragebindingconfig` |
| `ESStorageBindingConfig` | `ElasticSearchBinding` | `storage_cluster_name`, `bkbase_result_table_name` | `metadata_esstoragebindingconfig` |
| `DorisStorageBindingConfig` | `DorisBinding` | `storage_cluster_name`, `bkbase_result_table_name` | `metadata_dorisstoragebindingconfig` |
| `DataBusConfig` | `Databus` | `data_id_name`, `sink_names`, `transformer_config` | `metadata_databusconfig` |
| `ConditionalSinkConfig` | `ConditionalSink` | `conditions` | `metadata_conditionalsinkconfig` |
| `ClusterConfig` | — | `cluster_id`, `cluster_type` | `metadata_clusterconfig` |

---

## 八、ES 快照模型

### 8.1 EsSnapshot

位置：[es_snapshot.py](/root/bk-monitor/bk-monitor-base/src/bk_monitor_base/metadata/models/es_snapshot.py#L39)

| 字段 | 类型 | 说明 |
|------|------|------|
| `id` | AutoField (PK) | 主键 |
| `table_id` | CharField(128) | 结果表 ID |
| `target_snapshot_repository_name` | CharField(128) | 快照仓库名称 |
| `snapshot_days` | IntegerField | 快照天数（0 = 永久） |
| `creator` | CharField(32) | 创建者 |
| `create_time` | DateTimeField | 创建时间 |
| `last_modify_user` | CharField(32) | 最后更新者 |
| `last_modify_time` | DateTimeField | 最后更新时间 |
| `status` | CharField(16) | 快照状态（running/stopped） |
| `bk_tenant_id` | CharField(256) | 租户 ID |

**唯一约束**: `(table_id, target_snapshot_repository_name)`
**DB 表**: `metadata_essnapshot`

### 8.2 EsSnapshotRepository

位置：[es_snapshot.py](/root/bk-monitor/bk-monitor-base/src/bk_monitor_base/metadata/models/es_snapshot.py#L554)

| 字段 | 类型 | 说明 |
|------|------|------|
| `repository_name` | CharField | 仓库名称 |
| `cluster_id` | IntegerField | 关联集群 ID |
| `is_deleted` | BooleanField | 软删除标记 |

### 8.3 EsSnapshotIndice

位置：[es_snapshot.py](/root/bk-monitor/bk-monitor-base/src/bk_monitor_base/metadata/models/es_snapshot.py#L658)

| 字段 | 说明 |
|------|------|
| `snapshot` | FK → EsSnapshot |
| `indice_name` | 索引名 |

### 8.4 EsSnapshotRestore

位置：[es_snapshot.py](/root/bk-monitor/bk-monitor-base/src/bk_monitor_base/metadata/models/es_snapshot.py#L722)

快照恢复任务记录。

---

## 九、InfluxDB 集群模型

### 9.1 InfluxDBTagInfo

位置：[influxdb_cluster.py](/root/bk-monitor/bk-monitor-base/src/bk_monitor_base/metadata/models/influxdb_cluster.py#L63)

| 字段 | 类型 | 说明 |
|------|------|------|
| `database` | CharField(128) | 数据库名 |
| `measurement` | CharField(128) | 表名 |
| `tag_name` | CharField(128) | Tag 名 |
| `tag_value` | CharField(128) | Tag 值 |
| `cluster_name` | CharField(128) | 集群名 |
| `host_list` | CharField(128) | 使用中的机器 |
| `manual_unreadable_host` | CharField(128) | 静态不可读机器 |
| `force_overwrite` | BooleanField | 是否强制写入 |

**唯一约束**: `(database, measurement, tag_name, tag_value, cluster_name)`

### 9.2 InfluxDBClusterInfo

位置：[influxdb_cluster.py](/root/bk-monitor/bk-monitor-base/src/bk_monitor_base/metadata/models/influxdb_cluster.py#L339)

集群拓扑信息，包含 `cluster_name`, `host_name`, `port`, `status` 等字段。

### 9.3 InfluxDBHostInfo

位置：[influxdb_cluster.py](/root/bk-monitor/bk-monitor-base/src/bk_monitor_base/metadata/models/influxdb_cluster.py#L473)

集群主机信息，包含 `host_name`, `port`, `status`, `cluster_name` 等字段。

### 9.4 InfluxDBProxyStorage

位置：[influxdb_cluster.py](/root/bk-monitor/bk-monitor-base/src/bk_monitor_base/metadata/models/influxdb_cluster.py#L798)

| 字段 | 说明 |
|------|------|
| `proxy_cluster_id` | Proxy 集群 ID（FK → ClusterInfo） |
| `instance_cluster_name` | 后端 InfluxDB 实例名 |
| `is_default` | 是否默认映射 |

---

## 十、VM 模型

### 10.1 AccessVMRecord

位置：[record.py](/root/bk-monitor/bk-monitor-base/src/bk_monitor_base/metadata/models/vm/record.py#L16)

| 字段 | 类型 | 说明 |
|------|------|------|
| `data_type` | CharField(32) | 数据类型（bcs_cluster_k8s/bcs_cluster_custom/user_custom/access_vm） |
| `result_table_id` | CharField(128) | 结果表 ID |
| `bcs_cluster_id` | CharField(32) | BCS 集群 ID |
| `storage_cluster_id` | IntegerField | 存储集群 ID |
| `vm_cluster_id` | IntegerField | VM 集群 ID |
| `bk_base_data_id` | IntegerField | 计算平台数据 ID |
| `bk_base_data_name` | CharField(64) | 计算平台数据名称 |
| `vm_result_table_id` | CharField(64) | VM 结果表 ID |
| `remark` | CharField(256) | 接入备注 |
| `bk_tenant_id` | CharField(256) | 租户 ID |

**DB 表**: `metadata_accessvmrecord`

### 10.2 SpaceVMInfo

位置：[record.py](/root/bk-monitor/bk-monitor-base/src/bk_monitor_base/metadata/models/vm/record.py#L105)

| 字段 | 类型 | 说明 |
|------|------|------|
| `space_type` | CharField(64) | 空间类型 |
| `space_id` | CharField(128) | 空间 ID |
| `vm_cluster_id` | IntegerField | VM 集群 ID |
| `vm_retention_time` | CharField(16) | 保留时间（默认 "30d"） |
| `status` | CharField(16) | 状态 |

**DB 表**: `metadata_spacevminfo`

---

## 十一、StorageClusterRecord — 集群迁移记录

位置：[storage.py](/root/bk-monitor/bk-monitor-base/src/bk_monitor_base/metadata/models/storage.py#L5328)

| 字段 | 类型 | 说明 |
|------|------|------|
| `bk_tenant_id` | CharField(256) | 租户 ID |
| `table_id` | CharField(128) | 结果表 ID |
| `cluster_id` | IntegerField | 集群 ID |
| `is_current` | BooleanField | 是否当前集群 |
| `enable_time` | DateTimeField | 启用时间 |
| `disable_time` | DateTimeField | 停用时间 |

**DB 表**: `metadata_storageclusterrecord`
