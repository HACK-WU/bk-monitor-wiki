# 03-数据流转

**本文引用的文件**
- [space_table_id_redis.py](file://bk-monitor-base/src/bk_monitor_base/metadata/models/space/space_table_id_redis.py)
- [base.py](file://bk-monitor-base/src/bk_monitor_base/metadata/models/custom_report/base.py)
- [time_series.py](file://bk-monitor-base/src/bk_monitor_base/metadata/models/custom_report/time_series.py)
- [event.py](file://bk-monitor-base/src/bk_monitor_base/metadata/models/custom_report/event.py)
- [rules.py](file://bk-monitor-base/src/bk_monitor_base/metadata/models/record_rule/rules.py)

## 目录
1. [简介](#简介)
2. [空间路由推送数据流](#空间路由推送数据流)
3. [自定义上报数据流](#自定义上报数据流)
4. [记录规则数据流](#记录规则数据流)
5. [结论](#结论)

## 简介

本章描述空间与自定义上报专家覆盖的三条核心数据流：空间路由从 DB → Redis → 消费者的推送链路、自定义上报从接入 → 校验 → 存储 → 查询的完整链路、记录规则从配置 → 计算平台 → 结果存储的执行链路。

## 空间路由推送数据流

### 整体流程

```mermaid
sequenceDiagram
    participant Trigger as "触发源<br/>(API/任务)"
    participant STR as "SpaceTableIDRedis"
    participant DB as "MySQL"
    participant Redis as "Redis"
    participant Consumer as "消费者<br/>(unify-query等)"

    Trigger->>STR: "push_space_table_ids(space_type, space_id)"
    STR->>DB: "查询 Space/DataSource/ResultTable"
    DB-->>STR: "返回关联数据"
    STR->>STR: "按空间类型组装路由"
    STR->>Redis: "HMSET SPACE_TO_RESULT_TABLE_KEY"
    STR->>Redis: "HMSET RESULT_TABLE_DETAIL_KEY"
    opt is_publish=True
        STR->>Redis: "PUBLISH SPACE_TO_RESULT_TABLE_CHANNEL"
        STR->>Redis: "PUBLISH RESULT_TABLE_DETAIL_CHANNEL"
    end
    Redis-->>Consumer: "收到变更通知"
    Consumer->>Redis: "HGET 拉取最新路由"
    Redis-->>Consumer: "返回路由数据"
```

图表来源：[space_table_id_redis.py:67-117](file://bk-monitor-base/src/bk_monitor_base/metadata/models/space/space_table_id_redis.py#L67-L117)

### 三类路由数据

| 路由类型 | 写入方法 | 数据格式 | 消费者用途 |
|---------|---------|---------|-----------|
| 空间-结果表 | `push_space_table_ids` | `{space_uid: {table_id: {filters: [...]}}}` | 空间数据查询隔离 |
| 数据标签-结果表 | `push_data_label_table_ids` | `{data_label: [table_id, ...]}` | 按标签跨空间查询 |
| 结果表详情 | `push_table_id_detail` | `{table_id: {db, measurement, storage_type, fields, ...}}` | 结果表存储路由 |

章节来源：[space_table_id_redis.py:67-500](file://bk-monitor-base/src/bk_monitor_base/metadata/models/space/space_table_id_redis.py#L67-L500)

## 自定义上报数据流

### 时序自定义上报全链路

```mermaid
sequenceDiagram
    participant Agent as "采集器"
    participant Transfer as "Transfer"
    participant Redis as "Redis<br/>(指标缓存)"
    participant InfluxDB as "InfluxDB/VM"
    participant Query as "查询服务"

    Note over Agent,Query: === 数据上报阶段 ===
    Agent->>Transfer: "上报时序数据"
    Transfer->>Transfer: "按 data_id 路由"
    Transfer->>Redis: "ZADD 指标名+时间戳"
    Transfer->>Redis: "HMSET 指标维度信息"
    Transfer->>InfluxDB: "写入时序数据"

    Note over Agent,Query: === 指标同步阶段 ===
    Query->>Redis: "ZRANGEBYSCORE 拉取指标"
    Query->>Redis: "HMGET 拉取维度"
    Query->>Query: "update_time_series_metrics()"
    Query->>DB: "更新 TimeSeriesMetric 表"

    Note over Agent,Query: === 路由推送阶段 ===
    Query->>Redis: "HMSET 结果表详情"
    Query->>Redis: "PUBLISH 变更通知"
```

图表来源：[time_series.py:500-1000](file://bk-monitor-base/src/bk_monitor_base/metadata/models/custom_report/time_series.py#L500-L1000)

### 事件自定义上报全链路

```mermaid
sequenceDiagram
    participant Agent as "采集器"
    participant Transfer as "Transfer"
    participant ES as "Elasticsearch"
    participant Metadata as "元数据服务"

    Note over Agent,Metadata: === 数据上报阶段 ===
    Agent->>Transfer: "上报事件数据"
    Transfer->>ES: "写入事件（按 data_id 路由）"

    Note over Agent,Metadata: === 维度发现阶段 ===
    Metadata->>ES: "聚合查询 event_name"
    ES-->>Metadata: "返回事件名列表"
    Metadata->>ES: "采样查询 dimensions"
    ES-->>Metadata: "返回维度列表"
    Metadata->>Metadata: "update_event_dimensions_from_es()"
    Metadata->>DB: "更新 Event 表"
```

图表来源：[event.py:200-300](file://bk-monitor-base/src/bk_monitor_base/metadata/models/custom_report/event.py#L200-L300)

### 日志自定义上报全链路

```mermaid
sequenceDiagram
    participant Agent as "采集器"
    participant BkCollector as "BkCollector"
    participant BKData as "计算平台"
    participant Metadata as "元数据服务"

    Note over Agent,Metadata: === 配置下发阶段 ===
    Metadata->>Metadata: "create_log_group()"
    Metadata->>Metadata: "生成 bk_data_token"
    Metadata->>BkCollector: "refresh_custom_log_report_config()"

    Note over Agent,Metadata: === 数据上报阶段 ===
    Agent->>BkCollector: "上报日志（带 token 鉴权）"
    BkCollector->>BKData: "写入计算平台"
```

图表来源：[log.py:30-152](file://bk-monitor-base/src/bk_monitor_base/metadata/models/custom_report/log.py#L30-L152)

### 自定义上报创建完整数据流

```mermaid
flowchart TD
    Start(["create_custom_group"]) --> PreCheck["pre_check<br/>1. 标签存在性<br/>2. data_id 唯一性<br/>3. 同名组唯一性"]
    PreCheck --> Create["_create<br/>1. 生成 table_id<br/>2. 创建 group 记录"]
    Create --> CreateRT["create_result_table<br/>1. 创建 ResultTable<br/>2. 关联存储引擎<br/>3. 创建时间字段<br/>4. 设置 label"]
    CreateRT --> UpdateMetrics["update_metrics<br/>1. 创建指标字段<br/>2. 创建维度字段"]
    UpdateMetrics --> DSOption["DataSourceOption<br/>添加 flat_batch_key"]
    DSOption --> RefreshConsul["refresh_custom_report_config<br/>异步刷新配置到节点管理"]
    RefreshConsul --> End(["完成"])
```

图表来源：[base.py:180-350](file://bk-monitor-base/src/bk_monitor_base/metadata/models/custom_report/base.py#L180-L350)

## 记录规则数据流

### 规则创建到数据流启动

```mermaid
sequenceDiagram
    participant User as "用户/API"
    participant RRS as "RecordRuleService"
    participant RR as "RecordRule"
    participant RTF as "ResultTableFlow"
    participant BKData as "计算平台"
    participant VM as "VictoriaMetrics"

    User->>RRS: "create_record_rule(space, name, config)"
    RRS->>RR: "transform_bk_sql_and_metrics(config)"
    Note over RR: YAML → PromQL → BKSQL
    RR-->>RRS: "返回 BKSQL/指标/源表"

    RRS->>RR: "get_src_table_ids(space, metrics)"
    Note over RR: 通过指标反查源 VM 结果表
    RR-->>RRS: "返回源 VM 结果表列表"

    RRS->>RR: "get_dst_table_id(table_id)"
    RR-->>RRS: "返回目标 VM 结果表名"

    RRS->>RRS: "创建结果表/字段/VM 映射"
    RRS->>RRS: "写入 RecordRule 记录"

    RRS->>RTF: "create_flow(table_id)"
    RTF->>RTF: "compose_source_node"
    RTF->>RTF: "compose_process_node (promql_v2)"
    RTF->>RTF: "compose_vm_storage"
    RTF->>BKData: "apply_data_flow"
    BKData-->>RTF: "返回 flow_id"

    RRS->>RTF: "start_flow(flow_id, consuming_mode=Current)"
    RTF->>BKData: "start_data_flow"
    BKData-->>RTF: "启动完成"

    Note over VM: 数据流持续运行<br/>stream_source → promql_v2 → vm_storage
```

图表来源：[rules.py:39-399](file://bk-monitor-base/src/bk_monitor_base/metadata/models/record_rule/rules.py#L39-L399)

### 数据流节点拓扑

```mermaid
graph LR
    SRC1["源 VM RT 1<br/>stream_source"] --> PROC["promql_v2<br/>BKSQL 预计算"]
    SRC2["源 VM RT 2<br/>stream_source"] --> PROC
    SRC3["源 VM RT N<br/>stream_source"] --> PROC
    PROC --> STORAGE["vm_storage<br/>目标 VM RT"]
```

图表来源：[rules.py:277-352](file://bk-monitor-base/src/bk_monitor_base/metadata/models/record_rule/rules.py#L277-L352)

## 结论

空间与自定义上报专家的三条数据流形成闭环：空间路由推送确保消费者能正确路由到各空间的数据，自定义上报管理数据从采集到存储的元数据配置，记录规则在已有数据基础上创建预计算加速查询。三条流通过 Space 隔离机制和 Redis Pub/Sub 通知机制协同工作。
