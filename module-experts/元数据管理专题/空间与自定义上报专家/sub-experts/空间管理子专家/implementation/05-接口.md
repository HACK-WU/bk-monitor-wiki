# 05-接口

**本文引用的文件**
- [space.py](file://bk-monitor-base/src/bk_monitor_base/metadata/models/space/space.py)
- [managers.py](file://bk-monitor-base/src/bk_monitor_base/metadata/models/space/managers.py)
- [space_table_id_redis.py](file://bk-monitor-base/src/bk_monitor_base/metadata/models/space/space_table_id_redis.py)
- [utils.py](file://bk-monitor-base/src/bk_monitor_base/metadata/models/space/utils.py)

## 目录
1. [简介](#简介)
2. [Space 模型方法签名](#space-模型方法签名)
3. [SpaceManager 方法签名](#spacemanager-方法签名)
4. [SpaceTypeManager 方法签名](#spacetypemanager-方法签名)
5. [SpaceResourceManager 方法签名](#spaceresourcemanager-方法签名)
6. [SpaceTableIDRedis 方法签名](#spacetableidredis-方法签名)
7. [空间工具函数签名](#空间工具函数签名)
8. [结论](#结论)

## 简介

本章列出空间管理子专家各模型和工具函数对外暴露的方法签名，供快速查阅。详细契约见 [C1-能力契约.md](../C1-能力契约.md)。

## Space 模型方法签名

### Space

**类**：`Space(BaseModel)` | 文件：[space.py:41-100](file://bk-monitor-base/src/bk_monitor_base/metadata/models/space/space.py#L41-L100)

| 方法 | 签名 |
|------|------|
| `space_uid` | `@property → str` |
| `to_dict` | `(fields: list\|None, exclude: list\|None) → dict[str, Any]` |
| `bulk_create_space` | `@classmethod (bk_tenant_id: str, space_type_id: str, space_list: list) → None` |
| `get_bk_biz_id` | `() → int` |

### SpaceType

**类**：`SpaceType(BaseModel)` | 文件：[space.py:23-38](file://bk-monitor-base/src/bk_monitor_base/metadata/models/space/space.py#L23-L38)

无公开自定义方法（使用 Django ORM 默认 CRUD）。

### SpaceDataSource

**类**：`SpaceDataSource(BaseModel)` | 文件：[space.py:103-113](file://bk-monitor-base/src/bk_monitor_base/metadata/models/space/space.py#L103-L113)

无公开自定义方法。

### SpaceResource

**类**：`SpaceResource(BaseModel)` | 文件：[space.py:116-137](file://bk-monitor-base/src/bk_monitor_base/metadata/models/space/space.py#L116-L137)

无公开自定义方法。

### SpaceStickyInfo

**类**：`SpaceStickyInfo(models.Model)` | 文件：[space.py:140-148](file://bk-monitor-base/src/bk_monitor_base/metadata/models/space/space.py#L140-L148)

无公开自定义方法。

### BkAppSpaceRecord

**类**：`BkAppSpaceRecord(BaseModel)` | 文件：[space.py:151-162](file://bk-monitor-base/src/bk_monitor_base/metadata/models/space/space.py#L151-L162)

无公开自定义方法。

## SpaceManager 方法签名

**类**：`SpaceManager(models.Manager)` | 文件：[managers.py:24-170](file://bk-monitor-base/src/bk_monitor_base/metadata/models/space/managers.py#L24-L170)

| 方法 | 签名 |
|------|------|
| `compose_space_uid` | `(space_type_id: str, space_id: str) → str` |
| `split_space_uid` | `(space_uid: str) → tuple[str, str]` |
| `list_all_spaces` | `(bk_tenant_id: str, space_type_id?: str, space_id?: str, space_name?: str, id?: int, is_exact?: bool, page?: int, page_size?: int, exclude_platform_space?: bool, space_type_id_name?: dict) → dict` |
| `get_space_info_by_biz_id` | `(bk_biz_id: int) → dict[str, Any]` |
| `get_biz_id_by_space` | `(space_type: str, space_id: str) → int\|None` |

**内部方法**：
| 方法 | 签名 |
|------|------|
| `_filter` | `(spaces: QuerySet, bk_tenant_id: str, space_name?: str, space_id?: str, space_type_id?: str, id?: int, is_exact?: bool, exclude_platform_space?: bool) → QuerySet` |

## SpaceTypeManager 方法签名

**类**：`SpaceTypeManager(models.Manager)` | 文件：[managers.py:14-21](file://bk-monitor-base/src/bk_monitor_base/metadata/models/space/managers.py#L14-L21)

| 方法 | 签名 |
|------|------|
| `list_all_space_types` | `() → list[dict]` |

## SpaceResourceManager 方法签名

**类**：`SpaceResourceManager(models.Manager)` | 文件：[managers.py:164-171](file://bk-monitor-base/src/bk_monitor_base/metadata/models/space/managers.py#L164-L171)

| 方法 | 签名 |
|------|------|
| `get_resource_by_resource_type` | `(space_type_id: str, resource_type: str) → list[dict]` |

## SpaceTableIDRedis 方法签名

**类**：`SpaceTableIDRedis` | 文件：[space_table_id_redis.py:61-1684](file://bk-monitor-base/src/bk_monitor_base/metadata/models/space/space_table_id_redis.py#L61-L1684)

| 方法 | 签名 |
|------|------|
| `push_space_table_ids` | `(space_type: str, space_id: str, is_publish?: bool) → None` |
| `push_multi_space_table_ids` | `(spaces: list[Space], is_publish?: bool) → None` |
| `push_data_label_table_ids` | `(data_label_list?: list, table_id_list?: list, is_publish?: bool, bk_tenant_id?: str) → None` |
| `push_es_table_id_detail` | `(bk_tenant_id: str, table_id_list?: list, is_publish?: bool) → None` |
| `push_doris_table_id_detail` | `(bk_tenant_id: str, table_id_list: list, is_publish?: bool) → None` |
| `push_bkbase_table_id_detail` | `(bk_tenant_id: str, table_id_list?: list, is_publish?: bool) → None` |
| `push_table_id_detail` | `(table_id_list?: list, is_publish?: bool, include_es_table_ids?: bool, bk_tenant_id?: str) → None` |

## 空间工具函数签名

**文件**：[utils.py:1-1161](file://bk-monitor-base/src/bk_monitor_base/metadata/models/space/utils.py#L1-L1161)

### 空间 CRUD

| 函数 | 签名 |
|------|------|
| `create_space` | `(bk_tenant_id: str, creator: str, space_id: str, space_type_id: str, space_name: str, resources?: list, space_code?: str) → dict` |
| `update_space` | `(updater: str, space_type_id: str, space_id: str, space_name: str, space_code: str, resources: list[dict[str, Any]]) → dict[str, Any]` |
| `merge_space` | `(src_space_type_id: str, src_space_id: str, dst_space_type_id: str, dst_space_id: str) → None` |
| `disable_space` | `(spaces: list[dict[str, Any]]) → list[dict[str, Any]]` |

### 批量创建

| 函数 | 签名 |
|------|------|
| `create_bkcc_spaces` | `(biz_list: list[dict[str, Any]], create_builtin_data_link_delay?: bool) → bool` |
| `create_bcs_spaces` | `(project_list: list) → bool` |
| `create_bkcc_space_data_source` | `(biz_data_id_dict: dict) → None` |

### 查询函数

| 函数 | 签名 |
|------|------|
| `list_spaces` | `(bk_tenant_id: str, space_type_id?: str, space_id?: str, space_name?: str, id?: int, is_exact?: bool, is_detail?: bool, page?: int, page_size?: int, exclude_platform_space?: bool, include_resource_id?: bool) → dict` |
| `get_space_detail` | `(space_type_id?: str, space_id?: str, id?: int) → dict[str, Any]` |
| `get_dimension_values` | `(space_type_id: str, space_id: str, resource_type?: str) → list` |
| `get_related_spaces` | `(space_type_id: str, space_id: str, target_space_type_id?: str) → list` |
| `get_negative_space_related_info` | `(negative_biz_id: int) → dict` |
| `get_biz_ids_by_space_ids` | `(space_type: str, space_ids: list) → list` |
| `get_space_by_table_id` | `(table_id: str, bk_tenant_id?: str) → dict[str, Any]` |
| `get_valid_bcs_projects` | `(bk_tenant_id: str) → list` |

### 数据源管理

| 函数 | 签名 |
|------|------|
| `authorize_data_id_list` | `(space_type: str, space_id: str, data_id_list: list) → None` |
| `bulk_create_space_data_source` | `(space_type: str, space_id: str, data_id_list: list, from_authorization?: bool) → None` |
| `get_platform_data_id_list` | `() → list` |
| `get_space_data_id_list` | `(space_type_id: str) → list` |
| `set_data_source_space_type` | `(data_id_list: list, space_type_id: str) → None` |

### 结果表查询

| 函数 | 签名 |
|------|------|
| `get_platform_result_table_id_list` | `() → list` |
| `get_platform_result_table_list` | `() → list` |
| `get_platform_result_table_field_list` | `(tag?: str) → list` |
| `get_result_table_list` | `(filter_platform?: bool, table_id_list?: list) → list` |
| `get_result_table_data_id_dict` | `(data_id_list?: list, table_id?: str) → dict` |
| `get_data_id_list_by_biz_id` | `(bk_biz_id: int) → list` |

### 集群与项目

| 函数 | 签名 |
|------|------|
| `get_metadata_project_dict` | `(project_id?: str, desire_all_data?: bool) → dict` |
| `get_data_id_by_cluster` | `(cluster_id?: str, desire_all_data?: bool) → dict` |
| `get_shared_cluster_namespaces` | `(bk_tenant_id: str, cluster_id: str, project_code: str) → list` |
| `get_project_clusters` | `(bk_tenant_id: str, project_id: str) → list` |
| `get_metadata_cluster_list` | `() → list` |
| `add_cluster_data_id_list` | `(space_type_id: str, space_id: str, cluster_data_id_list: list, is_shared: bool) → list` |
| `compose_bcs_space_data_source` | `(bk_tenant_id: str, space_type_id: str, space_id: str, data_id_list: list, project_cluster_data_id_list: list, shared_cluster_data_id_list: list) → list` |

### 缓存函数

| 函数 | 签名 |
|------|------|
| `cached_ts_metric_data_id_list` | `() → list` |
| `cached_cluster_data_id_list` | `() → list` |
| `cached_cluster_k8s_data_id` | `() → dict[str, int]` |

### 辅助函数

| 函数 | 签名 |
|------|------|
| `reformat_table_id` | `(table_id: str) → str` |
| `create_bksaas_space_resource` | `(space_type: str, space_id: str, creator: str) → None` |

## 结论

空间管理子专家共暴露 40+ 个公开方法/函数，覆盖空间 CRUD、批量创建、数据源授权、结果表查询、集群管理、缓存六大功能域。方法签名详见各节，完整契约见 [C1-能力契约.md](../C1-能力契约.md)。
