# 05-接口

**本文引用的文件**
- [base.py](file://bk-monitor-base/src/bk_monitor_base/metadata/models/custom_report/base.py) — CustomGroupBase
- [time_series.py](file://bk-monitor-base/src/bk_monitor_base/metadata/models/custom_report/time_series.py) — TimeSeriesGroup
- [event.py](file://bk-monitor-base/src/bk_monitor_base/metadata/models/custom_report/event.py) — EventGroup + Event
- [log.py](file://bk-monitor-base/src/bk_monitor_base/metadata/models/custom_report/log.py) — LogGroup
- [rules.py](file://bk-monitor-base/src/bk_monitor_base/metadata/models/record_rule/rules.py) — RecordRule + ResultTableFlow

## 目录
1. [简介](#简介)
2. [CustomGroupBase 方法签名](#customgroupbase-方法签名)
3. [TimeSeriesGroup 方法签名](#timeseriesgroup-方法签名)
4. [EventGroup 方法签名](#eventgroup-方法签名)
5. [Event 方法签名](#event-方法签名)
6. [LogGroup 方法签名](#loggroup-方法签名)
7. [RecordRule 方法签名](#recordrule-方法签名)
8. [ResultTableFlow 方法签名](#resulttableflow-方法签名)
9. [结论](#结论)

## 简介

本章列出自定义上报子专家各模型对外暴露的方法签名，供快速查阅。详细契约见 [C1-能力契约.md](../C1-能力契约.md)。

## CustomGroupBase 方法签名

**类**：`CustomGroupBase(models.Model)` | 文件：[base.py:30-500](file://bk-monitor-base/src/bk_monitor_base/metadata/models/custom_report/base.py#L30-L500)

| 方法 | 签名 |
|------|------|
| `create_custom_group` | `@classmethod (bk_data_id: int, bk_biz_id: int, custom_group_name: str, label: str, operator: str, bk_tenant_id: str, metric_info_list?: list, table_id?: str, is_builtin?: bool, is_split_measurement?: bool, default_storage_config?: dict, additional_options?: dict, data_label?: str, bk_biz_id_alias?: str, **kwargs) → Self` |
| `modify_custom_group` | `(operator: str, custom_group_name?: str, label?: str, is_enable?: bool, metric_info_list?: list, field_list?: list, max_rate?: int, enable_field_black_list?: bool, data_label?: str, options?: dict) → bool` |
| `delete_custom_group` | `(operator: str) → bool` |
| `pre_check` | `@classmethod (label: str, bk_data_id: int, custom_group_name: str, bk_biz_id: int, bk_tenant_id: str) → dict` |
| `make_table_id` | `@staticmethod (bk_biz_id: int, bk_data_id: int, bk_tenant_id: str, table_name?: str) → str` |
| `_create` | `@classmethod (table_id?: str, bk_biz_id: int, bk_data_id: int, custom_group_name: str, label: str, operator: str, is_split_measurement: bool, bk_tenant_id: str, max_rate?: int, **filter_kwargs) → tuple[str, Self]` |
| `_post_process_create` | `@classmethod (custom_group, kwargs) → None` |
| `update_metrics` | `(metric_info) → NotImplementedError`（子类实现） |
| `remove_metrics` | `() → NotImplementedError`（子类实现） |
| `set_table_id_disable` | `() → None` |
| `get_datasource_options` | `() → list` |
| `custom_group_id` | `@property → Any` |
| `custom_group_name` | `@property → Any` (getter/setter) |
| `data_label` | `@property → str` |
| `datasource_options` | `@property → list` |
| `result_table_option` | `@property → dict` |

## TimeSeriesGroup 方法签名

**类**：`TimeSeriesGroup(CustomGroupBase)` | 文件：[time_series.py:50-2593](file://bk-monitor-base/src/bk_monitor_base/metadata/models/custom_report/time_series.py#L50-L2593)

### 创建/修改/删除

| 方法 | 签名 |
|------|------|
| `create_time_series_group` | `@classmethod (bk_data_id, bk_biz_id, time_series_group_name, label, operator, bk_tenant_id, metric_info_list?, table_id?, is_split_measurement?, is_builtin?, default_storage_config?, additional_options?, data_label?) → TimeSeriesGroup` |
| `modify_time_series_group` | `(operator, time_series_group_name?, label?, is_enable?, field_list?, enable_field_black_list?, metric_info_list?, data_label?, options?) → bool` |
| `delete_time_series_group` | `(operator) → bool` |

### 指标管理

| 方法 | 签名 |
|------|------|
| `update_time_series_metrics` | `() → bool` |
| `get_metric_info_list` | `() → list` |
| `get_metric_info_list_with_label` | `(dimension_name, dimension_value) → list` |
| `get_ts_metrics_by_dimension` | `(dimension_name, dimension_value) → list` |
| `get_metrics_from_redis` | `(expired_time?) → list` |
| `get_metric_from_bkdata` | `() → list` |
| `is_auto_discovery` | `() → bool` |
| `update_metrics` | `(metric_info) → bool` |
| `remove_metrics` | `() → None` |

### 表 ID 生成

| 方法 | 签名 |
|------|------|
| `make_table_id` | `@staticmethod (bk_biz_id, bk_data_id, table_name?, bk_tenant_id?) → str` |
| `make_metric_table_id` | `(field_name) → str` |

### 维度与字段

| 方法 | 签名 |
|------|------|
| `update_tag_fields` | `(table_id, tag_list, update_description?) → bool` |
| `update_metric_field` | `(field_name, is_disabled) → bool` |
| `bulk_refresh_rt_fields` | `(table_id, metric_info) → None` |

### 序列化

| 方法 | 签名 |
|------|------|
| `to_json` | `() → dict` |
| `to_json_v2` | `() → list` |
| `to_split_json` | `() → list` |
| `to_json_self_only` | `() → dict` |

### Scope 相关

| 方法 | 签名 |
|------|------|
| `get_scope_name_from_group_key` | `@classmethod (group_key, metric_group_dimensions?) → str` |
| `is_default_scope_info` | `(scope_name) → tuple[bool, str]` |

## EventGroup 方法签名

**类**：`EventGroup(CustomGroupBase)` | 文件：[event.py:30-260](file://bk-monitor-base/src/bk_monitor_base/metadata/models/custom_report/event.py#L30-L260)

| 方法 | 签名 |
|------|------|
| `create_event_group` | `@classmethod (bk_data_id, bk_biz_id, event_group_name, label, operator, bk_tenant_id, event_info_list?, table_id?, data_label?) → EventGroup` |
| `modify_event_group` | `(operator, event_group_name?, label?, is_enable?, event_info_list?, data_label?) → bool` |
| `delete_event_group` | `(operator) → bool` |
| `update_event_dimensions_from_es` | `(client?) → bool` |
| `get_event_info_list` | `(limit?) → list` |
| `to_json` | `(event_infos_limit?) → dict` |
| `make_table_id` | `@staticmethod (bk_biz_id, bk_data_id, bk_tenant_id, table_name?) → str` |
| `update_metrics` | `(metric_info) → Any` |
| `remove_metrics` | `() → None` |
| `consul_path` | `@property → str` |
| `result_table_option` | `@property → dict` |

## Event 方法签名

**类**：`Event(models.Model)` | 文件：[event.py:280-410](file://bk-monitor-base/src/bk_monitor_base/metadata/models/custom_report/event.py#L280-L410)

| 方法 | 签名 |
|------|------|
| `modify_event_list` | `@classmethod (event_group_id, event_info_list) → bool` |
| `event_group` | `@property → EventGroup` |
| `to_json` | `() → dict` |

## LogGroup 方法签名

**类**：`LogGroup(CustomGroupBase)` | 文件：[log.py:20-153](file://bk-monitor-base/src/bk_monitor_base/metadata/models/custom_report/log.py#L20-L153)

| 方法 | 签名 |
|------|------|
| `create_log_group` | `@classmethod (bk_data_id, bk_biz_id, log_group_name, label, operator, bk_tenant_id, table_id?, max_rate?) → LogGroup` |
| `modify_log_group` | `(operator, label?, is_enable?, max_rate?) → bool` |
| `delete_log_group` | `(operator) → bool` |
| `get_bk_data_token` | `() → str` |
| `to_json` | `(with_token?) → dict` |
| `make_table_id` | `@staticmethod (bk_biz_id, bk_data_id, bk_tenant_id, table_name?) → str` |

## RecordRule 方法签名

**类**：`RecordRule(BaseModelWithTime)` | 文件：[rules.py:39-162](file://bk-monitor-base/src/bk_monitor_base/metadata/models/record_rule/rules.py#L39-L162)

| 方法 | 签名 |
|------|------|
| `transform_bk_sql_and_metrics` | `@classmethod (rule_config: str) → dict[str, Any]` |
| `get_src_table_ids` | `@classmethod (space_type: str, space_id: str, metrics: list[str]\|set[str]) → list[str]` |
| `get_dst_table_id` | `@classmethod (table_id: str) → str` |

## ResultTableFlow 方法签名

**类**：`ResultTableFlow(BaseModelWithTime)` | 文件：[rules.py:164-395](file://bk-monitor-base/src/bk_monitor_base/metadata/models/record_rule/rules.py#L164-L395)

| 方法 | 签名 |
|------|------|
| `compose_flow_name` | `@classmethod (table_id: str) → str` |
| `compose_source_node` | `@classmethod (vm_table_ids: list) → list[dict[str, Any]]` |
| `compose_process_node` | `@classmethod (bk_tenant_id: str, table_id: str, vm_table_ids: list[str], waiting_time?: int) → dict[str, Any]` |
| `compose_vm_storage` | `@classmethod (bk_tenant_id: str, table_id: str, process_id: int, expires?: int, schemaless?: bool) → dict[str, Any]` |
| `create_flow` | `@classmethod (bk_tenant_id: str, table_id: str, waiting_time?: int, expires?: int, schemaless?: bool) → bool` |
| `start_flow` | `@classmethod (bk_tenant_id: str, flow_id: int, consuming_mode?: str) → bool` |
| `stop_flow` | `@classmethod (bk_tenant_id: str, flow_id: int) → bool` |
| `delete_flow` | `@classmethod (bk_tenant_id: str, flow_id: int) → bool` |

## 结论

自定义上报子专家共暴露 50+ 个公开方法，覆盖三种自定义上报类型（时序/事件/日志）和记录规则四大功能域。方法签名详见各节，完整契约见 [C1-能力契约.md](../C1-能力契约.md)。
