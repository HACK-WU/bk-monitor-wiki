# 06-测试：基础平台与网关专家

**本文引用的文件**
- [api/](file://bkmonitor/api/)

## 目录

1. [简介](#简介)
2. [测试位置](#测试位置)
3. [测试可执行性](#测试可执行性)
4. [测试缺口](#测试缺口)

## 简介

本专家对应 `api/{common,bk_login,bk_paas,bk_plugin,iam,bk_apigateway,docs}/` 目录。

章节来源：[api/](file://bkmonitor/api/)

## 测试位置

- **该模块无测试**：`api/` 目录下无 `tests/` 目录（`search_file` 仅 77 个 py，无测试文件）。

## 测试可执行性

- 不适用（无测试可执行）。

## 测试缺口

- 外部 API 封装层无单测：`BkUserApiResource` 双模式切换、`BkPluginSystemResource` 凭证替换、`CommonBaseResource` 动态 URL 渲染均无自动化验证。
- 这些封装是调用方（metadata 空间同步、kernel_api 认证、fta_web 插件动作）的关键依赖，补测价值高（可 mock 远端，验证请求 URL/头/异常包装）。

**出处行**：生成日期 2026-08-07，git commit：未提交（工作区）
