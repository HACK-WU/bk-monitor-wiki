# 06-测试：数据平台专家

**本文引用的文件**
- [api/](file://bkmonitor/api/)

## 目录

1. [简介](#简介)
2. [测试位置](#测试位置)
3. [测试可执行性](#测试可执行性)
4. [测试缺口](#测试缺口)

## 简介

本专家对应 `api/{bkdata,metadata,unify_query,log_search,aiops_sdk}/` 目录。

章节来源：[api/](file://bkmonitor/api/)

## 测试位置

- **该模块无测试**：`api/` 目录下无 `tests/`。

## 测试可执行性

- 不适用（无测试可执行）。

## 测试缺口

- 无单测覆盖。关键缺口：
  - `QueryDataResource` 双鉴权（token/user 分支）无测试。
  - `UseSaaSAuthInfoMixin` 认证注入（full_request_data/get_headers）无测试。
  - `UnifyQueryAPIResource` 空间路由（`UNIFY_QUERY_ROUTING_RULES`）无测试。
  - `log_search` 的 index_set pop/format 路径无测试。
- 补测建议：mock 远端（BK-BASE/unify-query/bk-log-search），验证请求 URL/头/认证分支。

**出处行**：生成日期 2026-08-07，git commit：未提交（工作区）
