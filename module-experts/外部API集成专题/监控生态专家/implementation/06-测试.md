# 06-测试：监控生态专家

**本文引用的文件**
- [api/](file://bkmonitor/api/)

## 目录

1. [简介](#简介)
2. [测试位置](#测试位置)
3. [测试可执行性](#测试可执行性)
4. [测试缺口](#测试缺口)

## 简介

本专家对应 `api/{monitor,grafana,apm_api,rum_api,aidev,bmw}/` 目录。

章节来源：[api/](file://bkmonitor/api/)

## 测试位置

- **该模块无测试**：`api/` 目录下无 `tests/`。

## 测试可执行性

- 不适用（无测试可执行）。

## 测试缺口

- 无单测覆盖。关键缺口：
  - `GrafanaApiResource` 直连（URL 拼接/请求头注入）无测试。
  - `DashboardExporter` 模板化导出（`$DS_xxx` 替换、`make_exportable`）无测试——as_code/导出导入依赖，价值高。
  - `aidev` 流式响应（`handle_stream_response`）无测试。
- 补测建议：mock Grafana/AIDEV 远端，验证请求 URL/头/响应转换；`DashboardExporter` 可用 fixture 仪表盘做纯单测。

**出处行**：生成日期 2026-08-07，git commit：未提交（工作区）
