# 实现：性能场景专家 / monitor_web.performance

**本文引用的文件**
- [resources.py](file://bkmonitor/packages/monitor_web/performance/resources.py)
- [views.py](file://bkmonitor/packages/monitor_web/performance/views.py)
- [urls.py](file://bkmonitor/packages/monitor_web/performance/urls.py)

## 目录
1. [简介](#简介)
2. [核心流程](#核心流程)
3. [设计模式](#设计模式)
4. [关键类与函数](#关键类与函数)
5. [热点路径](#热点路径)
6. [技术债](#技术债)
7. [结论](#结论)

## 简介
实现层围绕两条主线：一是「主机列表/指标的多源并行聚合」，由 `HostPerformanceResource` 和 `SearchHostMetricResource` 通过 `ThreadPool` 4 路并发拉取 Agent 状态、性能指标、进程、告警；二是「拓扑/详情的同步聚合」，由 `HostTopoNodeDetailResource` 和 `HostPerformanceDetailResource` 串行调用 CMDB API 组装完整信息。
章节来源：[resources.py](file://bkmonitor/packages/monitor_web/performance/resources.py#L43-L472)

## 核心流程
```mermaid
flowchart TD
  A[perform_request 入参] --> B[api.cmdb.get_host_by_topo_node 拉全量主机]
  B --> C[api.cmdb.get_topo_tree 拓扑链]
  C --> D[构造 host_dict 默认值\n指标=None/状态=UNKNOWN/进程=[]]
  D --> E[ThreadPool 4 路并行]
  E --> E1[get_agent_status\n→ data.status]
  E --> E2[get_performance_data\n→ data.cpu_usage/mem_usage/...]
  E --> E3[get_process_status\n→ data.component]
  E --> E4[get_alarm_count\n→ data.alarm_count]
  E1 & E2 & E3 & E4 --> F[pool.join 汇合]
  F --> G[返回 hosts + update_time]
```
`SearchHostMetricResource` 流程类似，区别：按 `bk_host_ids` 精确查 `api.cmdb.get_host_by_id`，无缓存，`get_alarm_count` 有 `try/except` 降级，支持 `start_time`/`end_time` 时间范围。
图表来源：[resources.py](file://bkmonitor/packages/monitor_web/performance/resources.py#L95-L127) · [resources.py](file://bkmonitor/packages/monitor_web/performance/resources.py#L445-L472)

## 设计模式
| 模式 | 位置 | 用途 |
|---|---|---|
| 并发聚合（ThreadPool） | [resources.py](file://bkmonitor/packages/monitor_web/performance/resources.py#L104-L112) | 4 路并行拉取异构数据源，缩短整体响应时间 |
| 缓存代理（CacheResource） | [resources.py](file://bkmonitor/packages/monitor_web/performance/resources.py#L43-L47) | `HostPerformanceResource` 继承 `CacheResource`，缓存整份列表 |
| 静态方法复用 | [resources.py](file://bkmonitor/packages/monitor_web/performance/resources.py#L286-L310) | `get_module_info` 被 `HostPerformanceResource` 跨类调用 |
| 聚合器（Data Aggregator） | [resources.py](file://bkmonitor/packages/monitor_web/performance/resources.py#L43-L127) | `host_dict` 作为聚合容器，各 fetch 方法原地填充 |
| 降级容错 | [resources.py](file://bkmonitor/packages/monitor_web/performance/resources.py#L415-L425) | `SearchHostMetricResource.get_alarm_count` 捕获异常降级为空列表 |

章节来源：[resources.py](file://bkmonitor/packages/monitor_web/performance/resources.py#L43-L472)

## 关键类与函数
| 名称 | 路径 | 职责 |
|---|---|---|
| `HostPerformanceResource` | [resources.py](file://bkmonitor/packages/monitor_web/performance/resources.py#L43-L127) | 主机列表聚合（缓存），`CacheResource` 子类 |
| `HostPerformanceResource.get_process_status()` | [resources.py](file://bkmonitor/packages/monitor_web/performance/resources.py#L55-L76) | 静态方法，拉进程信息填充 `component` |
| `HostPerformanceResource.get_alarm_count()` | [resources.py](file://bkmonitor/packages/monitor_web/performance/resources.py#L78-L91) | 静态方法，拉告警计数填充 `alarm_count`（无降级） |
| `HostPerformanceDetailResource` | [resources.py](file://bkmonitor/packages/monitor_web/performance/resources.py#L130-L178) | 单主机详情，主机不存在抛 `CustomException` |
| `HostTopoNodeDetailResource` | [resources.py](file://bkmonitor/packages/monitor_web/performance/resources.py#L181-L245) | 拓扑节点详情聚合 |
| `HostTopoNodeDetailResource.get_alarm_count()` | [resources.py](file://bkmonitor/packages/monitor_web/performance/resources.py#L186-L205) | 静态方法，统计主机关联告警总数 |
| `TopoNodeProcessStatusResource` | [resources.py](file://bkmonitor/packages/monitor_web/performance/resources.py#L248-L277) | 拓扑节点下进程名列表（status 置灰） |
| `SearchHostInfoResource` | [resources.py](file://bkmonitor/packages/monitor_web/performance/resources.py#L280-L340) | 主机基础信息 + 模块拓扑链 |
| `SearchHostInfoResource.get_module_info()` | [resources.py](file://bkmonitor/packages/monitor_web/performance/resources.py#L286-L310) | 静态方法，构造模块拓扑链（被跨类复用） |
| `SearchHostMetricResource` | [resources.py](file://bkmonitor/packages/monitor_web/performance/resources.py#L343-L472) | 指定主机实时指标（无缓存，支持时间范围） |
| `SearchHostMetricResource.get_agent_status()` | [resources.py](file://bkmonitor/packages/monitor_web/performance/resources.py#L369-L381) | 静态方法，拉 Agent 状态 |
| `SearchHostMetricResource.get_performance_data()` | [resources.py](file://bkmonitor/packages/monitor_web/performance/resources.py#L383-L396) | 静态方法，拉 TSDB 性能指标 |
| `SearchHostMetricResource.get_alarm_count()` | [resources.py](file://bkmonitor/packages/monitor_web/performance/resources.py#L415-L432) | 静态方法，拉告警计数（有 `try/except` 降级） |
| `PermissionMixin` | [views.py](file://bkmonitor/packages/monitor_web/performance/views.py#L25-L31) | 统一 `VIEW_HOST` 权限 |

章节来源：[resources.py](file://bkmonitor/packages/monitor_web/performance/resources.py#L43-L472) · [views.py](file://bkmonitor/packages/monitor_web/performance/views.py#L25-L31)

## 热点路径
- **`HostPerformanceResource.perform_request`**：拉全量主机 + 拓扑树 + 4 路并行聚合，是主机列表页的核心热点。`HOST` 缓存缓解了大部分请求压力，但缓存失效后的首次请求仍会全量拉取。
- **`get_host_by_topo_node` + `get_topo_tree`**：两个 CMDB 调用串行执行，拓扑树构建有一定开销（`convert_to_topo_link` 遍历全树）。
- **`SearchHostMetricResource`**：无缓存，每次请求都 4 路并发查 TSDB；`get_alarm_count` 的异常降级意味着告警数据可能静默缺失。
- **`HostTopoNodeDetailResource.get_alarm_count`**：遍历所有主机的告警计数再求和，主机数多时是 O(N×级别) 累加。
章节来源：[resources.py](file://bkmonitor/packages/monitor_web/performance/resources.py#L95-L127) · [resources.py](file://bkmonitor/packages/monitor_web/performance/resources.py#L445-L472)

## 技术债
- **`get_process_status` 代码重复**：`HostPerformanceResource` 和 `SearchHostMetricResource` 各有一份 `get_process_status` 静态方法，逻辑近乎相同（字段略有差异），未抽取公共方法。
- **`HostPerformanceResource.get_alarm_count` 无降级**：与 `SearchHostMetricResource.get_alarm_count` 不同，`HostPerformanceResource` 版本无 `try/except`，异常会中断整个 `ThreadPool.join`。
- **`ThreadPool` 无超时熔断**：4 路并行任一路慢（如 TSDB 超时）会拖慢整体响应，无独立超时控制。
- ~~**`SearchHostInfoViewSet` 权限缺失**~~（2026-08-31 核对：**已修复，条目过期**）：2026-08-12 的 commit 24f090ed5e 已补齐，当前 6 个 ViewSet 全部继承 `PermissionMixin`；分享场景另有 `SearchHostMetricResource.validate_scope_host_ids` 做 `bk_host_ids` 越权校验。详见 `C5-关键决策.md` 决策 10。
- **`TopoNodeProcessStatusResource` 硬编码置灰**：所有进程 `status` 直接置 `AGENT_STATUS.UNKNOWN`，`ports`/`protocol` 留空，不查实时状态。
- **`HostTopoNodeDetailResource` 串行调用**：拓扑树 + 主机列表 + 策略数 + 负责人 4 步串行，无并行优化。
- **`node` 为 None 的潜在 AttributeError**：`HostTopoNodeDetailResource.perform_request` 中 `node = topo_nodes.get(f"{bk_obj_id}_{bk_inst_id}")` 可能返回 None，非 module 类型时 `len(node.child)` 会抛 `AttributeError`。
章节来源：[resources.py](file://bkmonitor/packages/monitor_web/performance/resources.py#L55-L91) · [resources.py](file://bkmonitor/packages/monitor_web/performance/resources.py#L415-L432) · [views.py](file://bkmonitor/packages/monitor_web/performance/views.py#L54-L58) · [resources.py](file://bkmonitor/packages/monitor_web/performance/resources.py#L248-L277)

## 结论
实现以「ThreadPool 并行聚合 + CacheResource 缓存」为核心模式，适合多源数据聚合场景。扩展返回字段的标准路径：在 `host_dict` 加默认值 → 新增/修改 fetch 静态方法 → 在 `ThreadPool` 注册。主要风险在无超时的并行调用和 `HostPerformanceResource.get_alarm_count` 的无降级设计。`get_process_status` 代码重复是主要可优化点。
