# B01: S-01 数据模型设计 + Redis Token 工具 + 四态定义

> 前置批次: 无（拓扑根）
> 设计锚点: `S01_数据模型设计_DESIGN.md` §4b, §4c, §4d

---

## 文件改动

- [ ] 创建 `bkmonitor/bkmonitor/models/tapd.py`（新建文件） § S01 §4b.1
- [ ] 修改 `bkmonitor/bkmonitor/models/__init__.py`：追加 `from .tapd import *` § S01 §4b.1
- [ ] 创建 Migration：`python manage.py makemigrations` § S01 §10
- [ ] 确认 `bkmonitor/utils/cipher.py` 中 `AESCipher` 可用（不传 IV 模式） § S01 §4c

---

## 步骤 1：TapdWorkspaceBinding 模型

- [ ] `1.1` 创建 `TapdWorkspaceBinding(AbstractRecordModel)` § S01 §4b.1
  - `bk_tenant_id`: `CharField(max_length=64, default=DEFAULT_TENANT_ID)` § S01 §4b.1
  - `space_uid`: `CharField(max_length=128)` § S01 §4b.1
  - `bk_biz_id`: `IntegerField(db_index=True)` § S01 §4b.1
  - `tapd_workspace_id`: `CharField(max_length=64)` § S01 §4b.1
  - `tapd_workspace_name`: `CharField(max_length=255)` § S01 §4b.1
  - 继承自动字段：`id`, `is_enabled`, `is_deleted`, `create_user`, `create_time`, `update_user`, `update_time` § S01 §4b.1

- [ ] `1.2` `Meta` 配置 § S01 §4b.1
  - `db_table = "tapd_workspace_binding"`（裸 snake_case，**不加 `bkmonitor_` 前缀**）§ S01 §4b.1 / DESIGN.md §5
  - `unique_together = [("bk_tenant_id", "space_uid", "tapd_workspace_id")]` § S01 §4b.1
  - `verbose_name = "TAPD项目关联"`
- [ ] `1.3` `__all__ = ["TapdWorkspaceBinding"]` § S01 §4b.1
- [ ] `1.4` `TapdWorkspaceBindingSerializer`（ModelSerializer，内部校验用） § S01 §4b.2
  - `fields = ["id", "bk_tenant_id", "space_uid", "bk_biz_id", "tapd_workspace_id", "tapd_workspace_name", "create_user", "create_time", "update_time"]`
  - `read_only_fields = ["id", "create_user", "create_time", "update_time"]`

## 步骤 2：Redis Token 存储工具函数

- [ ] `2.1` `save_tapd_token(bk_tenant_id: str, username: str, token_data: dict)` § S01 §4c
  - `cipher = AESCipher(key=settings.SECRET_KEY)`（**不传 IV**）§ S01 §4c
  - `encrypted_token = cipher.encrypt(token_data["access_token"])` § S01 §4c
  - value = `{"access_token": encrypted_token, "tapd_user_id": token_data["tapd_user_id"], "token_type": token_data.get("token_type", "Bearer"), "expires_at": token_data["expires_at"]}` § S01 §4c
  - `ttl = token_data["expires_at"] - int(time.time())`
  - 若 `ttl <= 0`：不写入（视作过期） § S01 §4c
  - `redis_client.setex(f"tapd_uat:{bk_tenant_id}:{username}", ttl, json.dumps(value))` § S01 §4c

- [ ] `2.2` `get_tapd_token(bk_tenant_id: str, username: str) -> Optional[dict]` § S01 §4c
  - `raw = redis_client.get(f"tapd_uat:{bk_tenant_id}:{username}")`
  - 若 `raw is None`：返回 `None`
  - `data = json.loads(raw)`
  - `cipher = AESCipher(key=settings.SECRET_KEY)`（**不传 IV**）§ S01 §4c
  - `data["access_token"] = cipher.decrypt(data["access_token"])`
  - 返回 `data`

## 步骤 3：is_bound 四态定义

- [ ] `3.1` 定义四态枚举/常量 § S01 §4d
  - `BOUND = "bound"`：本地 binding 存在 + TAPD 已授权
  - `STALE = "stale"`：本地 binding 存在 + TAPD 未授权
  - `IMPORTABLE = "importable"`：本地 binding 不存在 + TAPD 已授权
  - `UNBOUND = "unbound"`：本地 binding 不存在 + TAPD 未授权

- [ ] `3.2` 定义计算函数 `compute_bound_status(workspace_id, local_bindings_set, granted_set) -> str` § S01 §4d
  - 输入：`workspace_id`(str), `local_bindings_set`(Set[str]), `granted_set`(Set[str])
  - 逻辑：
    - `local=True & granted=True` → `"bound"`
    - `local=True & granted=False` → `"stale"`
    - `local=False & granted=True` → `"importable"`
    - `local=False & granted=False` → `"unbound"`

## 步骤 4：Migration

- [ ] `4.1` 运行 `python manage.py makemigrations bkmonitor` 生成 Migration 文件 § S01 §10
- [ ] `4.2` Migration 文件检查：字段类型、索引、唯一约束、`bk_tenant_id` 默认值 § S01 §4b.1
- [ ] `4.3` Migration 运行：`python manage.py migrate`（如环境允许）

## 步骤 5：回归验证

- [ ] `5.1` `TapdWorkspaceBinding.objects.create(...)` 成功
- [ ] `5.2` 唯一约束冲突时 `IntegrityError`（通过 upsert ON DUPLICATE KEY UPDATE 解决）
- [ ] `5.3` `save_tapd_token` → `get_tapd_token` 回环测试：原始 token == 解密后 token
- [ ] `5.4` Redis TTL 正确性：key 在 expires_at 后自动删除
- [ ] `5.5` AESCipher 不传 IV 时：相同明文两次加密结果不同（随机 IV）
