# TAPD 授权与建单 — 数据模型质疑报告

> 审查对象：`data-flow-model.md`
> 审查时间：2026-06-15
> 审查策略：新增功能类
> 
> ⚠️ **注意**：本挑战报告 v4 段落（"新增异步刷新 Token"）已被 2026-06-17 设计评审否决——S-07 整套删除，Token 改用 Redis+AESCipher+TTL，不再持久化至 DB。本报告早前三轮质疑（v1~v3）的结论仍具参考价值。

---

## 📋 需求确认

### 需求内容
- **功能目标**：TAPD 授权与建单的数据模型设计，覆盖项目关联和用户 Token 存储
- **功能输入**：TAPD OAuth 回调参数、业务 ID、用户 Token
- **功能输出**：关联关系持久化、Token 持久化、查询结果
- **触发条件**：用户授权操作、TAPD 回调

### 需求来源
- **来源类型**：PRD 文档（requirement.md）
- **可靠性**：✅ 可靠

### 隐性需求
- **用户期望**：授权流程顺畅、错误有明确提示
- **行业惯例**：OAuth 标准流程、Token 安全存储
- **系统约束**：Django ORM、MySQL、与现有 bkmonitor 架构兼容
- **最佳实践**：Token 加密存储、审计日志、操作日志

---

## 🔍 需求覆盖验证

### 主流程验证
- **流程完整性**：✅ 完整 — 4 条数据流覆盖需求中全部接口（B-01~B-06）
- **流程正确性**：✅ 正确
- **流程顺序**：✅ 正确

### 分支流程验证
- **分支完整性**：⚠️ 部分遗漏
- **遗漏点**：需求 B-01 提到「与关联表比对标记已关联状态」（S-03），数据流 4.3 仅描述调 TAPD API 获取项目列表，**未体现与 TAPD_WORKSPACE_BINDING 表比对标记已关联状态的读取路径**

### 异常流程验证
- **输入异常**：⚠️ 部分处理 — 数据流 4.3 提到 token 过期返回错误码，但未描述 TAPD API 调用失败的处理
- **业务异常**：⚠️ 部分处理 — 未描述 B-03 回调中业务 ID 无效的处理路径
- **系统异常**：❌ 未处理 — 未描述数据库写入失败、TAPD API 不可用等系统异常的数据流

### 隐性需求验证
- **用户期望**：⚠️ 部分满足 — 未涉及授权状态判断的数据流
- **行业惯例**：⚠️ 部分符合 — `access_token` 明文存储，未提及加密
- **最佳实践**：⚠️ 部分遵循 — 未涉及审计日志

### 覆盖总结
- **总体评价**：⚠️ 基本覆盖
- **关键遗漏**：异常流程和数据安全未充分描述
- **风险等级**：中

---

## ⚠️ 边界条件验证

### 输入边界
- **字符串边界**：⚠️ `tapd_workspace_name`、`access_token` 未定义长度限制

### 输出边界
- **大量结果**：⚠️ 用户有权限的 TAPD 项目可能很多，未考虑分页

### 状态边界
- **异常状态**：⚠️ Token 过期但关联关系仍存在的混合状态未明确描述

### 资源边界
- **并发边界**：⚠️ 唯一约束兜底了并发写入，但未描述并发 upsert 的具体策略

### 边界总结
- **总体评价**：⚠️ 基本覆盖
- **风险等级**：低

---

## 🔗 兼容性验证

- **总体评价**：✅ 完全兼容 — 全新表和接口，不影响现有系统
- **风险等级**：低

---

## ⚠️ 风险验证

### 安全风险
- **数据泄露**：⚠️ `access_token` 明文存储，未提及加密或脱敏
- **权限问题**：⚠️ 未描述接口层面的鉴权方案

### 风险总结
- **总体风险**：🟡 中风险
- **关键风险**：Token 安全存储、接口鉴权方案缺失

---

## 🎨 体验质量质疑

### 正向体验
- **操作流畅度**：⚠️ 冷启动路径较长（需求层面已接受）
- **反馈及时性**：⚠️ B-03 回调后前端如何感知关联成功未明确

### 负向体验
- **恢复路径**：⚠️ B-03 回调失败后用户如何恢复未描述

### 体验总结
- **总体评价**：⚠️ 体验一般
- **风险等级**：低

---

## 📊 质疑总结

### 统计
- **总质疑数**：7
- **高风险**：0
- **中风险**：3
- **低风险**：4

### 风险分布
- 🟡 中风险：Token 安全存储未明确、接口鉴权方案缺失、异常流程未覆盖
- 🟢 低风险：字段长度约束、分页策略、回调失败恢复路径、前端状态感知机制

---

## 🎯 行动建议

### 必须处理
1. **明确 `access_token` 存储安全策略** — 加密存储还是依赖数据库层访问控制？设计阶段需确定
2. **明确接口鉴权方案** — B-01~B-06 各接口的鉴权方式需在设计文档中体现

### 建议处理
1. **补充异常流程数据流** — TAPD API 调用失败、业务 ID 无效等异常场景
2. **补充 B-01 项目列表与关联表比对逻辑** — 数据流 4.3 应体现查询 TAPD_WORKSPACE_BINDING 的步骤
3. **定义关键字段长度约束** — `access_token`、`tapd_workspace_name` 等字段

### 可选处理
1. **补充前端状态感知机制** — B-03 回调成功后前端如何实时感知
2. **补充监控指标** — Token 过期率、TAPD API 调用成功率
3. **考虑分页策略** — 用户 TAPD 项目数量较多时的分页方案

---

## 优化复查（v2）

> 复查时间：2026-06-15
> 复查对象：`data-flow-model.md` v2（优化后）

### 已关闭项（7/7）

| # | 原质疑项 | 关闭状态 | 处理方式 |
|---|----------|----------|----------|
| 1 | access_token 安全存储策略 | ✅ 已关闭 | 新增「安全策略」章节，明确 Fernet 加密 + 日志脱敏 |
| 2 | 接口鉴权方案 | ✅ 已关闭 | 新增「接口鉴权方案」章节，B-01~B-06 逐一明确 |
| 3 | 异常流程数据流 | ✅ 已关闭 | 4.1~4.3 各新增异常处理子节 |
| 4 | B-01 比对逻辑 | ✅ 已关闭 | 4.3 新增 TAPD_WORKSPACE_BINDING 读取 + is_bound 标记 |
| 5 | 字段长度约束 | ✅ 已关闭 | ER 图 + 字段清单同步更新 varchar 长度 |
| 6 | 前端状态感知 | ✅ 已关闭 | 4.2 新增重定向 URL 参数方案 |
| 7 | 分页策略 | ✅ 已关闭 | 4.3 标注 page_size=20 |

### 新增观察项（2 个低风险）

| # | 观察项 | 风险 | 建议 |
|---|--------|------|------|
| 1 | B-01 大批量项目比对性能 | 🟢 低 | 实现时按 space_id 预加载已关联集合 |
| 2 | 加密密钥轮换 | 🟢 低 | 设计阶段明确密钥存储方案 |

### 复查结论

- **总质疑数**：2（均为低风险观察项）
- **文档状态**：✅ 可进入 design-craft

---

## 优化复查（v3）

> 复查时间：2026-06-15
> 复查对象：`data-flow-model.md` v3（参考 demo 优化后）
> 触发原因：参考 `tapd_oauth_demo_bak.py` 发现 B-04 流程不清晰、B-01 错误处理不细致

### 优化内容

| # | 优化项 | 优化前 | 优化后 |
|---|--------|--------|--------|
| 1 | B-04 授权URL流程 | 后端302跳转 | 后端返回URL给前端，前端跳转 |
| 2 | B-01 错误处理 | 单一错误（服务不可用） | 区分权限不足/服务异常 |
| 3 | Token 存储方案 | 仅 MySQL | 新增 MySQL+Redis 双层评估 |

### 已关闭项（2/2）

| # | 原观察项 | 关闭状态 | 处理方式 |
|---|----------|----------|----------|
| 1 | B-04 流程不清晰 | ✅ 已关闭 | 明确后端返回URL给前端，补充 OAuth URL 参数 |
| 2 | B-01 错误处理不细致 | ✅ 已关闭 | 区分权限不足/服务异常两种错误 |

### 新增观察项（2 个低风险）

| # | 观察项 | 风险 | 建议 |
|---|--------|------|------|
| 1 | B-04 接口异常时前端需额外处理 | 🟢 低 | 实现时前端需处理 B-04 接口超时/失败场景 |
| 2 | Redis 缓存一致性维护 | 🟢 低 | 二期引入 Redis 时明确一致性策略 |

### 复查结论

- **总质疑数**：2（均为低风险观察项）
- **文档状态**：✅ 可进入 design-craft

---

## 优化复查（v4）

> 复查时间：2026-06-15
> 复查对象：`data-flow-model.md` v4（新增异步刷新 Token）
> 触发原因：用户要求添加异步刷新 token 功能

### 优化内容

| # | 优化项 | 优化前 | 优化后 |
|---|--------|--------|--------|
| 1 | refresh_token 字段 | 一期不存 | 存储，用于异步刷新 |
| 2 | 异步刷新数据流 | 无 | 新增 4.6 异步刷新 Token（用户访问时触发） |
| 3 | 防重复刷新策略 | 无 | 通过 updated_at 判断，间隔 >= 5 分钟 |
| 4 | 刷新失败处理 | 无 | 保留原 token，返回当前 token 仍可使用 |

### 关键设计决策

| 项目 | 方案 | 理由 |
|------|------|------|
| refresh_token 存储 | ✅ 存储 | 异步刷新需要调用 TAPD 刷新接口，必须有 refresh_token |
| 防重复刷新 | 用 updated_at 判断 | 减少字段，简化模型 |
| 刷新失败处理 | 保留原 token | 刷新失败原因多样，保留 token 可用于重试 |
| 刷新频率 | 用户访问时触发 | 利用用户访问时机，避免额外定时任务 |
| 提前刷新量 | 过期前 30 分钟 | 避免用户感知中断 |

### 复查结论

- **总质疑数**：0
- **文档状态**：✅ 可进入 design-craft
