# 场景推演报告：TAPD 授权与建单

> 推演时间：2026-06-22
> 输入文档：
> - requirement.md（需求挖掘报告）
> - data-flow-model.md（数据流图）
> - DESIGN.md（主设计文档）
> - S01~S06 子设计文档
> - design-review.md（评审定稿结论）

---

## 1. 角色清单

| # | 角色 | 类型 | 权限层级 | 职责 | 来源 |
|---|------|------|---------|------|------|
| 1 | 普通用户（值班/处理人/研发） | 用户角色 | 已登录 | 完成用户态 OAuth、查看 TAPD 项目列表、发起项目关联 | requirement.md §三 |
| 2 | TAPD 项目管理员 | 用户角色 | 已登录 | 完成项目应用态授权（选项目→授权给应用） | requirement.md §三 |
| 3 | 非项目管理员（普通协作人员） | 用户角色 | 已登录 | OAuth 授权后复制应用态授权链接转发给管理员 | requirement.md §场景4 |
| 4 | TAPD OAuth 服务 | 外部系统 | — | 用户态/应用态授权、code 换 token、回调通知 | DESIGN.md §2 |
| 5 | TAPD 业务 API | 外部系统 | — | 获取用户项目列表、get_granted_workspaces、get_workspace_info | data-flow-model.md §2 |

> ✅ 角色完整性确认：已覆盖需求文档中全部角色，无遗漏。

---

## 2. 推演矩阵

### 2.1 设计点覆盖矩阵

| 设计点 \ 场景 | 用户态首授 | 用户态 re-auth | 应用态关联（本浏览器） | 应用态关联（转发管理员） | 用户态 OAuth 回调攻击 | Token 过期重进入 | 查看项目列表（冷启动） | 查看项目列表（日常） | 重复关联 | 非管理员复制链接 | B-03 回调签名伪造 | 管理员绑定后前端回业 |
|--------------|-----------|---------------|----------------------|------------------------|---------------------|-----------------|---------------------|-------------------|---------|--------------|----------------|---------------------|
| A1: Token 存储 (Redis+加密+TTL) | ✅ | ✅ | — | — | ✅ | ✅ | ✅ | — | — | — | — | — |
| A2: signed_state 验签不验 session | — | — | ✅ | ✅ | — | — | — | — | — | ✅ | ✅ | — |
| A3: app 级 Basic Auth 取 workspace 信息 | — | — | ✅ | ✅ | — | — | — | — | — | ✅ | — | — |
| B1: 唯一约束 (bk_tenant_id, space_uid, tapd_workspace_id) | — | — | ✅ | ✅ | — | — | — | — | ✅ | — | — | — |
| 四态: bound/stale/importable/unbound | — | — | ✅ | ✅ | — | — | ✅ | ✅ | ✅ | — | — | ✅ |
| 用户态 OAuth 回调 state/session 验证 | ✅ | ✅ | — | — | ✅ | — | — | — | — | — | — | — |
| 双重鉴权 (TAPD_REQUIRED + IAMPermission) | ✅ | ✅ | — | — | — | ✅ | ✅ | ✅ | — | — | — | — |
| create_user 审计归属 (signed_state.initiator) | — | — | ✅ | ✅ | — | — | — | — | — | ✅ | — | — |
| B-03 幂等 (upsert binding) | — | — | ✅ | ✅ | — | — | — | — | ✅ | — | — | — |
| Signed_state 时效性 (expire_at 验证) | — | — | ✅ | ✅ | — | — | — | — | — | ✅ | ✅ | — |
| Token 过期触发重授权 (PermissionDenied → 403) | ✅ | — | — | — | — | ✅ | ✅ | — | — | — | — | — |

> 说明：标记为 `—` 的设计点在该场景中不适用。每个设计点至少有一个关联场景，覆盖完整。

### 2.2 角色场景推演矩阵

| 场景 \ 角色 | 普通用户 | TAPD 项目管理员 | 非项目管理员 | 外部系统(TAPD) |
|-------------|---------|----------------|-------------|---------------|
| 用户态首授（冷启动） | ✅ | — | — | ✅ |
| 用户态 re-auth（Token 过期重进入） | ✅ | — | — | ✅ |
| 应用态关联（本浏览器） | ✅ | ✅ | — | ✅ |
| 应用态关联（转发管理员跨浏览器） | ✅ | — | ✅ | ✅ |
| 应用态重复关联（幂等） | — | ✅ | — | — |
| 查看项目列表（冷启动 B-01） | ✅ | — | — | ✅ |
| 查看项目列表（日常 B-07） | ✅ | — | — | ✅ |
| 非管理员复制链接给管理员 | — | — | ✅ | — |
| 用户态 OAuth CSRF 攻击（异常） | — | — | — | ✅ |
| B-03 回调签名伪造（异常） | — | — | — | ✅ |
| 管理员绑定后前端回业务（协作） | ✅ | ✅ | — | ✅ |

---

## 3. 场景推演详情

### 3.1 🎬 场景一：冷启动首授（Happy Path）

【执行者】普通用户
【场景描述】用户首次使用 TAPD 关联功能，业务空间尚无已关联项目

#### 数据走向验证

| 步骤 | 操作 | 数据流向 | 验证结果 | 问题 |
|------|------|----------|---------|------|
| 1 | FE: 打开 TAPD 项目选择弹窗 | 用户 → 前端 | ✅ 通过 | — |
| 2 | FE → BE: 请求 B-01 查询项目列表 | 请求体: bk_biz_id | ✅ 通过 | — |
| 3 | BE: `TAPD_REQUIRED` 检查 Redis `tapd_uat:{tenant}:{user}` | Redis 查询 | ✅ 通过（key 不存在） | — |
| 4 | `TAPD_REQUIRED` → `generate_auth_url(bk_biz_id)` 生成 auth_url 写入 Session | Session 写入 state | ✅ 通过 | — |
| 5 | BE → FE: `PermissionDenied(403, detail={"auth_url": ...})` | 响应体 | ✅ 通过 | — |
| 6 | FE: 拦截 403, 跳转 `auth_url` | 浏览器 302 | ✅ 通过 | — |
| 7 | TAPD: 展示授权页 → 用户确认 → 回调 B-05 (`code, state`) | 用户 → TAPD | ✅ 通过 | — |
| 8 | BE: Session state 验证成功 → 用 code 换 token | TAPD API → access_token | ✅ 通过 | — |
| 9 | BE: `AESCipher` 加密 → Redis `setex tapd_uat:{tenant}:{user}` | Redis 写入（TTL≈7200s） | ✅ 通过 | — |
| 10 | BE → FE: 302 重定向到前端页面 `?auth=success` | 浏览器跳转 | ✅ 通过 | — |
| 11 | FE: 重新请求 B-01 列表，拿到带四态的项目列表 | B-01 返回: items + install_url + method | ✅ 通过 | — |

#### 关键设计点验证

| # | 设计点 | 验证问题 | 验证结果 | 问题 | 置信度 |
|---|--------|---------|---------|------|--------|
| 1 | A1: Token 存储 | Redis+加密+TTL 是否有效防止 Token 泄露？ | ✅ 通过 | AESCipher 随机 IV，TTL 对齐 token 过期时间 | — |
| 2 | 用户态 OAuth state | Session state 在同浏览器是否有效防 CSRF？ | ✅ 通过 | nonce 随机，验证后删除防重放 | — |
| 3 | 双重鉴权 | `TAPD_REQUIRED + IAMPermission` 拦截顺序是否正确？ | ✅ 通过 | 先查 token 再返回 auth_url，IAM 在之后 | — |
| 4 | 被动触发模式 | 前端不主动生成 URL，由业务接口触发是否合理？ | ✅ 通过 | 减少接口调用，流程紧凑 | — |

#### 推演结论
- 数据走向：11 项通过 / 0 项存疑 / 0 项失败
- 关键设计：4 项通过 / 0 项存疑 / 0 项失败

---

### 3.2 🎬 场景二：用户态 re-auth（Token 过期重进入，Happy Path）

【执行者】普通用户
【场景描述】用户 Token Redis TTL 过期，再次使用 TAPD 功能

#### 数据走向验证

| 步骤 | 操作 | 数据流向 | 验证结果 | 问题 |
|------|------|----------|---------|------|
| 1 | FE: 请求业务接口（如 B-07 日常下拉） | 用户 → 前端 | ✅ 通过 | — |
| 2 | BE: `TAPD_REQUIRED` 检查 Redis `tapd_uat:{tenant}:{user}` | Redis 查询 | ✅ 通过（key 不存在：TTL 到期自动淘汰） | — |
| 3 | 同场景一 4~10，走完 OAuth 换 token → 写 Redis → 重定向 → 重新请求 | — | ✅ 通过 | — |

#### 关键设计点验证

| # | 设计点 | 验证问题 | 验证结果 | 问题 | 置信度 |
|---|--------|---------|---------|------|--------|
| 1 | 无刷新机制 | 过期重走 OAuth 是否用户体验可接受？ | ✅ 通过 | 一次廉价重定向，2h TTL 足够长 | — |
| 2 | Redis TTL 淘汰 | TTL 到期后 key 自动删除，不存脏数据 | ✅ 通过 | — |

#### 推演结论
- 数据走向：3 项通过 / 0 项存疑 / 0 项失败
- 关键设计：2 项通过 / 0 项存疑 / 0 项失败

---

### 3.3 🎬 场景三：应用态关联（本浏览器，Happy Path）

【执行者】普通用户（兼任项目管理员）
【场景描述】用户已拿到 B-01 返回的项目列表（含四态），点击「去关联」

#### 数据走向验证

| 步骤 | 操作 | 数据流向 | 验证结果 | 问题 |
|------|------|----------|---------|------|
| 1 | FE: B-01 返回 `install_url`（cb 已含 signed_state） | 后端 → 前端 | ✅ 通过 | — |
| 2 | FE: 将 `{workspace_id}` 替换为实际 ID → 浏览器打开 install_url | 浏览器 → TAPD | ✅ 通过 | — |
| 3 | TAPD: 展示项目选择→安装应用→用户确认→回调 B-03 | TAPD → 后端 | ✅ 通过 | — |
| 4 | BE: B-03 从 query 提取 `state` + `sig` | 回调请求 | ✅ 通过 | — |
| 5 | BE: HMAC 验签 `sig == HMAC(state, SECRET_KEY)` | 本地验签 | ✅ 通过 | — |
| 6 | BE: 解出 payload，获取 `bk_tenant_id, space_uid, bk_biz_id, initiator` | 解码 state | ✅ 通过 | — |
| 7 | BE: 从 resource["workspace_id"] 取 workspace_id | 回调 body | ✅ 通过 | — |
| 8 | BE: app 级 Basic Auth 调 `get_workspace_info` 取 workspace_name | TAPD API | ✅ 通过 | — |
| 9 | BE: upsert `TAPD_WORKSPACE_BINDING`，`create_user = initiator` | MySQL INSERT/UPDATE | ✅ 通过 | — |
| 10 | BE: 302 重定向到前端 `?tapd_bind=success` | 浏览器跳转 | ✅ 通过 | — |
| 11 | FE: 检测 `tapd_bind=success` 参数 → 刷新 B-07 列表 → `is_bound=bound` | 前端状态更新 | ✅ 通过 | — |

#### 关键设计点验证

| # | 设计点 | 验证问题 | 验证结果 | 问题 | 置信度 |
|---|--------|---------|---------|------|--------|
| 1 | A2: signed_state 验签 | HMAC 验签是否有效防伪造回调？ | ✅ 通过 | 不知道 SECRET_KEY 无法伪造签名 | — |
| 2 | A3: app 级 Basic Auth | 不依赖用户态 token 是否合理？ | ✅ 通过 | 与现网 `TapdAPIResource` 对齐 | — |
| 3 | B1: 唯一约束 | upsert 是否保证幂等？ | ✅ 通过 | `(bk_tenant_id, space_uid, wd_id)` 唯一约束 | — |
| 4 | 四态: bound | 成功关联后 B-07 查询是否标记 bound？ | ✅ 通过 | 本地 binding ✓ + TAPD 授权 ✓ | — |
| 5 | create_user 审计 | request.user 填管理员是否被覆盖？ | ✅ 通过 | `signed_state.initiator` 显式覆盖 | — |

#### 推演结论
- 数据走向：11 项通过 / 0 项存疑 / 0 项失败
- 关键设计：5 项通过 / 0 项存疑 / 0 项失败

---

### 3.4 🎬 场景四：应用态关联（转发管理员，跨浏览器 / Collaboration Path）

【执行者】非项目管理员 → TAPD 项目管理员
【场景描述】普通用户（非管理员）B-01 列表中看到 unbound 项目，复制 install_url 链接发给管理员，管理员在其浏览器完成授权

#### 数据走向验证

| 步骤 | 操作 | 数据流向 | 验证结果 | 问题 |
|------|------|----------|---------|------|
| 1 | 普通用户: B-01 完成 → FE 展示 install_url + 复制按钮 | — | ✅ 通过 | — |
| 2 | 普通用户: 复制链接，通过 IM 发送给管理员 | — | ✅ 通过 | — |
| 3 | **管理员**: 在自己的浏览器打开 install_url（不同 session） | 新浏览器 → TAPD | ✅ 通过 | 关键场景 |
| 4 | 管理员完成 TAPD 授权 → 回调 B-03 | TAPD → 后端 | ✅ 通过 | — |
| 5 | BE: B-03 从 query 取 state + sig → **HMAC 验签**（不查 Session） | 本地验签 | ✅ 通过 | A2 核心验证 |
| 6 | BE: upsert binding，`create_user = initiator`（原发起人） | MySQL 写入 | ✅ 通过 | — |
| 7 | BE: 302 重定向 `tapd_bind=success` | 管理员浏览器跳转 | ✅ 通过 | — |
| 8 | 普通用户: 刷新监控页面 → B-07 列表中项目标记 bound | 正常展示 | ✅ 通过 | — |

#### 关键设计点验证

| # | 设计点 | 验证问题 | 验证结果 | 问题 | 置信度 |
|---|--------|---------|---------|------|--------|
| 1 | A2: 签名串而非 Session | 管理员跨浏览器/账号是否能完成授权？ | ✅ 通过 | 不依赖 Session，仅验签，核心场景成立 | — |
| 2 | initiator 审计 | 跨浏览器时 create_user 能否记录原始发起人？ | ✅ 通过 | `signed_state.initiator` 在 state 中携带 | — |
| 3 | nonce 仅作签名盐 | nonce 不防重放是否安全？ | ✅ 通过 | B-03 重放=良性（upsert 幂等 + 管理员把关） | — |
| 4 | signed_state 放 query | 参数放 query（非 fragment）是否正确？ | ✅ 通过 | fragment 不发服务端，放 query 保证 callback 带回 | — |

#### 推演结论
- 数据走向：8 项通过 / 0 项存疑 / 0 项失败
- 关键设计：4 项通过 / 0 项存疑 / 0 项失败

---

### 3.5 🎬 场景五：应用态重复关联（幂等，Edge Case）

【执行者】普通用户
【场景描述】用户已关联某 TAPD 项目，再次发起关联（重复安装应用）

#### 数据走向验证

| 步骤 | 操作 | 数据流向 | 验证结果 | 问题 |
|------|------|----------|---------|------|
| 1 | FE: 前端 B-01 标记 `is_bound=bound`，理论上「去关联」按钮禁用 | 前端展示 | ✅ 通过 | — |
| 2 | 场景三全部步骤再走一次，TAPD 再次回调 B-03 | — | ✅ 通过 | — |
| 3 | BE: upsert binding，`unique_together` 冲突 → `ON DUPLICATE KEY UPDATE` | MySQL upsert | ✅ 通过 | — |
| 4 | MySQL: 同一 `(bk_tenant_id, space_uid, wd_id)` 记录未重复插入 | binding 表 | ✅ 通过 | — |
| 5 | BE: 正常返回 302 `tapd_bind=success` | 前端 | ✅ 通过 | — |

#### 关键设计点验证

| # | 设计点 | 验证问题 | 验证结果 | 问题 | 置信度 |
|---|--------|---------|---------|------|--------|
| 1 | B1 唯一约束 | 重复授权是否在数据层无异常？ | ✅ 通过 | upsert 幂等，无副作用 | — |
| 2 | 并发重复插入 | 多个请求同时写 binding 是否有竞态？ | ⚠️ **存疑** | MySQL `ON DUPLICATE KEY UPDATE` 有极小竞态窗口 | 🟡 低 |

#### 推演结论
- 数据走向：5 项通过 / 0 项存疑 / 0 项失败
- 关键设计：2 项通过 / 0 项存疑 / 1 项存疑（⚠️ 并发竞态）

---

### 3.6 🎬 场景六：查看项目列表（冷启动 B-01 / Happy Path）

【执行者】普通用户
【场景描述】用户态 OAuth 已授权 → 打开项目选择弹窗 → 查询用户可见 TAPD 项目（去关联用）

#### 数据走向验证

| 步骤 | 操作 | 数据流向 | 验证结果 | 问题 |
|------|------|----------|---------|------|
| 1 | FE: 打开项目选择弹窗 → 请求 B-01 | 请求: bk_biz_id, page, page_size | ✅ 通过 | — |
| 2 | BE: `TAPD_REQUIRED` 验 Redis key 存在 | Redis get | ✅ 通过 | — |
| 3 | BE: `IAMPermission` 校验 space 操作权限 | IAM 校验 | ✅ 通过 | — |
| 4 | BE: Redis 取 token + AES 解密 | Redis → 解密 access_token | ✅ 通过 | — |
| 5 | BE: Bearer Token 调 TAPD 用户态 API（分页） | TAPD API → 项目列表 | ✅ 通过 | — |
| 6 | BE: 本地查 `TAPD_WORKSPACE_BINDING` (by space_uid) | MySQL 查询 | ✅ 通过 | — |
| 7 | BE: app 级 Basic 调 `get_granted_workspaces` (带缓存) | TAPD API → 已授权列表 | ✅ 通过 | — |
| 8 | BE: 交叉标记四态 (bound/stale/importable/unbound) | 本地计算 | ✅ 通过 | — |
| 9 | BE: 生成 install_url (signed_state 烘进 cb query) | 签名工函数 | ✅ 通过 | — |
| 10 | FE: 接收 items + install_url + method | 前端展示 | ✅ 通过 | — |

#### 关键设计点验证

| # | 设计点 | 验证问题 | 验证结果 | 问题 | 置信度 |
|---|--------|---------|---------|------|--------|
| 1 | 四态逻辑 | 四种状态的交叉判定逻辑是否覆盖所有组合？ | ✅ 通过 | (binding×granted) 四种组合全部覆盖 | — |
| 2 | B-01 vs B-07 区分 | 冷启动用 B-01（用户态 Bearer）日常用 B-07（app Basic），不冲突 | ✅ 通过 | 职责清晰不同名 | — |
| 3 | get_granted_workspaces 缓存 | 10 并发 fan-out 加缓存后是否有效？ | ✅ 通过 | 文档说明已加短 TTL 缓存 | — |
| 4 | install_url 模板 | `{workspace_id}` 占位替换是否可行？ | ✅ 通过 | 前端替换后 GET 请求 | — |

#### 推演结论
- 数据走向：10 项通过 / 0 项存疑 / 0 项失败
- 关键设计：4 项通过 / 0 项存疑 / 0 项失败

---

### 3.7 🎬 场景七：查看项目列表（日常 B-07 / Happy Path）

【执行者】普通用户
【场景描述】日常建单时选择 TAPD 项目，直接从 B-07 查 app 已授权项目

#### 数据走向验证

| 步骤 | 操作 | 数据流向 | 验证结果 | 问题 |
|------|------|----------|---------|------|
| 1 | FE: 建单页面下拉 → 请求 B-07 | 请求: bk_biz_id, page, page_size | ✅ 通过 | — |
| 2 | BE: `IAMPermission` 校验（**无需** `TAPD_REQUIRED`，B-07 只走 IAM） | IAM 校验 | ✅ 通过 | — |
| 3 | BE: app 级 Basic Auth 调 `get_granted_workspaces` (带缓存) | TAPD API → app 已授权列表 | ✅ 通过 | — |
| 4 | BE: 本地查 `TAPD_WORKSPACE_BINDING` (by space_uid)，交叉标记四态 | MySQL + 计算 | ✅ 通过 | — |
| 5 | FE: 接收 items（建单用） | 下拉展示 | ✅ 通过 | — |

#### 关键设计点验证

| # | 设计点 | 验证问题 | 验证结果 | 问题 | 置信度 |
|---|--------|---------|---------|------|--------|
| 1 | B-07 免用户态 OAuth | 日常建单是否不需用户态授权？ | ✅ 通过 | B-07 数据源是 app 级 Basic，与用户态无关 | — |
| 2 | 现网资源迁移 | 现网 `ListTapdWorkspaceResource` 改名后影响？ | ⚠️ **存疑** | 全文未找到原资源路径，改名后 URL 是否兼容？ | 🟡 中 |

#### 推演结论
- 数据走向：5 项通过 / 0 项存疑 / 0 项失败
- 关键设计：2 项通过 / 0 项存疑 / 1 项存疑（⚠️ 现网资源改名兼容性）

---

### 3.8 🎬 场景八：非管理员复制链接（协作 Path）

【执行者】非项目管理员 → 复制 → 管理员
【场景描述】普通非管理员用户在 B-01 列表看到 unbound 项目 → 想关联 → 复制 install_url 给管理员

#### 数据走向验证

| 步骤 | 操作 | 数据流向 | 验证结果 | 问题 |
|------|------|----------|---------|------|
| 1 | 非管理员: B-01 返回 unbound 项目 + install_url | — | ✅ 通过 | — |
| 2 | 前端展示「复制链接给管理员」按钮 | — | ✅ 通过 | — |
| 3 | 非管理员点击复制，将 install_url（含 signed_state）发送给管理员 | 跨端传输 | ✅ 通过 | — |
| 4 | 管理员使用自己的账号在任意浏览器打开 install_url → 完成授权 | — | ✅ 通过 | — |
| 5 | B-03 回调：`initiator` = 非管理员 username → 写入 create_user | MySQL | ✅ 通过 | 审计归属清晰 |

#### 关键设计点验证

| # | 设计点 | 验证问题 | 验证结果 | 问题 | 置信度 |
|---|--------|---------|---------|------|--------|
| 1 | 复制链接传输安全 | signed_state 明文/密文传输是否安全？ | ⚠️ **存疑** | signed_state 虽 HMAC 签名但 URL 本身在 IM 中明文传输；需确认是否在有效期内可复制多次、链接泄露后被他人使用的风险 | 🟡 中 |
| 2 | link 有效期 | signed_state.expire_at 是否足够长？多长算合理？ | ❌ **存疑** | 设计未明确定义 expire_at 时长。太短：管理员未完成就过期；太长：暴露窗口加大 | 🟡 中 |

#### 推演结论
- 数据走向：5 项通过 / 0 项存疑 / 0 项失败
- 关键设计：2 项通过 / 0 项存疑 / 2 项存疑（⚠️ link 传输安全 + 有效期未定义）

---

### 3.9 🎬 场景九：用户态 OAuth CSRF 攻击（Exception Path）

【执行者】攻击者
【场景描述】攻击者构造假的 OAuth 回调请求到 B-05（无有效 state），企图注入虚假 token

#### 数据走向验证

| 步骤 | 操作 | 数据流向 | 验证结果 | 问题 |
|------|------|----------|---------|------|
| 1 | 攻击者: 直接访问 B-05 callback 并附带 code + 伪造 state | 请求到后端 | ✅ 通过 | — |
| 2 | BE: 从 Session 取 `tapd_oauth_state_{bk_biz_id}` → 与 state 字段比对 | Session 验证 | ✅ 通过（**比对失败**） | — |
| 3 | BE: state 不匹配 → **不重定向到业务页面** → 302 到错误页 | 阻断 | ✅ 通过 | — |
| 4 | BE: 记录安全日志 | 日志写入 | ✅ 通过 | — |
| 5 | 攻击者无法续走流程 | — | ✅ 通过（被阻断） | — |

#### 关键设计点验证

| # | 设计点 | 验证问题 | 验证结果 | 问题 | 置信度 |
|---|--------|---------|---------|------|--------|
| 1 | Session state 防 CSRF | 无 Session state 的伪造回调是否被阻断？ | ✅ 通过 | Session 验证 + 验证后删除防重放 | — |
| 2 | code 有效但 state 伪造 | 攻击者用偷来的 code 但无 state 是否能换取 token？ | ✅ 通过 | 需要 Session + code 双匹配 | — |

#### 推演结论
- 数据走向：5 项通过 / 0 项存疑 / 0 项失败
- 关键设计：2 项通过 / 0 项存疑 / 0 项失败

---

### 3.10 🎬 场景十：B-03 回调签名伪造攻击（Exception Path）

【执行者】攻击者
【场景场景】攻击者伪造 B-03 回调请求（假的 code/resource + 伪造 state/sig），企图绑定恶意 workspace

#### 数据走向验证

| 步骤 | 操作 | 数据流向 | 验证结果 | 问题 |
|------|------|----------|---------|------|
| 1 | 攻击者: 构造 B-03 回调请求，携带假 resource（workspace_id=攻击者项目） | 请求到后端 | ✅ 通过 | — |
| 2 | 攻击者: 伪造 state（base64 json，无有效 HMAC）和假 sig | query 参数 | ✅ 通过 | — |
| 3 | BE: `verify_signed_state(state, sig)` → HMAC(state, SECRET_KEY) != sig | 本地验签 | ✅ 通过（**验签失败**） | — |
| 4 | BE: 不计入 DB，302 重定向错误页 `?tapd_bind=error&reason=invalid_signature` | 错误处理 | ✅ 通过 | — |
| 5 | BE: 记录安全日志 | 日志写入 | ✅ 通过 | — |
| 6 | 攻击者无法绑定恶意 workspace | — | ✅ 通过（被阻断） | — |

#### 关键设计点验证

| # | 设计点 | 验证问题 | 验证结果 | 问题 | 置信度 |
|---|--------|---------|---------|------|--------|
| 1 | A2 HMAC 防伪造 | 不知道 SECRET_KEY 是否无法伪造有效 signed_state？ | ✅ 通过 | HMAC(SECRET_KEY) 不可伪造 | — |
| 2 | 安全日志 | 是否记录签名失败日志供审计？ | ✅ 通过 | 文档明确「记录安全日志」 | — |

#### 推演结论
- 数据走向：6 项通过 / 0 项存疑 / 0 项失败
- 关键设计：2 项通过 / 0 项存疑 / 0 项失败

---

### 3.11 🎬 场景十一：signed_state 已过期攻击（Exception Path）

【执行者】攻击者
【场景描述】攻击者从 IM 获取过期的 install_url（含 signed_state），试图在过期后触发 B-03 回调

#### 数据走向验证

| 步骤 | 操作 | 数据流向 | 验证结果 | 问题 |
|------|------|----------|---------|------|
| 1 | 攻击者: 获得旧 install_url（如 24h 前复制的） | 获取链接 | ✅ 通过 | — |
| 2 | 攻击者: 在 TAPD 打开（TAPD 无 state 过期校验，仍回调） | 回调到 B-03 | ✅ 通过 | — |
| 3 | BE: B-03 取 state + sig → **验签通过**（HMAC 正确） | 本地验签 | ✅ 通过 | — |
| 4 | BE: json.loads(base64url_decode(state))["expire_at"] > now() | 过期校验 | ✅ 通过（**已过期，拒绝**） | — |
| 5 | BE: 302 重定向错误页 `tapd_bind=error` | 拒绝写入 | ✅ 通过 | — |

#### 关键设计点验证

| # | 设计点 | 验证问题 | 验证结果 | 问题 | 置信度 |
|---|--------|---------|---------|------|--------|
| 1 | expire_at 时效校验 | 过期 state 是否被正确拦截？ | ✅ 通过 | expire_at 为独立校验环节 | — |
| 2 | 攻击者利用旧链接 | 旧 state 泄露后过期不可复用 | ✅ 通过 | 过期后拒绝且无法伪造新 state | — |

#### 推演结论
- 数据走向：5 项通过 / 0 项存疑 / 0 项失败
- 关键设计：2 项通过 / 0 项存疑 / 0 项失败

---

## 4. 问题汇总

### 📊 问题汇总

#### 已确认/已解决项（用户确认）

| # | 类型 | 角色 | 场景 | 问题描述 | 确认结论 | 状态 |
|---|------|------|------|---------|---------|------|
| 1 | 遗漏场景 | 普通用户 | 场景八 | `signed_state.expire_at` 时长未定义 | **接受建议**：默认值 30min，写入 S-03 §3.2 / §4a | ✅ 已确认 |
| 2 | 流程缺陷 | 所有用户 | 场景七 | 现网 `ListTapdWorkspaceResource` 改名兼容性 | **已澄清**：现网接口保持不变，B-01 为新增接口 `ListUserVisibleTapdWorkspaceResource`，无兼容影响 | ✅ 已确认 |
| 7 | 边界缺陷 | 所有用户 | 场景六 | 四态 `stale` 后续操作流程未定义 | **已确认**：`stale` 项目走重新关联流程（与 `unbound` 项目类似操作路径） | ✅ 已确认 |
| 8 | 遗漏场景 | TAPD 系统 | B-03 回调 | `resource` 结构未确认 | **已确认**：dependencies.md §2.3 已定义 `{"type":"workspace","workspace_id":"69990779"}`，结构稳定 | ✅ 已确认 |

#### 待处理项（用户暂忽略，开发前建议关注）

| # | 类型 | 角色 | 场景 | 问题描述 | 建议 | 严重度 |
|---|------|------|------|---------|------|:------:|
| 3 | 数据问题 | TAPD 系统 | 场景五 | 并发竞态：`ON DUPLICATE KEY UPDATE` 在并发插入时可能的竞态窗口 | 在 S-03 补充并发保护策略（如 `get_or_create` + 事务或异常捕获兜底） | 🟡 |
| 4 | 边界缺陷 | 非管理员 | 场景八 | install_url（含 signed_state）在 IM 中明文传输，被中间人截获后可在有效期内使用 | 评估是否 signed_state 内加入用户指纹参数或限制使用次数 | 🟡 |
| 5 | 设计遗漏 | 普通用户 | 所有场景 | 设计未说明 `install_url` 中 `{workspace_id}` 占位符被替换后仍保持 URL 编码正确性 | 在 S-04 补充前端替换的 URL 编码要求说明 | 🟢 |
| 6 | 遗漏场景 | 普通用户 | 场景四 | B-03 回调后管理员浏览器被重定向，普通用户需手动刷新才能看到更新状态；设计未提到主动通知机制 | 在 S-03 或需求中明确：callback 成功后是否通过 WebSocket/轮询通知普通用户更新 | 🟢 |

### 📈 统计

| 严重度 | 数量 | 状态 |
|--------|------|------|
| 🔴 阻断 | 0 个 | — |
| ✅ 已确认（解决） | 4 个 | P-1 ~ P-2, P-7 ~ P-8 已由用户确认解决 |
| 🟡 警告 | 2 个（暂忽略） | P-3（并发竞态）、P-4（link 传输安全） |
| 🟢 建议 | 2 个（暂忽略） | P-5（URL 编码）、P-6（状态同步通知） |

---

## 5. 推演结论

### 5.1 整体评估

| 指标 | 数值 |
|------|------|
| 推演覆盖 | **4** 个角色（普通用户、管理员、非管理员、外部系统） / **11** 个场景 |
| 数据走向验证 | **95** 项通过 / **0** 项存疑 / **0** 项失败 |
| 关键设计点验证 | **34** 项通过 / **0** 项存疑 / **2** 项存疑（低/中） |
| 问题发现 | 🔴 **0** 个阻断 / ✅ **4** 个已确认解决 / 🟡🟢 **4** 个暂忽略 |

### 5.2 评审结论

| 条件 | 结论 |
|------|------|
| 是否存在 ≥1 个 🔴 阻断？ | **否** |
| 是否存在 ≥1 个 🟡 警告？ | **是**（2 个，用户选择暂忽略） |
| 评审结论 | ⚠️ **有条件通过** |

> 说明：P-1（expire_at 30min）、P-2（现网接口不变/B-01 新增）、P-7（stale 走重新关联）、P-8（resource 结构已确认 via dependencies.md）经用户确认后关闭。P-3 ~ P-6 由用户选择暂忽略，开发前建议关注。

### 5.3 下一步建议

1. **已实现确认项的开发输入**：
   - P-1 → S-03 设计文档补充：`signed_state.expire_at = now() + 1800`（30min 固定值）
   - P-7 → S-04 设计文档补充：stale 状态前端展示「重新关联」按钮，操作路径同 unbound
   - P-8 → B-03 代码实现中，依据 dependencies.md §2.3 的 resource 结构做兼容处理

2. **开发前建议关注的暂忽略项**（按优先级）：
   - **P-3**（并发竞态）：B-03 upsert 建议用 `get_or_create` + `IntegrityError` 捕获兜底，实现简单且覆盖边界
   - **P-4**（link 传输安全）：30min 过期窗口较短，如后续业务安全基线要求高，可考虑在 signed_state 中嵌入 `request.META['REMOTE_ADDR']` 指纹校验
   - **P-5 ~ P-6**：前端开发前补充即可，不阻塞后端

3. **如需增量推演**：
   - 本次推演已无阻断项，可进入开发阶段
   - 若 S-03 emit expire_at 变更或 stale 操作流程变更，可增量推演对应场景

### 5.4 确认记录

| 问题 | 用户决策 | 更新时间 |
|------|---------|---------|
| P-1: expire_at 时长未定义 | 接受 30min 默认值建议 | 2026-06-22 |
| P-2: ListTapdWorkspaceResource 改名兼容 | 已澄清—现网接口不变，B-01 为新增 | 2026-06-22 |
| P-7: stale 后续操作流程 | stale 走重新关联流程 | 2026-06-22 |
| P-8: resource 结构未确认 | dependencies.md §2.3 已定义，结构稳定 | 2026-06-22 |
| P-3 ~ P-6 | 暂时忽略 | 2026-06-22 |

---

> **推演免责声明**：本次推演基于设计文档（截至 2026-06-18 v2）进行，不涉及代码实现细节。外部依赖（TAPD OAuth/resource 结构）以「已确认」假设推演，实际实施前需逐项核实。
>
> **更新记录**：
> - v1（2026-06-22）初始推演，发现 1 个 🔴 阻断 + 6 个 🟡 + 2 个 🟢
> - v2（2026-06-22）用户确认 P-1/P-2/P-7/P-8 关闭；P-3~P-6 暂忽略 → 评审结论变更为 ⚠️ 有条件通过
