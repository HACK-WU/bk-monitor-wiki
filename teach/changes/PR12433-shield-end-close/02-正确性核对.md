# 正确性核对：PR#12433 屏蔽结束后静默关闭告警

> 本文为完整讲解（Phase 0→1→2→3→4→5→6→7）。变更规模跨 36 文件（> 20 阈值），按规范采用完整模式。

> 质量闸门，不可跳过。核对对象 = 00-变更大纲.md + 01-逐处解读.md 的全部结论。
> 核对方法 = 回读全量 diff（/tmp/teach-pr12433.diff，2192 行）+ 回读工作区源码（HEAD 状态即含本变更）逐条比对。

## 一、核对结论总览

| # | 前序结论（出处） | 核对证据 | 判定 |
|---|---|---|---|
| 1 | ShieldEndPolicy 枚举 notify_once/close（大纲） | [constants/shield.py:54](file://bkmonitor/constants/shield.py#L54) 实测 `class ShieldEndPolicy:` | ✅ 确认 |
| 2 | Shield.end_policy 默认 notify_once 存量零迁移（解读组1） | [models/base.py:803](file://bkmonitor/bkmonitor/models/base.py#L803) + 迁移文件 AddField default="notify_once" | ✅ 确认 |
| 3 | AlertDocument.shield_end_close 布尔字段（解读组1） | [documents/alert.py:83](file://bkmonitor/bkmonitor/documents/alert.py#L83) `shield_end_close = field.Boolean()` | ✅ 确认 |
| 4 | CloseShieldMatcher 过滤四条件（解读组2） | diff L378-L385：end_policy=close + is_enabled + 非 deleted + category∉(alert,event) + 非 is_quick | ✅ 确认 |
| 5 | 候选选择 (end, -shield_id) 取 max（解读组2） | diff L417-L421：`candidates.append((end, -int(shield.id), begin, shield_id))` 后 `max(candidates)` | ✅ 确认 |
| 6 | take_over 首行粘滞守卫（解读组2） | [close.py:22-24](file://bkmonitor/alarm_backends/service/converge/shield/close.py#L22-L24)：`if alert.shield_end_close or not alert.is_abnormal(): return` | ✅ 确认 |
| 7 | matching_window 跨天窗口截断（解读组2） | diff L540-L565：start>end 时 finish+1d/begin-1d + matcher.is_match 校验凌晨边界日 | ✅ 确认 |
| 8 | AlertShieldObj 语义分流（解读组2） | diff shield_obj.py：`_parse_cycle_config` 与 `is_match` 均按 `end_policy=="close"` 分流，非 close 走 super()/elif 保持原判定 | ✅ 确认 |
| 9 | saas_config 缓存短路二次确认（解读组2） | [saas_config.py:56](file://bkmonitor/alarm_backends/service/converge/shield/shielder/saas_config.py#L56) 注释原文 + 过滤条件 `end_policy != "close" or shield.is_match(self.alert)` | ✅ 确认 |
| 10 | Alert.update 托管分支：RECOVERED 不终结（解读组3.1） | diff alert.py L164-L172：`clear_next_status()` + 仅同 severity ABNORMAL 更新 event + add_log + return；注释原文一致 | ✅ 确认 |
| 11 | should_send_signal 三重条件（解读组3.1） | diff alert.py L614-L617：`self._refresh_db and not self.is_blocked and not self.shield_end_close` | ✅ 确认 |
| 12 | builder 两处调用点（解读组3.2） | diff processor.py L378-L392（存量分支 take_over + 命中即 update+continue）与 L438（新建回写前 take_over） | ✅ 确认 |
| 13 | send_check_task 过滤 + 日志改记实际投递（解读组3.2） | diff processor.py L192-L207：过滤条件加 `not alert.shield_end_close` + `if not alerts_params: return` + 日志改从 alerts_params 派生 | ✅ 确认 |
| 14 | rule_assign 跳过分派（解读组3.3） | diff rule_assign.py：enrich_alert 首行 `if alert.shield_end_close: return alert` + 中文注释原文 | ✅ 确确认 |
| 15 | filter_alerts 双重排除（解读组3.4） | diff manager/processor.py：fields 补 shield_end_close + 入参直滤 + missing_keys 快照回读排除 + 缓存二次确认补 shield_end_close | ✅ 确认 |
| 16 | send_signal 排除（解读组3.6） | diff service/alert/processor.py：`if alert.is_blocked or alert.shield_end_close: blocked += 1; continue` | ✅ 确认 |
| 17 | composite 排队复核（解读组3.7） | [composite/tasks.py:58](file://bkmonitor/alarm_backends/service/composite/tasks.py#L58)（alert 加载后、bk_biz_id 检查前第四道前置检查，全文件回读确认） | ✅ 确认 |
| 18 | create_action 三联动改写（解读组3.8） | diff create_action.py L357-L382：过滤 + alert_ids 派生 + severity 空列表防御，三处逐字对上 | ✅ 确认 |
| 19 | 关闭任务六步流程（解读组4 Mermaid 图） | diff shield_tasks.py 本体逐段比对：ES search_after 分批→Alert.mget→multi_service_lock→锁内二次复读→owner 快照校验→in_bulk 判活跃→set_end_status→先 ES 后缓存 | ✅ 确认 |
| 20 | 四条 fail-safe 路径（解读组4） | diff shield_tasks.py：owner 缺失 error+continue / shield_is_active 异常仅 exception 日志 / SQL 异常整批抛出（in_bulk 外层无 try）/ BulkIndexError 剔除失败 ID 继续 | ✅ 确认 |
| 21 | 既有扫描排除（解读组4） | diff manager/tasks.py：check_abnormal_alert 加 `~Q("term", shield_end_close=True)`、check_blocked_alert 同款、check_blocked_alert_finished 锁内复核 | ✅ 确认 |
| 22 | cron 注册 `* * * * *` cluster（解读组4） | [worker.py:229](file://bkmonitor/config/role/worker.py#L229) 实测 | ✅ 确认 |
| 23 | 接口层 close 拒绝校验（解读组5） | diff serializers.py validate + 测试断言（alert/event/快捷拒绝） | ✅ 确认 |
| 24 | EditShieldResource 不可修改校验（解读组5） | diff backend_resources.py + 测试 `test_edit_rejects_entire_change_before_mutation`（handle_time 未被调用，整次拒绝） | ✅ 确认 |
| 25 | 前端共享组件两段常驻提示（解读组6） | [alarm-shield-end-policy.vue:13](file://bkmonitor/webpack/src/monitor-pc/pages/alarm-shield/alarm-shield-components/alarm-shield-end-policy.vue#L13)（notify_once 默认 + 提示文案） | ✅ 确认 |
| 26 | Trace 端事件类屏蔽不渲染该字段（解读组6） | diff alarm-shield-config.tsx：条件展开 `...(tabData.active !== EShieldType.Event ? { end_policy } : {})` | ✅ 确认 |
| 27 | 测试 60 用例 6 文件（解读组7） | 实测 def test_ 函数数 + parametrize 展开：15+19+9+10+5+2 = 60 | ✅ 确认（修正后） |

**27 项结论：26 项确认，0 项存疑，1 项已在 Phase 2 评审中修正（测试计数），修正后全部闭环。**

## 二、Phase 2 评审已修正项（级联检查）

| 修正项 | 修正内容 | 级联影响 |
|---|---|---|
| 测试计数 test_shield_end_policy | 9 → 15（8 函数参数化展开） | 00-变更大纲.md 若引用了 9 需同步；五幕对照"约 60"已改为精确 60 |
| 测试计数 test_shield_close | 20 → 19（14 函数参数化展开） | 同上 |

级联检查：00-变更大纲.md 的测试行需回读确认是否含旧计数。见下节。

## 三、级联回读：00-变更大纲.md

大纲中"规模与噪音改动"与 36 文件全景表为主要事实载体。逐项回读核对：

- **36 文件 +1411/-35**：与 diffstat（/tmp/teach-diffstat.txt）一致，无需修正。
- **7 个意图分组**：与 01-逐处解读.md 分组一致。
- **噪音改动标注**（dimension.tsx key 修正、CSS lint、object 括号清理）：diff 中均确认为纯风格改动，与解读组1/组6 的"噪音改动"小节一致。
- 大纲未含测试用例计数（仅列文件），**无级联修正需求**。

## 四、核对方法说明（可复现）

- diff 是唯一事实源：/tmp/teach-pr12433.diff（`git diff HEAD` 产物，分支 feat/shield-window-close/#1010158081138095166，HEAD=f7e7cfda5c）。
- 工作区源码 = HEAD + 未提交变更 = 变更后状态，直接 grep/read 即为 after 状态；before 状态从 diff 的 `-` 行读取。
- 行号引用均为变更后工作区行号（与 01-逐处解读.md 的 file:// 引用一致）。

## 五、质量闸门判定

**通过**。27 项结论全部有证据支撑，Phase 2 评审修正的 2 处测试计数已级联闭环，无遗留存疑项。

存在须知项（非阻塞，供后续阶段引用）：
1. `is_match` 的 close 分支对 `AlertShieldObj` 无条件生效——非 close 屏蔽走 elif 原路径，行为不变；但所有构造 `AlertShieldObj` 的调用方（如 saas_config.py Shielder）自动获得新构造签名兼容（`business_timezone_name` 缺省 None 时自动解析）。
2. `shield_tasks.py` 的 `shield_is_active` 对 `shield.cycle_config` 缺 type 字段的行为：`close_time_matcher` 用 `int(cycle.get("type", -1))`，-1 未命中映射表回退 `TimeMatchBySingle`——单窗口屏蔽（无周期）落到该分支，is_match 按 begin/end 判定，行为合理。
