# 评审清单：ES 告警索引轮转（ILM）

> 评审强制检查点。Phase 1 落盘时占位（`- [ ]`），各节点评审完成且 P0 清零后勾选（`- [x]`）。
> 未勾选条目 = 评审未完成，断点续学时会先补做。

- [x] 大纲评审（`00-能力大纲.md`）— pedagogy + **专项维度 D8**（零术语可达性）｜ P0=0（原 A-1 三要素缺失，已补：一句话本质 / 直觉主图 / 特性卡）
- [x] 解读评审（`01-代码wiki.md`）— pedagogy + learner ｜ P0=0
- [x] 质量闸门（`02-正确性核对.md`）— 1 项修正 / 1 项存疑，已级联同步 ｜ P0=0
- [x] 收尾评审（`06-最终文档.md`）— pedagogy + learner + **专项维度 D8**（零术语三要素汇总后是否保留）｜ P0=0

> **维度必须写进条目**：清单粒度到「维度」而非只到「节点」——只写"大纲评审"时，评审者有没有跑 D8 无从判定，断点续学也发现不了漏查。

## 规则升级复评（2026-09-11）

`module-teach` 规则更新后，按新版对既有产物做了一次全量复评，并按用户确认的顺序修复。

| # | 新增强制项 | 复评结论 | 处置 |
|---|---|---|---|
| 1 | A-1 零术语开场（一句话本质 / 量化处境 / 直觉主图 / 特性卡） | ❌ 缺失 | ✅ 已补入 `00-能力大纲.md` 第零节 与 `06-最终文档.md` 第零节；新增 `assets/intuition-ledger-books.svg` |
| 2 | 每张图带读图指引 | ❌ 缺失 | ✅ 已补 4 张图（直觉主图 / 索引布局 SVG / update_index 判定 / reindex 搬运 / 相邻系统关系） |
| 3 | Phase 1.5 外部文档索引 | ❌ 未执行 | ✅ 已补「外部文档索引」小节，记录不建索引的判定与依据 |
| 4 | 变更类每份产物顶部模式标注 | ⚠️ 缺 5 份 | ✅ PR12433 的 `02`/`03`/`04`/`05`/`06` 已补齐 |
| 5 | 最终文档不使用 HTML | ⚠️ 违规 | ✅ 两张 archify HTML 已按规范 JSON 改写为手写 SVG，HTML 已删除，全部引用改为 SVG |

**HTML 处置说明**：原 archify 交付的两个交互 HTML 依赖浏览器渲染，与「Markdown 唯一主载体，不使用 HTML」冲突。因环境无 Chrome 无法走无头导出，改为依据 `archify/*.json`（规范源）手写等价 SVG，节点、连线、分组与原文一致。JSON 规范源保留，HTML 已删除。

## 规则升级复评（2026-09-14 · 第二轮）

规则再次更新，新增 D9 术语对标、讲解路线并入 D8⑤、评审清单须粒度到维度、概念类图来源写法等约束。复评与处置如下。

| # | 新增强制项 | 复评结论 | 处置 |
|---|---|---|---|
| 1 | **D9 术语对标**（自造词须给业界叫法 + 代码位置） | ❌ 全库 0 处 | ✅ `00-能力大纲.md` 「四、关键抽象」下新增术语对标表（物理索引 / 写别名 / 读别名 / update_index / slice / reindex 搬运 6 条） |
| 2 | **D8⑤ 讲解路线** | ❌ 缺失 | ✅ `00-能力大纲.md` 新增「六点五、讲解路线」；`profile` / `issue-periodic-tasks` 同步补入 |
| 3 | 评审清单须粒度到维度 | ❌ 原文件为评审记录、无 `- [ ]` 条目 | ✅ 本文件重写为占位式清单，四条均标注维度与实例数 |
| 4 | 概念类图来源写法（须多文件并列或标架构性总结） | ⚠️ 直觉主图仅单文件且缺 `file://` | ✅ 改为三文件行号区间并列，补 `file://` 前缀 |
| 5 | `## 目录` 条目 | ❌ 7 份产物缺 | ✅ 全部补齐 |
| 6 | 顶部 cite 块（本文引用的文件） | ❌ 7 份产物缺 | ✅ 全部补齐 |

**复评后 P0 数：0。** 本档案 A-1 三要素在第一轮已具备，本轮为新增维度补齐。

## 各节点评审明细

### 大纲评审（Phase 1）— pedagogy 单实例 + D8

| D8 锚点 | 结论 |
|---|---|
| ① 一句话本质且通篇无技术名词 | ✅ "把一本越写越厚的总账，改成按天装订的一册册薄本子" |
| ② 零术语直觉主图（SVG、图上无术语、带读图指引） | ✅ `assets/intuition-ledger-books.svg`，图上无技术名词，图下有「看图：」指引 |
| ③ 特性卡给出"代价" | ✅ 三行均列代价（跨切片查询需自行拼清单 / 写入放大 / 写入与查询位置错位） |
| ④ 术语首次登场回指直觉层说法 | ✅ 写别名 / 读别名 / 物理索引 / 索引模板首次出现均给一句人话解释 |
| ⑤ 讲解路线 | ✅ 本轮补齐 |

**P0：0 ｜ P1：0**

### 解读评审（Phase 2）— pedagogy + learner 双实例

| 维度 | 结论 |
|---|---|
| D1 可溯源 | ✅ 全文 `file://` 引用带行号区间，无单点 |
| D2 无编造 | ✅ 无法判定的项列为"待确认"（Q1–Q5），未猜测 |
| D3 范围标注完整 | ✅ 通用 / 专用逐点标注，`[通用]` 部分无外部链接（无编造风险） |
| D4 认知阶梯不倒序 | ✅ 感知层 → 概念层 → 机制层 → 实操层 → 定位层 |
| D6 图表选型 | ✅ 流程 / 状态 / 序列用 Mermaid，结构密集图用 SVG 入 `assets/`；每图带来源与读图指引 |

**P0：0 ｜ P1：0**

### 质量闸门（Phase 3）

1 项修正（已级联同步至 `00`/`01`）、1 项存疑（已在 `02` 标注，不影响放行）。

**P0：0**

### 收尾评审（Phase 7）— pedagogy + learner 双实例 + D8

汇总后 A-1 三要素完整保留（第零节），`[通用]`/`[专用]` 标注与各阶段 `章节来源` 未丢失。

**P0：0 ｜ P1：0**

> **独立性说明**：本档案全程为**主 agent 内联评审**（`course-reviewer` 子 agent 未创建）。主 agent 同时扮演生成者、评审者与裁决者，独立性弱于独立子 agent。若需更强独立性，可按 `create-sub-agent` 规范创建 `course-reviewer` 后重跑。
