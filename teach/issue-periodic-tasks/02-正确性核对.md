# 正确性核对（质量闸门）

> 回读实际代码，逐条校验 Phase 1–2 的结论。判定：已确认 / 存疑 / 有误。

## 核对清单

| # | 前序结论 | 判定 | 代码证据 |
|---|---|---|---|
| 1 | issue 只有 2 个 beat 周期任务 | 已确认 | `config/role/worker.py` 全文 grep `issue` 仅 L241 / L243 两条 crontab 注册 |
| 2 | `generate_issue_llm_title` 不是周期任务 | 已确认 | 无 crontab 注册；仅 `issue_processor.py#L179` `apply_async` 派发 |
| 3 | `sync_issue_alert_stats` cron = `*/5 * * * *`，cluster 模式 | 已确认 | `worker.py#L241` |
| 4 | `refresh_issue_llm_title_examples` cron = `0 * * * *`，cluster 模式 | 已确认 | `worker.py#L243` |
| 5 | 两任务队列均为 `celery_action_cron` | 已确认 | `issue_tasks.py#L47`、`L1438` |
| 6 | `generate_issue_llm_title` 队列 `celery_llm_task`，软 60s / 硬 90s | 已确认 | `issue_tasks.py#L1084` |
| 7 | 周期任务含 5 项职责 | 已确认 | docstring `issue_tasks.py#L48-L56` 明列 5 条 |
| 8 | orphan 阈值 300s | 已确认 | `ORPHAN_ISSUE_THRESHOLD_SECONDS = 300`（L39），使用处 L204 |
| 9 | Issue scan 页大小 500 | 已确认 | `ISSUE_SCAN_PAGE_SIZE = 500`（L40），使用处 L831 |
| 10 | Alert scan 页大小 500 | 已确认 | `ALERT_SCAN_PAGE_SIZE = 500`（L41），使用处 L852 |
| 11 | 进度日志每 100 条 | 已确认 | `PROGRESS_LOG_INTERVAL = 100`（L44），使用处 L84 |
| 12 | backfill 扫描下界 7 天上限 | 已确认 | `_BACKFILL_ALERT_SCAN_MAX_LOOKBACK_SEC = 7 * 86400`（L231），使用处 L320 |
| 13 | `last_alert_time` 毫秒转秒 | 已确认 | L190 `int(raw_max / 1000)` + L188-L189 注释 |
| 14 | 哨兵 TTL 30 天 | 已确认 | `core/cache/key.py#L1259` `"ttl": 30 * CONST_ONE_DAY` |
| 15 | 哨兵 key 为 `issue.legacy_migration.done` | 已确认 | `core/cache/key.py#L1258` |
| 16 | 续命为双重确认（exists → ES 探查 → set） | 已确认 | `issue_tasks.py#L124-L153` |
| 17 | 匹配排序 = live 优先 + 维度数降序 | 已确认 | `_match_order_key`（L310-L313）返回 `(is_live, -len(agg_tuple))` |
| 18 | 时间边界命中后 `break` 不回退 | 已确认 | L352-L357（`matched = True` + `break`） |
| 19 | `_allowed_scope_keys` 三态语义 | 已确认 | docstring L410-L424 明写 None / set() / {...} 区别 |
| 20 | impact_scope 实例列表截 `[:50]` | 已确认 | 多处（L648 / L660 / L675 / L756 等） |
| 21 | 两 scan 工具用 `search_after` | 已确认 | L833 / L854 `extra(search_after=search_after)` |
| 22 | LLM 只对 `alert_not_found` 重试，延迟 1s/3s | 已确认 | `ALERT_LOOKUP_RETRY_DELAYS = (1, 3)`（L867）+ L1115 条件 |
| 23 | rename operator 恒为 `system` | 已确认 | L1075 `issue.rename(title, operator="system", ...)` |
| 24 | `enforce_unique=False` | 已确认 | L1075 同调用 |
| 25 | refresh 任务白名单空则 return | 已确认 | L1447-L1451 |
| 26 | refresh 扫描上限 2000 条 | 已确认 | `max_scan = 2000`（L1457） |
| 27 | refresh 扫描近 30 天、排除 operator=system | 已确认 | L1455 `scan_start = now - 30 * 24 * 3600`；L1466 `exclude("term", operator="system")` |
| 28 | `migrate_legacy_active_issues` 自身不 set 哨兵 | 已确认 | `documents/issue.py` docstring L1036-L1043 + noop 分支 L1073-L1074 + 收尾 L1111-L1112，三处明写「本函数不直接 set 哨兵」 |
| 29 | 哨兵唯一写入点 = `_mark_legacy_migration_done()`，唯一调用点 = `issue_tasks.py#L146` | 已确认 | 全仓 grep `legacy_migration` 仅 1 处 `.set(`（`documents/issue.py#L1133`）、1 处调用（`issue_tasks.py#L146`） |

## 修正记录

| # | 原结论 | 修正为 | 来源 |
|---|---|---|---|
| 1 | （Phase 1 原文写"哨兵不存在 + 确认无 legacy 时重新 set"，未强调 Redis 存在即早退） | 补充：正常路径为 Redis `exists` 命中即 return，几乎零开销；仅哨兵缺失才触发 ES 探查 | `issue_tasks.py#L124-L127` |
| 2 | （Phase 2 未说明 refresh 的 `max_scan` 与 30 天窗口的具体行） | 补充精确行号：L1455（30 天）、L1457（2000 上限）、L1466（排除 system） | `issue_tasks.py#L1455-L1466` |
| 3 | （Phase 1 表格中"Legacy 哨兵续命"描述为"哨兵不存在 + 确认无 legacy 时重新 set"） | 细化为三态：Redis 命中→跳过；legacy=0→续命；legacy>0→仅 warning 交下次部署 | `issue_tasks.py#L124-L153` |
| 4 | **【有误】** `01-代码wiki.md` L70 与 `06-最终文档.md` L101 均写「迁移完成后在 Redis 写全局哨兵」，暗示哨兵由 `migrate_legacy_active_issues` 设置 | 迁移只 RESOLVE 不写哨兵；哨兵唯一写入点 `_mark_legacy_migration_done()`（`documents/issue.py#L1116-L1135`），唯一调用点是周期任务 `issue_tasks.py#L146`，初始 set 与续命同源。原因：post_migrate 跑在 web/saas role，其 settings 缺 `REDIS_*_CONF`，`core.cache.key` 模块加载会抛 `AttributeError` 导致 migrate 非 0 退出。代价：哨兵未 set 期间 processor 多走 fallback，最大窗口 1 个周期，仅性能无损正确性 | `documents/issue.py#L1036-L1043`（docstring）、`#L1073-L1074`（noop 分支）、`#L1111-L1112`（收尾）、`#L1121-L1123`（`_mark_legacy_migration_done` docstring） |

## 存疑项

无。

## 级联检查记录

| 被修正结论影响的产物 | 更新项 |
|---|---|
| `00-能力大纲.md` | 哨兵续命条目已按三态描述（原文即含"哨兵不存在 + 确认无 legacy 时重新 set"，与修正 3 一致，无需改动） |
| `01-代码wiki.md` | ① 哨兵续命小节已含 Redis 早退描述（L124-L127），与修正 1 一致；② 术语回指已按新人视角 P1 插入；③ 3.2 标题行号区间 `#L234-L384` 经核对无误；④ **【第二轮修正】** L70「迁移完成后在 Redis 写哨兵」已改为「只 RESOLVE 不写哨兵」，并新增「反直觉点」段说明 role 隔离原因与 5min 窗口代价 |
| `02-正确性核对.md` | 本文，无自引用 |
| `06-最终文档.md` | **【第二轮修正】** L101 同样的错误表述已同步修正为「迁移函数不写哨兵 / 初始 set 与续命同源」，与 `01-代码wiki.md` 口径一致 |

> 结论：26 条已确认（#1–#27，其中 #28/#29 为第二轮补充）/ 1 条细化修正 / **1 条有误（修正 4，已修复）** / 0 条存疑。
>
> 第二轮修正说明：用户追问「设置哨兵的代码在哪 + processor 跳过在哪」时回读源码，发现修正 1/3 虽把续命三态描述准确了，但**初始 set 的归属**写错了——把「迁移完成时写哨兵」当成了前提。实际该前提不成立：迁移函数因 role 隔离无法触碰 Redis，哨兵从头到尾只有周期任务一个写入点。已同步修正 `01` / `06` 两份产物。
