# 评审清单：APM Profiling 实现

> 评审强制检查点。Phase 1 落盘时占位（`- [ ]`），各节点评审完成且 P0 清零后勾选（`- [x]`）。
> 未勾选条目 = 评审未完成，断点续学时会先补做。
> **评审执行方式**：主 agent 内联（未委派独立子 agent，独立性受限）。如需更独立的评审，可创建 `course-reviewer` 子 agent 后重跑。

- [x] 大纲评审（`00-能力大纲.md`）— pedagogy + **专项维度 D8**（零术语可达性）｜ P0=0（本轮补齐 A-1 三要素后复评）；另有 6 条 P1 已修（双管线声明 / 依赖描述 / 依赖图 / 章节来源 / 抽象补全 / 子模块表补标注）
- [x] 解读评审（`01-代码wiki.md`）— pedagogy + learner ｜ 双实例 P0 并集 8 条已修（术语入门节 + 系统位置 SVG + 双管线前置 + pprof 地基 + 4 处行号纠正）
- [x] 质量闸门（`02-正确性核对.md`）— 24 条已确认，0 有误 / 0 存疑，4 条修正已级联同步
- [x] 收尾评审（`06-最终文档.md`）— pedagogy + learner + **专项维度 D8**（零术语三要素汇总后是否保留）｜ P0 并集 2 条已修（聚合规则改为「查表+静默回退 SUM+三值」、入门节上移至大纲前）+ 4 处单点行号改区间 + 时间单位口径不一致标注
- [x] 术语对标（**专项维度 D9**）— 全产物 ｜ 本轮新增术语对标表 8 条，无编造业界叫法

## 规则升级复评（2026-09-14）

规则新增 D9 术语对标、讲解路线并入 D8⑤、评审清单须粒度到维度等约束，本档案复评与处置如下。

| # | 新增强制项 | 复评结论 | 处置 |
|---|---|---|---|
| 1 | **D8 ①–③**：一句话本质 / 零术语直觉主图 / 特性卡 | ❌ 三项全缺（P0） | ✅ 已补；新增 `assets/intuition-photo-stack.svg`（图上零术语 + 读图指引） |
| 2 | **D8 ⑤ 讲解路线** | ❌ 缺失 | ✅ 已补入 `00-能力大纲.md` |
| 3 | **D9 术语对标** | ❌ 缺失 | ✅ 「关键抽象」下新增术语对标表 8 条（`Profile` / `FunctionTree` / `value`·`self_value` / `ConverterType.Tree` / 聚合方法 / `Diagrammer` / pyroscope 回灌 / `perf_script`），重点标注"页面主链路不用 `Profile`"这一最大坑 |
| 4 | **外部文档索引节**（纯专用也须写"无"） | ❌ 缺节 | ✅ 已补，记为"无（未收集）" |
| 5 | 每图带读图指引（D6） | ❌ 11 张图全缺 | ✅ 已逐图补齐（依赖图 / 数据流 SVG / 查询时序 / 上传链路 / 状态机 等） |
| 6 | cite 块 + `## 目录` | ⚠️ `02`/`06` 缺 cite、`02` 缺目录 | ✅ 已补齐 |
| 7 | 评审清单须标注维度 | ⚠️ 未标 D8/D9 | ✅ 本清单已补维度标注 |

**复评后 P0 数：0。**
